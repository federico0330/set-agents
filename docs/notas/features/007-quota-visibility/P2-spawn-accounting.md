# 007-quota-visibility · P2-spawn-accounting

<!-- notas:auto -->
## Motivo

- objetivo: Persistir en el expediente el usage que el spawn ya recibe y hacer visible el gasto por carril incluido Pi, para que el costo de la delegacion deje de ser el unico eje sin traza
- complejidad: high
- riesgo: Sube SCHEMA a 6: toda base schema-5 existente deja de abrir si migration_required no se generaliza. La posicion de las …
- paths: `ai/scripts/routing_core/store.py`, `ai/scripts/set_agents_app.py`, `ai/scripts/set_agents_spawn.py`, `ai/scripts/cost-report.py`, `TIPS-USO.md`, `docs/adr/0010-spawn-accounting.md`, `docs/adr/README.md`, `docs/specs/007-quota-visibility/**`, `ai/state/features/007-quota-visibility.json`, `ai/state/STATUS.md`, `ai/state/decisions-log.jsonl`, `ai/state/narrative-log.jsonl`, `docs/notas/**`
- depende de: P1-schema-normalize

## Tareas

- [x] SCHEMA 5->6 con el vocabulario de cost-report FIELDS + cost_micros + usage_status, columnas justo despues de project_key y en el orden de los ALTER (completed) · Siete columnas anulables declaradas inmediatamente despues de PROJECT_KEY_COLUMN; dispatches pasa de 32 a 39 columnas y la cola es exactamente usage_input..usage_status. Los CHECK >= 0 son detectores de corrupcion, no validacion de entrada: _usage_row bindea solo NULL o enteros validados, y un CHECK violado adentro de close_run haria ROLLBACK del cierre, que es justo lo que AC-11 prohibe., Una sola secuencia ordenada con dos consumidores (USAGE_COLUMNS): _create_schema la empalma y el paso 5->6 emite un ALTER por elemento. El acoplamiento de AC-09 pasa a ser aritmetica en vez de disciplina -- no hay segunda lista que mantener en sincronia., El INSERT posicional de _authorize_issued pasa a lista explicita de columnas (_AUTHORIZED_COLUMNS, 32 nombres derivados de _IDENTITY). Verificado que el orden coincide con la tabla. Ninguna columna futura vuelve a tocar esa linea., test_the_usage_columns_sit_exactly_where_alter_table_puts_them y test_the_usage_vocabulary_matches_cost_report mostrados rojos antes (AttributeError USAGE_COLUMNS_SQL), verdes despues. El primero incluye su complemento: las mismas columnas declaradas antes de state NO reproducen el texto migrado.
- [x] migration_required generico y CLI de migracion con from/to reales; encadenado 4->5->6 (completed) · migrate_from_v4 reemplazado por migrate(), cadena declarativa _MIGRATION_STEPS keyeada por la version de origen, toda adentro de un solo BEGIN EXCLUSIVE con una unica comparacion de DDL al final -- necesidad y no preferencia: la comparacion es contra el canonico ACTUAL, asi que un chequeo intermedio tras 4->5 compararia un schema-5 contra canonico-6 y fallaria siempre., _stored_schema_version es el unico lector de meta.schema_version: los tres literales '4' y el '5' de vuelta desaparecieron. migration_required usa < SCHEMA y nunca != SCHEMA, para que una base escrita por un arnes MAS nuevo conteste False en vez de ofrecer un downgrade., Banner con from/to observados. ADR-0008 D8 pineaba el literal; el formato no cambia, los valores pasan a ser observados, porque con schema 6 existiendo 'from=4 to=5' es una afirmacion falsa y no una eleccion de formato. El backup pasa a llamarse por la version de origen real., test_the_migration_banner_reports_the_versions_it_observed corre DOS esquemas de origen por el mismo camino y exige que los from= DIFIERAN: un regex sobre un solo fixture pasa igual contra un from=4 hardcodeado. Mutacion 'migration_required con != en vez de <' cazada; mutacion 'orden de ALTER invertido' cazada por el test de migracion (y NO por el de colocacion, dicho en su docstring)., python3 -m unittest tests.test_routing: Ran 89 tests, OK
- [x] close_run persiste usage; CLI rechaza duro un --usage invalido; close_run defensivo marca usage_status (completed) · parse_usage() en set_agents_app.py, idioma de cmd_route_decide: ValueError pelado, sin whitelist de claves. Rechaza solo lo no-JSON o JSON-no-objeto; acepta shapes no mapeadas (AC-12 lo exige). parse_float=decimal.Decimal preserva el texto exacto del proveedor., _cost_micros() en store.py: scaleb(6) + ROUND_HALF_UP sobre el Decimal parseado desde texto. Medido: 0.0000005 via parse_usage redondea a 1; el mismo valor via decimal.Decimal(float) redondea a 0 -- la justificacion completa de parse_float=Decimal, no solo citada., Limite [0,2^53) fijado sobre cost_micros, no sobre dolares: 9007199254.740991 dolares -> 2^53-1 micros (valido), 9007199254.740992 -> 2^53 micros (invalido). Leido al reves contradice AC-11, ya documentado en el spec (B-08)., _usage_row() en store.py: NULL nunca se coacciona a 0 (sample real de Pi sin cache/reasoning -> esas columnas NULL, status ok). {} y None son 'absent', nunca 'invalid'. Cualquier valor no confiable (tipo, negativo, mismatch de totalTokens contra la suma de los campos presentes) descarta TODA la fila -- usage_status='invalid' ES el registro del descarte, no una mezcla parcialmente confiada., 9 tests nuevos mostrados en rojo antes (AttributeError: parse_usage / _cost_micros / _usage_row no existian), verdes despues. python3 -m unittest tests.test_routing: Ran 100 tests, OK (base 91)., close_run() ahora recibe usage=None por default. Rama dispatched->terminal_<outcome>: el UPDATE incluye las siete columnas via _USAGE_SET_CLAUSE, bindeadas con _usage_row(usage). Rama authorized+failure->abandoned: fuerza _usage_row(None) SIN IMPORTAR lo que el llamador haya pasado -- un run que nunca se despacho no puede semanticamente tener consumido nada, y esa invariante vive aca, no en la disciplina del llamador., cmd_route_terminal() en set_agents_app.py gana usage_text=None. Si viene, se parsea con parse_usage(); ValueError -> ROUTING_INPUT_INVALID exit 2 ANTES de tocar el store (el run no esta cerrado todavia, no hay nada que proteger). Lo parseable pero no confiable NO se rechaza aca -- es el borde del store., --usage sumado a argparse, a _routing_args (para que no dispare other_mode) y a modifier_misuse (usage sin --route-terminal -> ROUTING_INPUT_INVALID), con un test cada uno: test_dispatch_cli_mode_and_modifier_exclusion cubre el segundo; test_route_terminal_usage_flows_from_the_cli_into_the_stored_row prueba que --usage + --route-terminal llega hasta la fila (si _routing_args no lo hubiera declarado, ese mismo test habria fallado por other_mode)., set_agents_spawn.py:371-374: --usage se agrega SOLO cuando outcome=='success' (detail.get('usage') del spawn exitoso). Los otros dos --route-terminal ... failure (:362 dispatch fallido antes de spawn, :382 excepcion) nunca lo llevan y jamas pasan por esta linea. model_mismatch/failure tampoco tienen clave 'usage' en su detail, asi que se omite naturalmente sin casos especiales., 8 tests nuevos mostrados en rojo antes (TypeError: close_run() unexpected keyword usage; argparse unrecognized --usage; conteo de lineas de stdout en 0 en vez de 1), verdes despues. python3 -m unittest discover -s tests: Ran 268 tests, OK (base 263).
- [x] --routing-report con tokens por ruta y el alcance declarado (completed) · report() en store.py gana la clave 'tokens', HERMANA de 'per_route' (nunca mezclada adentro): agrupa por COALESCE(actual_route_id,selected_route_id,'none') sobre dispatches, scopeado a self.project_key. SUM() sobre una columna totalmente NULL en el grupo da NULL, nunca 0 -- coercionarlo seria la misma fabricacion que AC-08 prohibe, generalizada al agregado., tokens['scope'] declara la diferencia de alcance EN EL JSON, no solo en prosa: percentiles/per_route salen de events (global de maquina, sin project_key); tokens sale de dispatches (por proyecto). cmd_routing_report no se toco -- ya pasa el dict entero como data, y ninguna clave nueva se llama reason_codes (el comando lee esa clave especifica del reporte para decidir ok/exit code)., 2 tests en test_routing.py: uno prueba SUM a traves de dos runs en la MISMA ruta (150+50 input, cache_read sigue NULL); otro prueba que un run cerrado SIN --latency-ms aporta tokens y ningun percentil -- la prueba de que los dos conjuntos de claves de ruta no son subconjunto uno del otro, no una afirmacion de prosa., cost-report.py gana collect_pi() + _pi_project_key(): lee routing.db via --home (nunca via routing_core -- store.py deriva home de pwd.getpwuid a proposito, ADR-0005), filtra usage_status='ok' (absent/invalid no tienen nada usable), atribuye por project_key SOLO con --project (recomputando la clave), si no reporta el carril sin atribuir. --help/epilog y TIPS-USO.md declaran el limite de cobertura (solo spawns despachados por el arnes)., 3 tests en test_harness.py: _pi_project_key coincide con set_agents_app.project_key_for en las dos ramas (hash-fallback y project.json persistido); atribucion solo con --project (otra proyecto nunca se filtra); filas absent/invalid no producen sesiones fantasma. Mutacion verificada: sacar la linea collect_pi(...) de main() tira rojo el test de atribucion, restaurada vuelve a verde., python3 -m unittest discover -s tests: Ran 273 tests, OK (base 268).
- [x] colector pi en cost-report.py con atribucion solo bajo --project (completed) · collect_pi() + _pi_project_key() en cost-report.py, leyendo routing.db via --home (nunca routing_core). Filtra usage_status='ok'. Atribuye solo con --project, recomputando la clave localmente; sin --project reporta el carril sin atribuir (project='?')., 3 tests en tests/test_harness.py: test_pi_collector_project_key_matches_project_key_for (la derivacion duplicada coincide con project_key_for en las dos ramas), test_cost_report_pi_collector_attributes_only_with_project (otro proyecto nunca se filtra sin --project, se excluye con --project), test_cost_report_pi_collector_ignores_rows_with_no_usable_usage (absent/invalid no generan sesiones fantasma). Mutacion verificada., Verificacion en vivo: cost-report.py --project . listo el carril pi real (gpt-5.6-luna, implementer, sessions=1, input=3.3k) al lado de claude-code y opencode, sobre el spawn real corrido para este paquete.
- [x] ADR-0010 (completed) · docs/adr/0010-spawn-accounting.md escrito con el esqueleto moderno (0008/0009/0011): metadata (Estado, numeracion, amends, citas verificadas), Contexto, D1-D6, Scale/Data/Security, Consecuencias. D4 documenta la generalizacion de la cadena de migracion, superseding en parte a ADR-0008 D8., ADR-0008 D8 enmendado inline (idioma R3/007-P1 ya establecido en el propio archivo): el banner from=4 to=5 pasa a ser historico del paso 4->5 especifico, apunta a ADR-0010 D4. Fila 0010 agregada a docs/adr/README.md, nota de reserva retirada., El guard del hueco reservado (tests/test_harness.py) era un no-op real: assertNotIn('0010', linked) comparaba membresia exacta contra una lista de NOMBRES DE ARCHIVO completos, nunca podia fallar. Generalizado a parsear la nota de reserva (' is reserved by feature') y verificar que ese numero no tenga TAMBIEN una fila entregada -- mutacion verificada: agregar una nota de reserva falsa sobre un ADR ya entregado (0009) tira el test., docs/architecture/overview.md (excepcion aprobada): --usage agregado a la fila de --route-terminal en la matriz de modificadores, y a la oracion del exit 2 ROUTING_INPUT_INVALID, citando ADR-0010 D3. Clausula agregada, matriz no reescrita., Verificacion en vivo (un spawn real por el carril Pi, role=implementer): encontro un bug real -- Pi manda cacheRead/cacheWrite en camelCase, no cache_read/cache_write, y _usage_row nunca los habria capturado. Corregido con _USAGE_FIELD_ALIASES en store.py, pinned por test_usage_row_accepts_pis_actual_camelcase_cache_keys sobre el payload real, mutacion verificada. Registrado con log-decision (pi-usage-cache-keys-are-camelcase-not-snake-case)., --routing-report --json tras el spawn real muestra tokens.input=3321, tokens.output=5, tokens.cost_micros=3351 en la ruta real. cost-report.py --project . lista el carril pi (gpt-5.6-luna, implementer, sessions=1, input 3.3k) al lado de claude-code/opencode., ./ai/scripts/verify.sh -> VERIFY_PASS (274 tests, base 259 al empezar P2 +15 del paquete). ./build.sh --check -> SELF_SCAFFOLD_SYNC_OK files=2. git diff --check limpio. check-owned-paths.py -> OWNERSHIP_PASS, out_of_scope=[], las dos excepciones aprobadas (ADR-0008, overview.md) resueltas correctamente., Correccion de shell del validation anterior (los backticks se interpretaron como command substitution): el guard generalizado parsea la nota de reserva con el patron backtick-NNNN-backtick seguido de "is reserved by feature" en docs/adr/README.md, extrae el numero de 4 digitos, y falla si ese numero tambien aparece como una fila linkeada y entregada.

## Hallazgos

- F-SEC-01 [critical] closed — 
- F-SEC-02 [high] closed — 
- F-SEC-03 [medium] closed — 
- F-SEC-04 [medium] closed — 
- F-PR-01 [high] closed — 
- F-PR-02 [high] closed — 
- F-PR-03 [medium] closed — 
- F-PR-04 [medium] closed — 
- F-PR-05 [low] closed — 
- F-PR-06 [low] refuted —  · refutado por finding-verifier: La afirmacion central ('full-table-scan', 'sin indice') es falsa, medida con EXPLAIN QUERY PLAN contra un store schema-… [EXPLAIN QUERY PLAN SELECT SUM(usage_input),... FROM dispatches WHERE project_ke…]
- N-01 [high] closed — 
- N-02 [low] closed — 
- N-03 [low] closed — 

## Recorrido

- review: repair_required (10 hallazgos)
- verificación: 1 refutados, 9 sostenidos
- verificación: 0 refutados, 1 sostenidos
- repair: F-SEC-01, F-PR-01, F-SEC-02, F-PR-02, F-SEC-03, F-SEC-04, F-PR-05, F-PR-03, F-PR-04 → 7 archivos
- repair: N-01, N-02, N-03 → 5 archivos
- delta review: repair_required
- delta review: pass
- testing: pass
- runtime QA: pass (waived)
- gate `package verify`: pass
- gate `self-scaffold-sync`: pass
- gate `whitespace`: pass
- gate `ownership`: pass
- gate `adversarial-proof`: pass
- gate `live-verification`: pass

context pack: `docs/specs/007-quota-visibility/context/P2-spawn-accounting.md`

↩ [[features/007-quota-visibility|007-quota-visibility]]
<!-- /notas:auto -->

## Notas propias

_Lo que escribas fuera del bloque auto se preserva en cada regeneración._
