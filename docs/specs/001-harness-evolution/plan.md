# Implementation plan

1. Replace profile manifests with one role roster and an active profile selector.
2. Add deterministic generation and validation for all harness formats.
3. Add read-only coordinator enforcement and lifecycle agent prompts.
4. Add staged installation, managed-file indexing, configuration overlays, backup, smoke checks, and rollback.
5. Add fixture-based policy, generation, bootstrap, release, and memory timeout tests.
6. Regenerate tracked outputs, run verification, and perform a separate read-only audit and judge.

