Reading additional input from stdin...
OpenAI Codex v0.147.0
--------
workdir: /home/federico/SET-AGENTES
model: gpt-5.6-terra
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 019ff8b4-bae5-7973-9122-c3b84d3efd3c
--------
user
Actua como package-reviewer independiente. HACE LA REVISION VOS MISMO: no delegues, no spawnees subagentes, no uses herramientas de coordinacion. Sos read-only.

Sos el package-reviewer independiente de 022 / P1-registro-de-proveedores en /home/federico/SET-AGENTES.
Sos READ-ONLY: no parchees nada, no edites archivos del repo. Tu salida son hallazgos.

El writer fue otro modelo, de otro proveedor. No le creas nada: re-ejecutá.

Leé primero:
- docs/specs/022-disponibilidad-real/context/P1-registro-de-proveedores.md (el context pack)
- docs/specs/022-disponibilidad-real/evidence/P1-implementer.md (lo que el writer afirma)
- El diff: `git diff` + `git status` (hay archivos nuevos sin trackear: ai/scripts/provider_registry.py, docs/adr/0042-*.md)

Revisá EXACTAMENTE estos cuatro puntos. Nada mas. No amplies el alcance.

PUNTO 1 - La derivacion es real y completa?
Los siete simbolos tienen que derivar de provider_registry.PROVIDERS, no ser copias paralelas.
En particular: `catalog.py:653` sigue teniendo la lista de proveedores HARDCODEADA
("openai-codex", "anthropic", "opencode-zen", "opencode-go") dentro de build_snapshot, y el
comentario de al lado se autodenomina "site 3 of 5". El orquestador lo encontro y NO lo dictamino.
Decidi vos: es un duplicado manual que AC-01 debia eliminar, o es alcance de P2? Argumenta con la
spec en la mano (AC-01 vs AC-04) y con lo que pasa si alguien suma un proveedor manana.

PUNTO 2 - El contrato de orden y tipo.
provider_registry declara que el orden de insercion del dict "is a real contract".
set_agents_app.py:106 hace tuple(provider_registry.PROVIDERS); models_config.py:55 hace
set(provider_registry.PROVIDERS). Verifica: hay algun consumidor que dependa del orden y NO este
cubierto por un test? Si el orden se rompiera, que test se pone rojo? Corre el test y mostralo.
Si la respuesta es "ninguno", eso es un hallazgo.

PUNTO 3 - El ADR es honesto?
docs/adr/0042-provider-registry-single-source.md tiene que (a) superseder EXPLICITAMENTE el no-goal
que estaba en set_agents_app.py:90-94 ("defined independently here, never importing"), y (b) corregir
la afirmacion de ADR-0034:124-126 con la medicion. Verifica ademas que no quedaron comentarios en el
codigo contradiciendo lo que el codigo ahora hace. Cita file:line de lo que encuentres.

PUNTO 4 - MORDIDA: la guarda nueva sirve?
tests/test_routing.py tiene test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source.
Rompe la fuente a proposito (copia el archivo con `cp` a /tmp primero, mutalo, corre el test dirigido,
restaura con `cp`). NO uses git checkout/restore/stash.
Tiene que ponerse ROJO. Pega la salida literal del rojo y del verde tras restaurar.
Si pasa igual con la fuente rota, es un hallazgo critical.

REGLAS:
- Cada bloque que pegues es literal, o marcado como recortado. Si no lo corriste, escribi "sin verificar".
- Comandos largos: corrélos como `ai/scripts/heartbeat-run.py --interval 20 -- <comando>`. NUNCA los pipees
  a `tail`: sin -f, tail no emite un byte hasta EOF y te colgas. Es doctrina del repo (ADR-0041).
- `pytest` NO esta instalado. Es `python3 -m unittest`.
- No corras la suite entera salvo que un hallazgo lo exija; para tests dirigidos usa
  `python3 -m unittest tests.test_routing.RoutingTests.<nombre> -v`.

FORMATO DE SALIDA (texto plano, al final de todo):
VEREDICTO: approved | revision_required
Y por cada hallazgo:
  ID: F1, F2, ...
  SEVERIDAD: critical | high | medium | low
  DONDE: file:line
  QUE: una frase concreta y accionable
  PRUEBA: el comando que corriste y su salida literal
Si no hay hallazgos, decilo explicitamente y mostra la evidencia de los cuatro puntos igual.
codex
Voy a hacer una revisión read-only y reproducible, limitada a los cuatro puntos indicados. Uso `request-triage` porque es el protocolo obligatorio de entrada para clasificar el trabajo antes de inspeccionar el repositorio.
exec
/bin/bash -lc "sed -n '1,240p' /home/federico/SET-AGENTES/.codex/skills/request-triage/SKILL.md" in /home/federico/SET-AGENTES
 exited 2 in 0ms:
sed: can't read /home/federico/SET-AGENTES/.codex/skills/request-triage/SKILL.md: No such file or directory

exec
/bin/bash -lc "sed -n '1,260p' /home/federico/.codex/skills/request-triage/SKILL.md" in /home/federico/SET-AGENTES
 succeeded in 0ms:
---
name: request-triage
description: Intake + mode selection for the orchestrator — classify an incoming request into an execution mode (consult/analysis, feature/SDD, scoped-feature, quick-fix, or incident/break-glass), ask scoping questions BEFORE starting, and know which normally-dormant agents (security-auditor, brainstormer, ux-ui-designer) to pull in and when. Load at the START of every user request, before delegating anything.
license: MIT
compatibility: opencode
metadata:
  enabled_for: orchestrator
---

# Request Triage

## When to use
At the very start of EVERY user request, before any delegation. Classify the request, decide the mode, ask
what you must, then run the matching flow. Do not dive into the SDD pipeline reflexively — that is exactly the
failure this skill exists to prevent (applying the heavy flow to a problem that needed a fast, clever fix).

## Step 0 — intake (always, before delegating)
1. Restate the request in one line and name the **mode** you are choosing + why.
2. **Resolvé antes de preguntar (ADR-0037)** applies here too, before any scoping question: check the
   original request, `docs/notas/`, `ai/state/decisions-log.jsonl`, and the approved spec/ADRs — a
   scoping doubt one of those already answers is resolved with `log-decision`, not asked.
3. If the request is still ambiguous on scope, risk, or intent after that check, ask **1–2 scoping
   questions** — do NOT start. Terse in execution, but interrogate up front. This is where you "stop the
   cart".
4. Only then delegate the first action of the chosen mode.

## The five modes

### 0. Consult / analysis — thinking together, no pipeline (check this FIRST)
Triggers: "qué opinás", "cómo encararías", "analizame esta idea", comparisons, design questions — any request
where the user wants analysis or a recommendation, not a code change. This is a first-class mode, not a
misclassified feature.
Flow: NO `init`, NO state file, NO pipeline. Delegate in parallel (these spawns are allowed and cheap):
- `brainstormer` — 3-6 genuinely different options with tradeoffs,
- `architect` — read-only pass over the three axes (relational vs non-relational vs vector store, API Gateway,
  deploy platform) plus design patterns / clean-architecture shape; NO ADR unless the user asks,
- `security-auditor` — design-level threat sketch, only when the idea touches auth/money/PII/external input.
Then SYNTHESIZE yourself into ONE multi-lens analysis: data model, architecture/patterns, security,
algorithms/complexity — ending with a recommendation plus runner-up. Close by asking: "¿Lo convierto en spec
(feature) o en scoped?". A consult NEVER starts the pipeline on its own; if durable learning surfaced,
delegate a `memory-scribe` note to the domain knowledge.

### 1. Feature / build — full SDD (opt-in; only when the work truly demands it)
Triggers — full SDD is chosen ONLY when at least one of these holds; otherwise drop to scoped-feature (if a
concrete risk signal is present) or quick-fix (the default lane):
- a net-new system or module (not an addition to established infrastructure),
- genuinely multi-package work (several coherent packages with dependencies),
- the request touches one of the three architecture axes (data store type, API Gateway, deploy platform)
  with no ADR covering it.
Before code, **run the scoping interrogation** (load `system-design-decisions`): what future/scale do you
expect? where centralized vs decentralized? what must be secure day one? and name these three axes
explicitly, not generically — **does persistence need vector/semantic search or does relational cover it?
does this need an API Gateway or is it a monolith with one client? where does it deploy and why (Vercel/PaaS
vs VPS/IaaS vs managed)?** Then the rigorous
**SDD → BDD → package workflow → regression tests** flow: spec → design+ADR (SDD) → acceptance
Given-When-Then (BDD) → spec challenge → user approval → package planning → package implementation with local
validations → package gates → one deep package review → consolidated repair → delta review → regression tests →
integration → judge → release → memory. Tests do NOT approve implementation; the independent package reviewer
does. This order is mandatory in feature mode; the three lanes below are the only
exceptions (scoped-feature / quick-fix / incident).

### 2. Scoped-feature — for bounded work that shows a concrete risk signal
Triggers: a **well-bounded** change on established infrastructure where at least ONE concrete risk signal is
present: money/billing, data migrations, auth/permissions/PII, a public contract or shared API, genuinely
multi-module work, or the user explicitly asking for the full treatment ("hacelo con el pipeline completo").
The canonical case: "a login view + password recovery on Supabase". Without one of these signals, a bounded
change is a quick-fix, not a scoped-feature — running spec + planner + panel + judge on an ordinary bugfix is
the same waste as running the full panel after every task (that is what turned a login into a 4-5h grind).
Flow: SDD-lite (spec + acceptance Given-When-Then; an ADR only if there is a genuinely new architectural
decision) → BDD connection point with the user → one or more coherent packages → package gates → **ONE
consolidated package review over the complete package diff** → `security-auditor` when the package
touches that surface → regression tests → `adversarial-judge` → release → memory.
What is cut: deep review does NOT run after each task or each trivial repair. Security guarantee is preserved by
reviewing the complete relevant package/diff once, then delta-reviewing repairs. Escalate to feature mode only
when one of feature mode's explicit triggers appears (net-new system, multi-package, uncovered architecture
axis) — "this feels big" is a reason to scope the package better, not to escalate.

### 3. Quick-fix — the DEFAULT for small and medium bounded work
Triggers: any small-or-medium, well-understood change with a clear blast radius — a bugfix, a bounded behavior
tweak, a change across 1-3 files, copy/config, one function or one component. This is the write-side half of
the same 1-3 number `orchestrator.md`'s "Direct-read vs. delegated-explore threshold" (ADR-0020) uses for the
read side — the two are kept as one cross-referenced constant, not two independent numbers. This is the default lane: most
day-to-day requests land here unless a concrete scoped/feature trigger is present. Flow: implement →
`gate-runner` verify → done. Skip spec/design/ADR and the full audit panel — UNLESS the diff itself turns out
to touch a concrete risk signal (auth/money/PII/migration/public contract), then **escalate to scoped-feature
or feature mode**, naming the signal. MANDATORY at close: record the minimal durable trace with
`python3 ai/scripts/feature-state.py log-quickfix --summary "<what/why>" --result done --file <path> --gate "<gate evidence>"`
— quick-fixes with no trace are how the development thread gets lost.

### 4. Incident / break-glass — production is broken NOW
Triggers: production down or a user blocked with no in-app path, and speed matters more than ceremony (e.g. a
prod user needs a password reset and there is no forgot-password flow → a one-shot `psql` script, done).
Flow: fastest correct + ingenious fix, minimal ceremony, delegate the actual change to `debugger`/`implementer`
(you still never implement yourself). MANDATORY afterward — never skip: (a) write down exactly what was done,
(b) open a follow-up task to do it properly, (c) delegate a memory note. Break-glass is a conscious, logged
exception to the rigor default — not a licence to abandon it.

Ambiguous which mode? Ask. Otherwise pick the LIGHTEST mode that covers the risk actually observed in the
request and the code — not the hypothetical risk. Escalate only when you find a concrete signal from the
lists above, and name that signal in the narration. Downgrading is equally legitimate: if mid-scoped the
planner or implementer establishes the change is smaller than assumed (no signal actually present), degrade
to quick-fix and record why with `log-decision` — escalation is not a one-way ratchet.

## Physical budgets per mode (enforced by the state CLI, not by prose)

The mode you choose is not just a flow — it sets hard budgets in the feature state. Pass it to
`feature-state.py init` via `--mode`:

| Mode | `--mode` | Spawns/package | Deep review cycles | Gate failures |
|---|---|---|---|---|
| Feature / SDD | `feature` (opt-in) | 12 | 2 | 3 |
| Scoped-feature | `scoped` (default) | 8 | 2 | 3 |
| Quick-fix | `quick-fix` | 4 | 1 | 2 |
| Incident | `incident` | 6 | 1 | 2 |

Two physical waivers keep light modes light without prose exceptions:
- `record-repair --skip-delta` skips the delta review ONLY when every repaired finding is ≤ medium severity
  and the repair touched ≤ 3 files (the CLI rejects it otherwise, and the waiver is recorded in the event).
- `create-package --runtime-surface false` (planner declaration) lets a package with no observable runtime
  surface become accept-ready after testing, without spawning `app-runner`/`runtime-verifier`. Default is
  `true` — fail-safe.

`record-spawn` blocks the feature when the spawn budget is exhausted. If a quick-fix genuinely needs more
than 4 spawns, that is the signal it was misclassified — re-triage to `scoped`/`feature` (a conscious,
logged decision), do not fight the budget.

## Architecture red-flags (transversal — check in EVERY mode, including quick-fix)
Before delegating in ANY mode, check the request against the three named axes above: **data store type
(including vector vs relational)**, **API Gateway**, and **deploy platform (Vercel/PaaS vs VPS/IaaS)**. The
check fires on EVIDENCE, not plausibility: the request or the files it names concretely require one of these
axes (a new kind of persistence, a new external entry point, a deploy change) AND no existing ADR covers it.
When it fires, do not implement directly — escalate to at least `scoped-feature` with an architecture
checkpoint (`architect` loads `system-design-decisions`, proposes options, and the orchestrator asks the user
per its Question policy) before any code is written. This applies even to a request that looks like a
quick-fix on its surface ("add semantic search to the docs page" is a one-line ask, but it is a data-store
decision). What it does NOT mean: "could hypothetically touch persistence someday" is not a red flag — an
ordinary bugfix inside existing tables/routes/deploy stays a quick-fix. A safe default is NOT an escape
hatch for these three axes specifically — see `orchestrator.md`'s Question policy.
**Named-platform carve-out (ADR-0025)**: when the request itself names the platform ("deploy this to
Vercel", "store it in Supabase"), that axis is DECIDED — record it with `log-decision` and proceed without
asking; `architect` writes the formal ADR afterwards. The red-flag question is only for an axis the user
left genuinely open.

## Waking the dormant agents (concrete triggers, not "by risk")
These agents are permitted but easy to forget — pull them in on these triggers. The trigger is evidence in
the plan or the diff (the concrete files/paths the package owns: auth/, payments/, migrations/, ui
components, .env-adjacent config), not the topic of the request in the abstract; `package-planner` justifies
each entry in `required_reviewers` with the paths that demand it:
- **auth / money / PII / any external input** → `security-auditor` is MANDATORY before the judge (its report
  covers the attack path AND the hardening/detection plan in one pass — no separate hand-off agent).
- **any user-facing surface / new UI** → `ux-ui-designer` (brand-grade, accessible; not generic defaults).
- **the approach is genuinely open / multiple viable designs** → `brainstormer` before committing.
- **queries / lists / transactions / migrations** → covered by `package-reviewer`'s own data-integrity/
  scalability checklist (already mandatory, no separate agent).

**Cadence by mode:** in package workflow, specialized reviewers run when the package touches their surface, or
when a repair changes that surface. They do not run after every ordinary task. A final evidence pass before
`adversarial-judge` is mandatory.

## Hard logic → hosted implementer (never the local leaf)
Concurrency, atomic transactions, money/financial rules, and security-critical logic must NOT be implemented by
the local leaf model (the cheap 8B) — a weak first draft on exactly this logic triggers more strong-auditor
rework than it saves. When a task touches these, pin implementation to a **hosted** model (e.g. `openai/gpt-5.4`)
rather than the local `implementer`. Route such implementation deliberately from the package plan. The
local leaf stays for boilerplate/CRUD/UI churn, which the audit panel then reviews.

## Output of triage
State: chosen mode + one-line why, the scoping questions asked (if any), and the first delegation. In
incident mode, also name the follow-up task you will open once the fire is out.

The first delegation is announced with the dual-register narration block from `orchestrator.md`
(`Cliente:` / `Ingeniería:`), and so is every delegation after it — in every mode. In consult mode the
parallel fan-out is narrated as ONE logical instance and persisted with `feature-state.py log-narrative`,
since a consult has no feature state file.

codex
Modo elegido: revisión/analysis read-only, porque pediste un dictamen independiente sobre un paquete ya implementado. No hace falta ninguna decisión de producto adicional; voy a re-ejecutar evidencia dirigida y no voy a modificar el repo.
exec
/bin/bash -lc "sed -n '1,260p' docs/specs/022-disponibilidad-real/context/P1-registro-de-proveedores.md && git status --short && git diff --no-ext-diff && sed -n '1,260p' docs/specs/022-disponibilidad-real/evidence/P1-implementer.md" in /home/federico/SET-AGENTES
 succeeded in 0ms:
# Context pack — P1-registro-de-proveedores (ADR-0042)

Spec: `docs/specs/022-disponibilidad-real/spec.md`, **AC-01, AC-02, AC-03**. Primer paquete de
022; los otros cuatro dependen de éste y **no** están en vuelo.

## Qué es este paquete, en una línea

Un único `PROVIDERS` del que se **derivan** las tablas de proveedores que hoy están en lockstep
manual. **No cambia comportamiento.** Si algo se comporta distinto después, es un defecto de
este paquete, no una mejora.

## Los siete símbolos, medidos hoy (no de memoria)

| Símbolo | Ubicación | Nota |
|---|---|---|
| `_OPENCODE_PROVIDER_KEYS` | `ai/scripts/routing_core/catalog.py:116-117` | display text de `opencode auth list` |
| `_OPENCODE_CLI_IDS` | `catalog.py:125-126` | argumento CLI de `opencode models <id>` |
| `_PAIR_COMMANDS` | `catalog.py:154-161` | **ya deriva** su mitad opencode de `_OPENCODE_CLI_IDS` |
| `PROVIDER_BILLING_KIND` | `catalog.py:172-173` | |
| key map de `_configured_models` | `catalog.py:216-217` | provider → clave de `[catalog]` |
| `DISCOVERABLE_PROVIDERS` | `ai/scripts/models_config.py:41` | es un `set` |
| `_MODEL_PREFERENCE_PROVIDERS` | `ai/scripts/set_agents_app.py:94` | es una `tuple` **ordenada** |

**La spec los llama "seis" y nombra siete.** No adivines cuál cuenta es la buena: derivá los
siete y **decí en la evidencia** cuál es el número correcto y por qué (probablemente seis son
los duplicados *manuales*, porque `_PAIR_COMMANDS` ya deriva la mitad opencode). Si corregís el
conteo, corregilo también en el objetivo del paquete vía la evidencia — no edites la spec.

## Tres trampas verificadas. Leelas antes de diseñar.

### 1. El "test que protege el sexto duplicado" no es lo que la spec sugiere — es peor y mejor a la vez

Medido con `grep -rn '_MODEL_PREFERENCE_PROVIDERS' tests/`:

- **SÍ existe un lockstep real** en `tests/test_routing.py:3191-3200`
  (`test_adr0034_ac10_discoverable_providers_lockstep_guard`): assertea
  `models_config.DISCOVERABLE_PROVIDERS == {provider for _, provider in _PAIR_COMMANDS}`.
  **Ese test tiene que seguir pasando sin tocarlo.** Es tu red.
- **`_MODEL_PREFERENCE_PROVIDERS` no tiene ninguno.** Su única aserción está en `:3965`,
  **adentro de un `with mock.patch(...)` de otro test** cuyo propósito es distinto, comparando
  contra un literal hardcodeado. Es incidental, no una guarda.

O sea: la guarda falta exactamente en el símbolo que vive en otro archivo. Escribí **ese** test.

### 2. Hay un no-goal explícito y previo que decís lo contrario de lo que vas a hacer

`set_agents_app.py:90-94`, literal:

```
# The closed, four-provider universe `_PAIR_COMMANDS` already probes
# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
```

Y `:113-114` afirma que es **"byte-identical to `models_config.DISCOVERABLE_PROVIDERS`"** —
lo cual **no es cierto en el sentido literal**: uno es `set`, el otro `tuple` ordenada.

Dos consecuencias obligatorias:

- **ADR-0042 tiene que superseder ese no-goal explícitamente**, no ignorarlo. Alguien lo decidió
  con una razón; la razón nombrada es la tabla de *billing-kind*, que es más angosta que "no
  derivar nada". Decilo así.
- **Los comentarios que quedan contradiciendo al código son un defecto entregable.** Si derivás,
  reescribí `:90-94` y `:113-114`. Un comentario que miente es lo que este paquete existe para
  eliminar.
- **Preservá el orden de la tupla.** `DISCOVERABLE_PROVIDERS` es un `set` sin orden; si derivás
  la tupla de un `set`, el orden queda no determinístico entre corridas. El orden de hoy es
  `("openai-codex", "anthropic", "opencode-zen", "opencode-go")` y llega a superficie de usuario.

### 3. Dónde vive `PROVIDERS` es la decisión de diseño del paquete, y hay una restricción dura

Medido:

- `catalog.py` **no importa** `models_config` en absoluto.
- `models_config.py` importa `routing_core.catalog` **sólo lazy, adentro de funciones**
  (`:257`, `:275`), y el docstring de `detect_subscriptions` (`:250-251`) dice por qué:
  *"Lazy import (no routing_core dependency at module load)"*.

Entonces: **si ponés `PROVIDERS` en `catalog.py`, hacer que `models_config.DISCOVERABLE_PROVIDERS`
lo derive a nivel de módulo rompe esa disciplina.** No la rompas por conveniencia. Un módulo
neutro sin dependencias que importen los tres consumidores es la salida obvia, pero la elección
es tuya y **va argumentada en ADR-0042 con esta medición**, no como preferencia estética.

## AC-02 — cómo se prueba que no cambiaste nada

Test de caracterización: cada tabla derivada **igual a los literales de hoy**. Los literales van
pegados en el test, no importados de la fuente nueva — un test que compara la derivación contra
sí misma es exactamente la guarda falsa de la trampa 1, ahora escrita por vos.

Ya existen tests que assertean estos valores contra literales (`:3131-3136` los dos mapas
opencode, `:3256-3266` `PROVIDER_BILLING_KIND` con `assertEqual` de dict completo). **No los
toques.** Que sigan verdes sin una sola edición es la mejor prueba de que el refactor preserva
comportamiento; editarlos destruye la prueba.

## AC-03 — qué corrige el ADR

`ADR-0034:124-126` afirma que el día que opencode exponga Copilot no haría falta tocar código.
**Es inexacto**: harían falta entradas en las tablas de arriba. ADR-0042 lo corrige **con la
medición** y explica que este paquete es lo que vuelve cierta esa afirmación. No edites ADR-0034
retroactivamente: `docs/adr/README.md` marca la relación (no-goal de la spec).

## Restricciones

- **ADR-0042 primero** — `ls docs/adr/` para confirmar que el número está libre; indexalo en
  `docs/adr/README.md`. `owned_paths` tiene el directorio `docs/adr`, no un nombre inventado.
- **Sin refactors oportunistas.** El pedido es derivar tablas. No reorganices `catalog.py`, no
  toques `_probe_pairs`, no toques el sort key. El techo `[catalog]` tri-estado es **P2**.
- **No uses `git checkout`, `git restore` ni `git stash`.** Para morder y restaurar: `cp` y `cp`.
- No relajes, saltees ni borres tests.
- `tests/test_harness.py` assertea frases doctrinales por grep: `grep -n` antes de mover texto.

## Validación

`python3 -m unittest discover -s tests` (**`pytest` NO está instalado**) · `./ai/scripts/verify.sh`
→ `VERIFY_PASS` · `./build.sh --check` → `GLOBAL_TREE_SYNC_OK` + `BUILD_CHECK_PASS` ·
`git diff --check` limpio.

**Corré los comandos largos así, exactamente:**

```
ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest discover -s tests
```

No lo pipees a `tail`: sin `-f`, `tail` no emite un byte hasta EOF, la suite tarda 7-10 minutos y
el watchdog del runtime te mata a los 600 s (ADR-0041). Murieron ocho instancias así en la sesión
anterior, **una de ellas con la herramienta ya nombrada en su prompt**. No es una opción entre
varias: es el comando.

## Evidencia

`docs/specs/022-disponibilidad-real/evidence/P1-implementer.md`: tabla AC → cambio
(`archivo:línea`) → prueba; los siete símbolos con su derivación; el conteo seis-vs-siete
resuelto; el argumento de dónde vive `PROVIDERS` con la medición de imports; la prueba de que
`:3191-3200`, `:3131-3136` y `:3256-3266` pasan **sin editarlos**; y los gates.

**Por cada test nuevo: neutralizá el cambio, confirmá el rojo, revertí, pegá la prueba.**
Aparecieron tests decorativos en tres de los cinco paquetes de 019 y en P1 de 020.

**Cada bloque que pegues es literal, o está marcado como recortado.** La sesión anterior acumuló
nueve afirmaciones de verificación que no resistieron la re-ejecución, tres del propio
orquestador. Si no lo corriste, escribí "sin verificar".

## Checkpoint

Escribí la evidencia en el **primer minuto** y guardá a disco a medida que avanzás. Si sentís que
te acercás al límite de ejecución, escribí progreso parcial + próximos pasos exactos antes de
parar.

## Fuera de alcance

El techo tri-estado (P2) · la firma de credencial y el de-auth (P3) · `providers.toml` y los
comandos `--provider-*` (P4) · altas/bajas automáticas (P5) · el sort key · agregar Copilot ·
features 023-025.
 M ai/scripts/models_config.py
 M ai/scripts/routing_core/catalog.py
 M ai/scripts/set_agents_app.py
 M ai/state/STATUS.md
 M ai/state/decisions-log.jsonl
 M docs/adr/README.md
 M "docs/notas/00 - Proyecto.md"
 M docs/specs/022-disponibilidad-real/spec.md
 M tests/test_routing.py
?? ai/scripts/provider_registry.py
?? ai/state/features/022-disponibilidad-real.json
?? docs/adr/0042-provider-registry-single-source.md
?? "docs/notas/decisiones/2026-08-13 el-probe-dice-vivo-y-la-inferencia-dice-token-vencido.md"
?? "docs/notas/decisiones/2026-08-13 ruteo-nombra-el-runtime-que-realmente-ejecuta.md"
?? docs/notas/features/022-disponibilidad-real.md
?? docs/notas/features/022-disponibilidad-real/
?? docs/specs/022-disponibilidad-real/bitacora.md
?? docs/specs/022-disponibilidad-real/context/
?? docs/specs/022-disponibilidad-real/evidence/
diff --git i/ai/scripts/models_config.py w/ai/scripts/models_config.py
index 4df6cd6..f758247 100644
--- i/ai/scripts/models_config.py
+++ w/ai/scripts/models_config.py
@@ -16,6 +16,15 @@ import os
 import tempfile
 from pathlib import Path
 
+# ADR-0042 (022 PKG-1): the single provider registry. Deliberately NOT a `routing_core`
+# import -- `provider_registry.py` has zero dependencies of its own (no `routing_core`, no
+# `models_config`), so importing it here at module scope costs nothing, unlike
+# `routing_core.catalog` (see `detect_subscriptions`/`auto_profile` below, both of which
+# still lazily import that ONE module for a real reason: any `routing_core` submodule
+# import runs `routing_core/__init__.py`, which loads `service.py`/`store.py` -- sqlite3,
+# subprocess -- at import time).
+import provider_registry
+
 ROOT = Path(__file__).resolve().parents[2]
 LANES = ("go-zen", "zen", "local")
 PERMISSION_PROFILES = ("guarded", "yolo")
@@ -38,7 +47,12 @@ ROUTING_PROVIDERS = {"openai-codex", "anthropic"}
 # tier/family inferred). Distinct from ROUTING_PROVIDERS on purpose — the curated
 # allowlist stays closed (immutable contract "probeable, never routable, by 012 alone");
 # this is the separate, opt-in surface that makes a discovered model routable.
-DISCOVERABLE_PROVIDERS = {"openai-codex", "anthropic", "opencode-zen", "opencode-go"}
+# ADR-0042 (022 PKG-1): derives from the single `provider_registry.PROVIDERS` registry now,
+# same four members as the pre-ADR-0042 literal. Order-insensitive on purpose (a `set`) --
+# `set_agents_app._MODEL_PREFERENCE_PROVIDERS` is the ORDERED sibling table, and it derives
+# from `provider_registry.PROVIDERS` directly rather than from this set, because a tuple
+# built from a set has no deterministic order across runs.
+DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)
 ROUTING_DEFAULTS = {
     "enabled_providers": ["openai-codex", "anthropic"], "xhigh_benchmarked": False,
     # ADR-0034 (019 PKG-1): "auto" is the new default -- discovered_providers derives
diff --git i/ai/scripts/routing_core/catalog.py w/ai/scripts/routing_core/catalog.py
index 2b784f1..16f990a 100644
--- i/ai/scripts/routing_core/catalog.py
+++ w/ai/scripts/routing_core/catalog.py
@@ -11,6 +11,13 @@ import time
 import tomllib
 from pathlib import Path
 from .domain import CatalogSnapshot, RoutingError, StaticRoute, _sorted_strings
+# ADR-0042 (022 PKG-1): the single provider registry every provider-keyed table in this file
+# derives from. Deliberately a plain top-level import, not a relative `.` one -- the module
+# lives OUTSIDE `routing_core` on purpose (see its own docstring and ADR-0042's "Dónde puede
+# vivir PROVIDERS" section): it has zero dependencies of its own, so importing it here costs
+# nothing this package's other two consumers (`models_config.py`, `set_agents_app.py`) don't
+# already pay to import it too.
+from provider_registry import PROVIDERS
 
 # ADR-0006 (AM-2): probe cache is a filtering-only optimization. TTL bounds staleness;
 # the invalidation key covers uid and the canonical catalog/routing config.
@@ -107,14 +114,19 @@ def canonical_model(provider: str, model: str) -> str:
     return CANONICAL_MODEL.get((provider, model), model)
 
 
+# ADR-0042 (022 PKG-1): both maps below DERIVE from the single `PROVIDERS` registry now
+# (`provider_registry.py`) -- before this ADR they were two independently hand-edited
+# literals, one of six provider-keyed tables that had to move in lockstep by hand across
+# this file, `models_config.py`, and `set_agents_app.py`. Values are unchanged from the
+# pre-ADR-0042 literals (PKG-1 AC-02 characterization test pins this).
+#
 # Catalog provider -> credential display text `_parse_opencode_auth` yields (map 1 of 2,
 # 012 AC-02). Used ONLY at the credential-set membership check. For the two pre-existing
 # pairs this string happens to coincide with the CLI argument below; for opencode-zen/
 # opencode-go it does not ("opencode zen"/"opencode go", two-token display text, vs the
 # single-token CLI ids in _OPENCODE_CLI_IDS) — re-measured live against `opencode auth
 # list --pure` (docs/specs/012-discovered-inventory/spec.md AC-02).
-_OPENCODE_PROVIDER_KEYS = {"openai-codex": "openai", "anthropic": "anthropic",
-                           "opencode-zen": "opencode zen", "opencode-go": "opencode go"}
+_OPENCODE_PROVIDER_KEYS = {provider: spec.opencode_auth_key for provider, spec in PROVIDERS.items()}
 # Catalog provider -> CLI argument for `opencode models <id>` AND the `<id>/<model>` line
 # prefix `_parse_opencode_models` strips (map 2 of 2, 012 AC-02). Independently
 # addressable from _OPENCODE_PROVIDER_KEYS above: using the CLI-id value at the
@@ -122,8 +134,7 @@ _OPENCODE_PROVIDER_KEYS = {"openai-codex": "openai", "anthropic": "anthropic",
 # single-token string and report the pair absent on every machine (the defect this
 # two-map split fixes; PI_MODEL_MAP is explicitly the wrong precedent here — it
 # translates a model name within one already-matched provider, a different axis).
-_OPENCODE_CLI_IDS = {"openai-codex": "openai", "anthropic": "anthropic",
-                     "opencode-zen": "opencode", "opencode-go": "opencode-go"}
+_OPENCODE_CLI_IDS = {provider: spec.opencode_cli_id for provider, spec in PROVIDERS.items()}
 # ADR-0034 (019 PKG-1) re-measurement, live, 2026-08-10 (opencode 1.18.14): `opencode
 # auth list --pure` shows 4 real credentials -- `opencode-go`, `openai`, `github-copilot`,
 # `opencode` (auth.json keys). `github-copilot` is authenticated but `opencode models
@@ -169,8 +180,8 @@ _PAIR_COMMANDS = {
 # Code OAuth), `opencode-go` stays subscription, `opencode-zen` stays the sole metered
 # entry. This is the day `billing_rank` (below) starts reading it -- the previous "no
 # weighting/selection logic reads this map yet" note is retired.
-PROVIDER_BILLING_KIND = {"openai-codex": "subscription", "anthropic": "subscription",
-                         "opencode-go": "subscription", "opencode-zen": "metered"}
+# ADR-0042 (022 PKG-1): derives from `PROVIDERS` now, same values as the pre-ADR-0042 literal.
+PROVIDER_BILLING_KIND = {provider: spec.billing_kind for provider, spec in PROVIDERS.items()}
 # ADR-0035 AC-12: the FREE-model convention is the `-free` suffix -- the exact same
 # pattern `inference._FAST_HINTS` already treats as a signal, never a second, competing
 # definition of "free" invented here.
@@ -197,23 +208,32 @@ def allowed_probe(runtime: str, provider: str) -> tuple[tuple[str, ...], ...] |
     return _PAIR_COMMANDS.get((runtime, provider))
 
 
+# ADR-0042 (022 PKG-1): provider -> `[catalog]` TOML key, derived from `PROVIDERS` (same
+# values as the pre-ADR-0042 inline literal `_configured_models` used to carry). Site (2) of
+# the five-site lockstep `_configured_models` documents below -- the other four sites (the
+# `[catalog]` table itself, `build_snapshot`'s own comprehension, and the two
+# `models_config.py` sites) are a DIFFERENT, wider concern (012 AC-04: whether a TOML key
+# exists and round-trips through `load_config`/`emit` at all) and are out of this package's
+# scope -- only this key map derives from the single provider registry.
+_CATALOG_KEYS = {provider: spec.catalog_key for provider, spec in PROVIDERS.items()}
+
+
 def _configured_models(config: dict, provider: str) -> set[str]:
     # 012 AC-04: same five-site lockstep as the pre-existing two providers (corrected
     # 012 repair F-04: the original comment undercounted "three sites" — this package
     # added two more in models_config.py) — this key map is site (2) of 5 that must move
-    # together with: (1) models.toml's [catalog] table itself; (2) this key map; (3)
-    # build_snapshot's own `configured_models` comprehension (catalog.py, below); (4)
-    # models_config.load_config's optional-key validation loop for the same two TOML
-    # keys; (5) models_config.emit's preservation loop for the same two keys (without
-    # which a future `./setup-models.sh` write would silently drop the allowlist). Sites
-    # (4)/(5) exist because [catalog] has no closed-schema check the way [routing]/
-    # [permissions] do — an allowlist key present in the TOML but absent from both
-    # models_config.py sites is read here (fine) but never survives a re-emit (data
-    # loss). Extending only site (1) leaves the other providers' allowlists unreachable
-    # from code; extending only (1)-(3) without (4)/(5) leaves them reachable but
-    # silently unpersisted on the next wizard run.
-    key = {"openai-codex": "codex", "anthropic": "claude",
-           "opencode-zen": "opencode_zen", "opencode-go": "opencode_go"}.get(provider)
+    # together with: (1) models.toml's [catalog] table itself; (2) this key map (now
+    # `_CATALOG_KEYS` above, ADR-0042); (3) build_snapshot's own `configured_models`
+    # comprehension (catalog.py, below); (4) models_config.load_config's optional-key
+    # validation loop for the same two TOML keys; (5) models_config.emit's preservation
+    # loop for the same two keys (without which a future `./setup-models.sh` write would
+    # silently drop the allowlist). Sites (4)/(5) exist because [catalog] has no
+    # closed-schema check the way [routing]/[permissions] do — an allowlist key present in
+    # the TOML but absent from both models_config.py sites is read here (fine) but never
+    # survives a re-emit (data loss). Extending only site (1) leaves the other providers'
+    # allowlists unreachable from code; extending only (1)-(3) without (4)/(5) leaves them
+    # reachable but silently unpersisted on the next wizard run.
+    key = _CATALOG_KEYS.get(provider)
     models = config.get("catalog", {}).get(key, ()) if key else ()
     if not isinstance(models, (list, tuple)):
         return set()
diff --git i/ai/scripts/set_agents_app.py w/ai/scripts/set_agents_app.py
index 33cac42..8714593 100644
--- i/ai/scripts/set_agents_app.py
+++ w/ai/scripts/set_agents_app.py
@@ -30,6 +30,7 @@ sys.path.insert(0, str(Path(__file__).resolve().parent))
 sys.modules.setdefault("set_agents_app", sys.modules[__name__])
 import coord_policy
 import models_config
+import provider_registry
 import routing
 import set_agents_spawn
 import tui
@@ -89,9 +90,20 @@ def _project_root_or_harness() -> Path:
 MODEL_PREFERENCE_PATH = STATE_DIR / "model-preference.toml"
 
 # The closed, four-provider universe `_PAIR_COMMANDS` already probes
-# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
-# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
-_MODEL_PREFERENCE_PROVIDERS = ("openai-codex", "anthropic", "opencode-zen", "opencode-go")
+# (`routing_core/catalog.py:165-172`). ADR-0042 (022 PKG-1) supersedes this table's own
+# ORIGINAL "defined independently here, never importing" framing -- that framing was wider
+# than the reason it named: the AC-06 non-goal it cites (014-model-preference-policy,
+# test_ac06_no_provider_billing_kind_reference_in_new_code) is specifically about never
+# letting the catalog module's own billing-kind classification table decide provider
+# membership here, and that narrower non-goal still holds -- this tuple's derivation below
+# never reads that table (nor even names it -- see the test above, unchanged by this
+# package). What changed is provider IDENTITY: this tuple now derives from the single
+# `provider_registry.PROVIDERS` registry every provider-keyed table in the harness shares,
+# instead of being a second hand-maintained copy of the same four provider ids. Order is a
+# real contract (surfaces in pin/preference validation errors) --
+# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
+# because that sibling table is an unordered `set`.
+_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
 _MODEL_PREFERENCE_CLASSES = ("decision", "grunt", "build")
 # AC-02's inertness note: the six roles with real, live effect today (`### Honest scope`,
 # spec) -- every other in-scope role, and the whole `decision` class, is genuinely inert.
@@ -112,7 +124,9 @@ def _model_preference_die(message):
 
 def _effective_preference_providers():
     """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
-    -- byte-identical to `models_config.DISCOVERABLE_PROVIDERS`) UNION whatever the
+    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
+    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
+    purpose, ordered tuple vs. unordered set, never in membership) UNION whatever the
     CURRENT effective snapshot reports as routable, when that snapshot is cheaply
     resolvable -- a live probe, never network/credential material. Deliberately never
     called from `load_model_preference`/`load_model_pin` (the boot path, `_read_model_
@@ -129,7 +143,10 @@ def _effective_preference_providers():
     which today it structurally cannot: every provider it can produce comes from
     `routing_core.catalog._PAIR_COMMANDS`'s own audited provider set, and that set is
     pinned equal to `models_config.DISCOVERABLE_PROVIDERS` (== `_MODEL_PREFERENCE_
-    PROVIDERS`, AC-10's lockstep test). So the probe below is skipped whenever the
+    PROVIDERS`, guarded for real since ADR-0042 -- both are derived from
+    `provider_registry.PROVIDERS`, and `test_adr0042_ac01b_...` in tests/test_routing.py
+    compares this tuple against `_PAIR_COMMANDS`'s own provider set directly, never a
+    second hardcoded literal). So the probe below is skipped whenever the
     base set already covers `DISCOVERABLE_PROVIDERS` -- a cheap, exact short-circuit
     that avoids paying a subprocess probe (each `_PAIR_COMMANDS` entry can block up to
     20s) on every `--model-pin-set`/`--model-preference-set` call for a union that
diff --git i/ai/state/STATUS.md w/ai/state/STATUS.md
index ecc12f1..5a80021 100644
--- i/ai/state/STATUS.md
+++ w/ai/state/STATUS.md
@@ -2,7 +2,7 @@
 
 _Generado por `feature-state.py` en cada mutación de estado. No editar a mano._
 
-Actualizado: 2026-08-12T21:09:06+00:00
+Actualizado: 2026-08-13T01:03:16+00:00
 
 ## Features
 
@@ -26,6 +26,7 @@ Actualizado: 2026-08-12T21:09:06+00:00
 | 019-harness-evolution | feature | DONE | P5-tools-discovery (accepted) | 5/5 | 14/12 | 1/2 | 0 | - | - | 2026-08-12T02:43:19+00:00 transition |
 | 020-honest-dashboard | feature | DONE | P2-anclas-verificables (accepted) | 2/2 | 4/12 | 1/2 | 1 | - | - | 2026-08-12T11:19:21+00:00 transition |
 | 021-gates-que-no-mienten-ni-callan | feature | DONE | P2-gates-que-no-callan (accepted) | 2/2 | 6/12 | 2/2 | 0 | - | - | 2026-08-12T21:09:05+00:00 transition |
+| 022-disponibilidad-real | feature | PACKAGE_REVIEW | P1-registro-de-proveedores (package_review) | 0/5 | 2/12 | 0/2 | 0 | - | - | 2026-08-13T01:03:16+00:00 record-spawn |
 
 ## Quick-fixes recientes
 
@@ -37,6 +38,14 @@ Actualizado: 2026-08-12T21:09:06+00:00
 
 ## Bitácora (últimos 15)
 
+[2026-08-13T01:03:16+00:00] P1-registro-de-proveedores · package-reviewer · started · modelo openai-codex/gpt-5.6-terra · effort high
+Cliente: Una segunda opinion, hecha por otro proveedor de IA distinto del que escribio el codigo, que revisa que el cambio no haya roto nada ni prometido de mas.
+Ingeniería: Writer fue claude-code/anthropic/opus (run1_370bfc8a). Independencia por PROVEEDOR distinto (service.py:353 la exige dura): reviewer en opencode/openai-codex/gpt-5.6-terra, decision dec1_4ac1490e, independence_verified=true. Asignacion acotada a 3 puntos + 1 mordida por la leccion de los ocho stalls.
+
+[2026-08-13T00:13:18+00:00] P1-registro-de-proveedores · implementer · started · modelo anthropic/opus · effort medium
+Cliente: Unificar la lista de proveedores de IA que hoy esta repetida en siete lugares del codigo, para que sumar uno nuevo sea una linea y no siete.
+Ingeniería: P1 de 022 (AC-01..03). Deriva _OPENCODE_PROVIDER_KEYS, _OPENCODE_CLI_IDS, _PAIR_COMMANDS, PROVIDER_BILLING_KIND, el key map de _configured_models, DISCOVERABLE_PROVIDERS y _MODEL_PREFERENCE_PROVIDERS de un PROVIDERS unico. Refactor puro: AC-02 exige caracterizacion byte-identica. Trampas medidas: el lockstep real existe en test_routing.py:3191-3200 y hay que preservarlo; _MODEL_PREFERENCE_PROVIDE…
+
 [2026-08-12T21:09:05+00:00] done
 Cliente: La feature 021 quedo cerrada. El control que decia verificar que los archivos generados estaban al dia ahora los verifica de verdad, y cuando el harness trabaja varios minutos se nota en vez de parecer colgado.
 Ingeniería: 021 DONE. 2 paquetes, ADR-0041. build.sh --check pasa de no comparar nada a comparar los 4 arboles con perfil go-zen fijo; heartbeat-run.py mas la doctrina imperativa en spawn-prompt y punteros en los skills del ejecutor. Suite 970 -> 979. Los gates finales se corrieron CON heartbeat-run.py y emitio 4 latidos en 489s.
@@ -89,11 +98,3 @@ Ingeniería: P1 de 020 (AC-01..05, AC-12): un predicado compartido de feature vi
 Cliente: La feature 019 quedo cerrada: los cinco paquetes aceptados, integrados y con los gates globales en verde. El harness ahora adopta solo los proveedores que configuras, prefiere lo que ya pagas, te explica que cambio en tu forma de pensar el sistema, resuelve antes de preguntarte, y te pide permiso para sumar herramientas en vez de frenarse.
 Ingeniería: 019 DONE. 5/5 paquetes accepted, 6 module_impacts, ADRs 0034-0038 mas 0039 (arreglo del motor de estado autorizado aparte). Suite 815 -> 917 (+102), VERIFY_PASS, CHECK_PASS + SELF_SCAFFOLD_SYNC_OK files=2, git diff --check limpio. Deuda explicita registrada: las anclas file:line sembradas en docs/modules/ derivaron dentro de la misma feature (set_agents_app.py:2510 corrida +742 lineas) -- la desv…
 
-[2026-08-12T02:06:32+00:00] P5-tools-discovery · integrator · started · modelo openai-codex/gpt-5.6-sol · effort balanced
-Cliente: Ultimo paso: comprobar que las cinco partes funcionan juntas y no solo por separado.
-Ingeniería: integrator sobre 019: los 5 paquetes accepted, los 5 con module_impacts registrados (el gate de INTEGRATION que construyo P3 ya paso). Verifica los criterios de cierre (a)-(f) de la seccion 3 de la spec, corre los gates globales y consolida la evidencia de entrega.
-
-[2026-08-12T01:32:09+00:00] P5-tools-discovery · delta-reviewer · started · modelo anthropic/opus · effort medium
-Cliente: Verificacion final independiente del ultimo paquete.
-Ingeniería: delta-reviewer ronda 4 sobre P5. Alcance: NEW-03 (forma nativa completa del spec mcp) y NEW-04 (transcripcion corregida). El orquestador ya re-verifico las 8 variantes en vivo. Contramedidas vigentes por decisions-log slug cuarta-verificacion-fabricada-y-patron-del-hermano: auditoria al azar en TODAS las rondas, y atacar la clase, no el ejemplo.
-
diff --git i/ai/state/decisions-log.jsonl w/ai/state/decisions-log.jsonl
index 53b07f1..ab50a58 100644
--- i/ai/state/decisions-log.jsonl
+++ w/ai/state/decisions-log.jsonl
@@ -104,3 +104,5 @@
 {"actor": "orchestrator", "at": "2026-08-12T18:07:10+00:00", "consequences": "Cuarto desliz de bookkeeping del orquestador en la sesion, sumado a los dos de owned_paths y al presupuesto de verificacion contado por llamadas. Todos del mismo tipo: estado que el orquestador debe mantener a mano y ninguna herramienta le recuerda. Candidato claro a mejora del harness -- no esta en el plan aprobado y queda propuesto, no hecho.", "context": "Al relanzar el implementer de P2 acotado a AC-07 corri --route-decide y record-spawn pero NO --route-dispatched. El run quedo en authorized, el --route-terminal success posterior no lo registro como writer, y el --route-decide del reviewer devolvio REVIEW_IDENTITY_INVALID. Lo mismo habia pasado con el writer de 019/P4, que tambien habia quedado sin cerrar.", "decision": "Corregido dispatchando y cerrando el run en orden. LECCION: la secuencia route-decide -> record-spawn -> route-dispatched es una unidad; omitir el tercer paso rompe la verificacion de independencia del reviewer siguiente, y el sintoma (REVIEW_IDENTITY_INVALID) no dice cual fue el paso que falto. Vale para todo relanzamiento, que es justo donde se saltea porque uno cree estar repitiendo algo ya hecho.", "feature_id": "021-gates-que-no-mienten-ni-callan", "package_id": "", "slug": "olvide-route-dispatched-en-el-relanzamiento", "title": "Cuarto desliz de bookkeeping del orquestador: omiti --route-dispatched en un relanzamiento"}
 {"actor": "orchestrator", "at": "2026-08-12T19:57:10+00:00", "consequences": "Evidencia a favor del paquete: el problema es real y recurrente, y ahora esta medido en un rol distinto. Evidencia en contra de darlo por resuelto: AC-07 pone la doctrina en spawn-prompt/SKILL.md, que la carga el orquestador al REDACTAR; no hay nada que se la imponga al agente que la recibe. Queda como limitacion conocida a nombrar en el ADR, no como defecto de P2 -- imponerselo al receptor seria otra feature.", "context": "El package-reviewer de 021/P2 murio con 'no progress for 600s' revisando justamente el paquete anti-stall, y esperando la suite -- la condicion exacta que heartbeat-run.py existe para evitar. Sus ultimas palabras fueron 'sigo esperando... chequeo el progreso de la suite sin abusar del polling'. El encargo YA le decia que usara heartbeat-run.py o escribiera a archivo.", "decision": "DOS CORRECCIONES a caracterizaciones previas del orquestador. (1) El patron NO es 'agentes mutadores de corrida larga': este era read-only. Lo comun a los cinco stalls es ESPERAR UN COMANDO LARGO SIN EMITIR, sea el agente mutador o no. La caracterizacion anterior, registrada en el slug cuarto-stall-de-la-sesion-patron-de-infraestructura, era incorrecta. (2) Nombrar la herramienta en el encargo NO alcanza: el agente la tenia nombrada y aun asi hizo polling. La instruccion tiene que ser imperativa y concreta -- 'corre la suite ASI', con el comando exacto pegado -- no una opcion entre varias.", "feature_id": "021-gates-que-no-mienten-ni-callan", "package_id": "", "slug": "quinto-stall-corrige-el-patron-y-la-mitigacion", "title": "Quinto stall: el patron no era 'agentes mutadores' y nombrar la herramienta no alcanza"}
 {"actor": "orchestrator", "at": "2026-08-12T20:13:57+00:00", "consequences": "P2 queda implementado con sus cuatro AC hechos y los gates en verde (977 OK / 2 skips, VERIFY_PASS, GLOBAL_TREE_SYNC_OK + BUILD_CHECK_PASS, git diff --check limpio), pero SIN review independiente. 021 no puede pasar a DONE hasta resolverlo. El arbol quedo sano tras las dos muertes, verificado.", "context": "El package-reviewer de P2 murio dos veces con 'no progress for 600s'. La segunda murio TEMPRANO -- sus ultimas palabras fueron 'ahora leo la evidencia del implementer y el ADR' -- o sea NO esperando la suite, que era la hipotesis del orquestador. Eso descarta la explicacion anterior. Seis stalls en una sesion, en roles mutadores y read-only, uno de ellos leyendo archivos: es el entorno de ejecucion, no la redaccion de los encargos ni el patron del pipe. La doctrina propia del orquestador dice: un relanzamiento por asignacion, y una segunda muerte de la misma asignacion es un blocker real que se reporta.", "decision": "NO se intenta un tercer relanzamiento identico. Se reporta como blocker y se le ofrecen a Federico tres caminos: (a) partir el review en dos encargos mas chicos, cada uno con menos ejes y sin correr la suite completa; (b) que el orquestador haga una verificacion reducida y la etiquete explicitamente como NO-review-independiente, dejando el paquete aceptado con esa salvedad registrada; (c) esperar y reintentar mas tarde, si el entorno mejora. El orquestador recomienda (a): conserva la independencia, que es lo que el paquete necesita, y ataca la unica variable que controlamos, que es el tamano del encargo.", "feature_id": "021-gates-que-no-mienten-ni-callan", "package_id": "P2-gates-que-no-callan", "slug": "sexto-stall-segunda-muerte-del-mismo-encargo", "title": "HUMAN_DECISION_REQUIRED: sexto stall de la sesion y segunda muerte del review de P2"}
+{"actor": "orchestrator", "at": "2026-08-13T00:13:40+00:00", "consequences": "Un route_id que nombra un runtime que no ejecuto es evidencia falsa del mismo tipo que ADR-0026 prohibe. Tambien fija el criterio de independencia del reviewer de este paquete: el writer es opus, asi que el reviewer necesita otro modelo con contexto limpio.", "context": "La primera decision de P1 salio con el default selected_runtime=opencode y eligio openai-codex/gpt-5.6-terra (run1_881c88). Pero el unico mecanismo de delegacion del orquestador en esta sesion son subagentes de Claude Code, que corren modelos Claude. Registrar ese route_id habria dejado en el paquete la evidencia de que el implementer corrio en opencode.", "decision": "El descriptor lleva selected_runtime=claude-code cuando la delegacion es por la herramienta Agent. La corrida opencode se abandono (--route-terminal). La decision efectiva es run1_370bfc8a, claude-code/anthropic/opus, effort medium.", "feature_id": "022-disponibilidad-real", "package_id": "P1-registro-de-proveedores", "slug": "ruteo-nombra-el-runtime-que-realmente-ejecuta", "title": "El descriptor de ruteo nombra el runtime que realmente ejecuta"}
+{"actor": "orchestrator", "at": "2026-08-13T01:06:46+00:00", "consequences": "Es evidencia empirica para 022: listable != usable. AC-16 y P3 asumen que un probe de listado prueba liveness; esta medicion muestra que no. Deberia entrar como insumo de P3 (liveness-real) y de AC-19 (separar listed_by_provider de usable). Tambien queda medido que en opencode TODOS los roles del harness son subagent y solo 'orchestrator' es primary, asi que 'opencode run --agent <rol>' no puede despachar un rol: cae al agente por defecto con un warning.", "context": "El router eligio opencode/openai-codex/gpt-5.6-terra para el reviewer de P1 (dec1_4ac1490e, independence_verified=true). La ejecucion real fallo: 'Error: Provided authentication token is expired.' Medido en la misma sesion: 'opencode auth list --pure' muestra la credencial OpenAI (oauth) presente, y 'opencode models openai --pure' lista 13 modelos sin error. O sea el probe -que es una llamada de LISTADO- dice que el par esta vivo, y la INFERENCIA no funciona.", "decision": "El reviewer se ejecuta en opencode/gpt-5.6-terra servido por opencode-zen (credencial API key, verificada con un smoke que devolvio PONG). Mismo modelo que eligio el router, proveedor distinto al del writer (anthropic), independencia preservada. Se registra el desvio en la evidencia del review.", "feature_id": "022-disponibilidad-real", "package_id": "P1-registro-de-proveedores", "slug": "el-probe-dice-vivo-y-la-inferencia-dice-token-vencido", "title": "Medicion en vivo: listar modelos funciona y la inferencia falla con token vencido"}
diff --git i/docs/adr/README.md w/docs/adr/README.md
index 0ab282c..9b060fa 100644
--- i/docs/adr/README.md
+++ w/docs/adr/README.md
@@ -46,3 +46,4 @@
 | [0039](0039-reopen-directed-counter-reset.md) | `reopen` resetea exactamente el contador que lo bloqueó, estructurado en el blocker, nunca inferido del texto | Accepted | 2026-08-11 | — | — |
 | [0040](0040-honest-digest-shared-liveness-predicate.md) | Tablero honesto: `feature_is_live` un solo predicado en `model.py` (`final_state == "DONE"` exacto), umbral de estancamiento como constante única, sección `## Necesita tu decisión` con tope de dos menciones por bloqueada | Accepted | 2026-08-12 | — | — |
 | [0041](0041-build-check-verifies-global.md) | `build.sh --check` compara `Global/` de verdad contra una generación forzada a `--profile go-zen`, distingue self-scaffold de árbol generado en su salida, y fija por escrito que `--check` corre siempre antes que la suite cuando ambos son evidencia de gate | Accepted | 2026-08-12 | — | — |
+| [0042](0042-provider-registry-single-source.md) | Un único `PROVIDERS: dict[str, ProviderSpec]` (`ai/scripts/provider_registry.py`) del que se derivan las siete tablas de proveedores hoy en lockstep manual; corrige la afirmación de ADR-0034 "Rejected alternatives" sobre Copilot con la medición | Accepted | 2026-08-12 | Corrige en parte 0034 (afirmación de Rejected alternatives sobre Copilot, no-goal de la spec 022 — 0034 no se edita) | — |
diff --git i/docs/notas/00 - Proyecto.md w/docs/notas/00 - Proyecto.md
index f1900a3..0959aef 100644
--- i/docs/notas/00 - Proyecto.md	
+++ w/docs/notas/00 - Proyecto.md	
@@ -21,6 +21,7 @@
 - [[features/019-harness-evolution|019-harness-evolution]] — fase `DONE` · paquetes 5/5 · **DONE**
 - [[features/020-honest-dashboard|020-honest-dashboard]] — fase `DONE` · paquetes 2/2 · **DONE**
 - [[features/021-gates-que-no-mienten-ni-callan|021-gates-que-no-mienten-ni-callan]] — fase `DONE` · paquetes 2/2 · **DONE**
+- [[features/022-disponibilidad-real|022-disponibilidad-real]] — fase `PACKAGE_REVIEW` · paquetes 0/5
 
 ## Qué falta
 
@@ -41,21 +42,21 @@
 
 ## Decisiones
 
+- [[decisiones/2026-08-13 el-probe-dice-vivo-y-la-inferencia-dice-token-vencido|Medicion en vivo: listar modelos funciona y la inferencia falla con token vencido]]
+- [[decisiones/2026-08-13 ruteo-nombra-el-runtime-que-realmente-ejecuta|El descriptor de ruteo nombra el runtime que realmente ejecuta]]
 - [[decisiones/2026-08-12 sexto-stall-segunda-muerte-del-mismo-encargo|HUMAN_DECISION_REQUIRED: sexto stall de la sesion y segunda muerte del review de P2]]
 - [[decisiones/2026-08-12 quinto-stall-corrige-el-patron-y-la-mitigacion|Quinto stall: el patron no era 'agentes mutadores' y nombrar la herramienta no alcanza]]
 - [[decisiones/2026-08-12 olvide-route-dispatched-en-el-relanzamiento|Cuarto desliz de bookkeeping del orquestador: omiti --route-dispatched en un relanzamiento]]
 - [[decisiones/2026-08-12 p2-murio-por-limite-de-sesion-con-trabajo-parcial|El implementer de P2 murio por limite de sesion con AC-06, 08 y 09 hechos y AC-07 pendiente]]
 - [[decisiones/2026-08-12 segunda-vez-owned-paths-con-adr-adivinado|Segunda vez que escribo owned_paths con un nombre de ADR que todavia no existe]]
 - [[decisiones/2026-08-12 commitear-un-paquete-en-review-es-guardar-no-aceptar|Commitear un paquete que esta en PACKAGE_REVIEW: guardar no es aceptar]]
-- [[decisiones/2026-08-12 correccion-setup-models-si-habia-que-tocarlo|CORRECCION: la nota que decia que setup_models.py seguia funcionando sin tocarlo era falsa]]
-- [[decisiones/2026-08-12 la-evidencia-de-build-check-de-019-y-020-no-probaba-drift|Los gates de 019 y 020 que citaban 'build.sh --check -> CHECK_PASS' como prueba de sin-drift no probaban eso]]
 
 ## Referencias
 
 - `ai/state/STATUS.md` — dashboard técnico
 - `docs/adr/` — decisiones formales de arquitectura
 
-_Actualizado: 2026-08-12T21:09:05+00:00_
+_Actualizado: 2026-08-13T01:03:16+00:00_
 <!-- /notas:auto -->
 
 ## Notas propias
diff --git i/docs/specs/022-disponibilidad-real/spec.md w/docs/specs/022-disponibilidad-real/spec.md
index 031b759..bc8d18b 100644
--- i/docs/specs/022-disponibilidad-real/spec.md
+++ w/docs/specs/022-disponibilidad-real/spec.md
@@ -59,16 +59,34 @@ Cinco, **serializados**: A2 y A3 tocan el mismo archivo y paralelizarlos garanti
 ### PKG-1 — `registro-de-proveedores`
 - **AC-01**: un único `PROVIDERS: dict[str, ProviderSpec]` del que se **derivan**
   `_PAIR_COMMANDS`, `_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`,
-  `DISCOVERABLE_PROVIDERS` y el key-map de `_configured_models`. Sumar un proveedor pasa de
-  cinco entradas en lockstep manual a **una fila**.
+  `DISCOVERABLE_PROVIDERS`, el key-map de `_configured_models` y **`_MODEL_PREFERENCE_PROVIDERS`
+  (`set_agents_app.py:94`)** — el sexto duplicado, que la primera versión de esta spec no
+  nombraba (F4 del challenge). Sumar un proveedor pasa de **seis** entradas en lockstep manual
+  a **una fila**.
+  **Ojo con su test**: `tests/test_routing.py:3965` dice proteger ese literal pero lo compara
+  contra **otro literal hardcodeado idéntico**, no contra `DISCOVERABLE_PROVIDERS`. Y el
+  comentario de `set_agents_app.py:131-133` afirma lo contrario. Es exactamente el defecto que
+  este paquete existe para eliminar, disfrazado de guarda: el test tiene que comparar contra la
+  **fuente real**.
 - **AC-02**: test de caracterización — las tablas derivadas son **byte-idénticas** a los
   literales de hoy. Este paquete no cambia comportamiento.
 - **AC-03**: ADR-0042 corrige la afirmación de `ADR-0034:124-126` con la medición.
 
 ### PKG-2 — `techo-catalogo-tri-estado`
-- **AC-04**: `[catalog].<key>` pasa a tri-estado, con **el precedente exacto de
-  `[subscriptions]`** (`models_config.py:361-382`): lista = techo curado, `[]` = veto,
-  **ausente = auto**. `_configured_models` se reemplaza por
+- **AC-04**: el tri-estado aplica **solo a `opencode_zen`, `opencode_go` y a proveedores
+  futuros sin key dedicada** — decisión de Federico (2026-08-12), no re-litigable.
+  `[catalog].claude` y `[catalog].codex` **siguen siendo listas obligatorias no vacías**, como
+  hoy. Razón medida por el challenge (F1): `models_config.py:130-136` las exige con `die`,
+  `load_roles` (`:352-357`) las indexa sin fallback, y son las **únicas con filas curadas** en
+  `routes.v1.toml` — si pasaran a auto, cada fila curada existente dispararía el
+  `CATALOG_CEILING_REQUIRED` de AC-06. `[catalog].codex_effort` queda **explícitamente fuera**:
+  es una lista de efforts, no un techo de proveedor, y meterla en la redacción genérica
+  "`<key>`" fue un error de la primera versión.
+  Los tres estados: lista = techo curado, `[]` = veto, **ausente = auto**.
+  **Del precedente de `[subscriptions]` (`models_config.py:361-382`) se toma la FORMA, no el
+  manejo de error** (F1): allí "ausente" degrada a `WARN` y sigue, porque es validación de
+  build; acá alimenta el filtrado en vivo del snapshot de ruteo, y AC-06 exige que el caso malo
+  falle **fuerte y nombrado**. Importar la mansedumbre de subscriptions contradiría a AC-06. `_configured_models` se reemplaza por
   `resolve_ceiling(config, provider) -> ("curated", set) | ("auto", None) | ("veto", set())`,
   consumido por los **tres** sitios que hoy divergen: `_probe_pairs:467-469`,
   `_read_probe_cache:409` (que hoy re-intersecta y en modo auto dejaría el caché siempre vacío)
@@ -93,6 +111,17 @@ Cinco, **serializados**: A2 y A3 tocan el mismo archivo y paralelizarlos garanti
 - **AC-09**: la firma se compone de **presencia e identidad de credencial, nunca material y
   nunca campos que rotan**, hasheada, jamás logueada ni en el envelope — misma disciplina que
   `pi_auth_provider_keys`. Bump de `_CACHE_SCHEMA_VERSION` con su test: hoy no existe ninguno.
+  **Límite estructural de claude-code, aceptado por Federico y a documentar en ADR-0043** (F2):
+  `~/.claude/.credentials.json` **no tiene ningún campo de identidad de cuenta** — codex sí
+  (`tokens.account_id`), y los únicos campos no-rotantes de claude (`scopes`,
+  `subscriptionType`, `rateLimitTier`) no identifican una cuenta. Así que para claude-code la
+  firma detecta **presencia y ausencia** (logout, credencial borrada, archivo ausente) pero
+  **no** un cambio de una cuenta a otra del mismo plan sin pasar por logout. Detectarlo exigiría
+  hashear el `refreshToken`, que esta feature **no** hace. El límite se declara; no se disimula.
+  **Supuesto a validar temprano en este paquete, no de memoria** (F2): que `refreshToken`,
+  `scopes` y `subscriptionType` efectivamente no roten en un refresh normal. Captura A/B del
+  archivo antes y después de un refresh real; si rotan, la firma de claude-code hay que
+  rediseñarla.
 - **AC-10**: **una sola caché**, en la raíz del store (`store.py:326-334`), que ya tiene la
   disciplina de directorio privado y es la que usa la vía de decisión. Migran
   `set_agents_app.py:144,497,843,3133` y `models_config.py:258-259,277-278`. La legada se poda
@@ -111,9 +140,13 @@ Cinco, **serializados**: A2 y A3 tocan el mismo archivo y paralelizarlos garanti
   (`opencode.json#/provider/ollama`), y **jamás** toca una clave que el harness no puso. Test
   obligatorio: un provider agregado a mano por el usuario sobrevive intacto a un install que
   poda otro.
-- **AC-15**: siembra migratoria desde el `opencode.json` vivo, marcando lo del harness como
-  `origin=harness-legacy`. **A nadie le desaparece nada**; recién ahí `--provider-remove
-  ollama` tiene sentido.
+- **AC-15**: siembra migratoria desde el `opencode.json` vivo que registra **todo** lo que haya
+  bajo `provider.*` — decisión de Federico (F3): lo del harness como `origin=harness-legacy` y
+  **cualquier provider que el usuario haya agregado a mano como `origin=user`**. Desde el primer
+  arranque se puede listar, verificar y quitar desde `set-agents`, que es el pedido literal.
+  La alternativa —registrar solo lo que el harness reconoce— dejaba invisibles los providers
+  propios de quien ya tenía el harness instalado, que es el caso más probable.
+  **A nadie le desaparece nada**: el registro declara qué hay y de dónde vino, nunca borra.
 
 ### PKG-5 — `altas-y-bajas-automaticas`
 - **AC-16**: para cada credencial `detected_unlistable` que `route_doctor` ya reporta, se
@@ -129,9 +162,14 @@ Cinco, **serializados**: A2 y A3 tocan el mismo archivo y paralelizarlos garanti
   `alive | dead | unreachable` —nunca "no existe" cuando fue "no contestó"— con el timestamp de
   la medición, y ofrece `--prune-dead`. **Nunca** dentro de `route()`, **nunca** en la clave de
   caché, **nunca** en el spawn.
-- **AC-19**: `--route-doctor` pasa a reportar `listed_by_provider` **y** `usable_after_ceiling`
-  por separado. Hoy `models_listable` mezcla las dos y se lee como la primera: esa es la mentira
-  a corregir.
+- **AC-19**: la separación `listed_by_provider` / `usable_after_ceiling` va en **las tres
+  superficies que muestran "qué proveedores hay"**, no solo en `--route-doctor` (F5):
+  `route_doctor` (`catalog.py:714-793`), `cmd_doctor_all` (`set_agents_app.py:849`) y
+  **`_estado_general_lines` (`:3134`)**. Esa última es el panel del **primer ítem del menú**, o
+  sea la vidriera: es donde un usuario no técnico va a mirar "¿el harness ya ve mi suscripción
+  nueva?". Hoy las tres imprimen `len(models)` post-techo y se leen como "lo que el proveedor
+  expone". El defecto de invisibilidad total se arregla gratis en las tres porque comparten
+  `_probe_pairs`; el de la etiqueta engañosa hay que arreglarlo en cada una.
 
 ## No-goals
 
diff --git i/tests/test_routing.py w/tests/test_routing.py
index 68e76da..fd54acf 100644
--- i/tests/test_routing.py
+++ w/tests/test_routing.py
@@ -20,6 +20,7 @@ ROOT=Path(__file__).resolve().parents[1]
 sys.path.insert(0,str(ROOT/"ai/scripts"))
 import claude_code_spawn
 import models_config
+import provider_registry
 import routing
 import set_agents_app
 import set_agents_spawn
@@ -4002,6 +4003,65 @@ class RoutingTests(unittest.TestCase):
         bogus_inventory = {("opencode", "github-copilot"): {"some-copilot-model"}}
         self.assertEqual(routing_catalog.resolve_discovered_providers(config, bogus_inventory), ())
 
+    # ------------------------------------------------- ADR-0042 (022 PKG-1): single provider
+    # registry -- `provider_registry.PROVIDERS` is now the one source every provider-keyed
+    # table below derives from, replacing six independently hand-edited literal copies.
+
+    def test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals(self):
+        # AC-02 characterization: every literal pasted here is the EXACT pre-ADR-0042 value
+        # (copied from the removed literals, not imported from provider_registry or derived
+        # in this test) -- proves the refactor changed plumbing, never values. Order matters
+        # only for the tuple (AC-01's provider-registry insertion order contract); the dicts
+        # and the set compare by value, so their literal order below is not significant.
+        self.assertEqual(list(provider_registry.PROVIDERS),
+                         ["openai-codex", "anthropic", "opencode-zen", "opencode-go"])
+        self.assertEqual(routing_catalog._OPENCODE_PROVIDER_KEYS, {
+            "openai-codex": "openai", "anthropic": "anthropic",
+            "opencode-zen": "opencode zen", "opencode-go": "opencode go",
+        })
+        self.assertEqual(routing_catalog._OPENCODE_CLI_IDS, {
+            "openai-codex": "openai", "anthropic": "anthropic",
+            "opencode-zen": "opencode", "opencode-go": "opencode-go",
+        })
+        self.assertEqual(routing_catalog.PROVIDER_BILLING_KIND, {
+            "openai-codex": "subscription", "anthropic": "subscription",
+            "opencode-go": "subscription", "opencode-zen": "metered",
+        })
+        # AC-01's fifth symbol: the key map that used to live INLINE inside
+        # `_configured_models` (a local dict literal, unreachable from outside the function)
+        # is now the module-level `_CATALOG_KEYS`, itself derived from `PROVIDERS`.
+        self.assertEqual(routing_catalog._CATALOG_KEYS, {
+            "openai-codex": "codex", "anthropic": "claude",
+            "opencode-zen": "opencode_zen", "opencode-go": "opencode_go",
+        })
+        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS,
+                         {"openai-codex", "anthropic", "opencode-zen", "opencode-go"})
+        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
+                         ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
+
+    def test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source(self):
+        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
+        # lived at test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_
+        # not_just_the_constant (below, :3965 pre-refactor line count) -- inside a
+        # `mock.patch` block whose real purpose is testing the live-probe degrade path, and
+        # it compared against ANOTHER hardcoded literal, never against the audited source.
+        # That test is untouched by this package (still passes, proven by the full-suite
+        # gate) and still is not the guard -- THIS is the guard: two independent
+        # cross-checks against the real source, neither of them a second copy-pasted tuple.
+        #
+        # Cross-check 1: same pattern as test_adr0034_ac10_discoverable_providers_lockstep_
+        # guard above (which only ever covered DISCOVERABLE_PROVIDERS, never this tuple,
+        # despite set_agents_app.py's own comment claiming otherwise pre-ADR-0042) --
+        # membership against `_PAIR_COMMANDS`'s own audited provider set.
+        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
+        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
+        # Cross-check 2: against the ordered registry directly -- this is what makes a
+        # FUTURE regression (someone re-introducing a hand-maintained tuple in
+        # set_agents_app.py instead of deriving it) fail loudly the moment `PROVIDERS`
+        # changes without a matching edit here, rather than silently agreeing with a second,
+        # equally-stale literal (the exact defect this package exists to close).
+        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
+
     # --------------------------------------------- 014-model-preference-policy (AC-01..09)
 
     _MP_TIERED_ROLES = frozenset({"debugger", "delta-reviewer", "finding-verifier",
# P1-registro-de-proveedores — evidencia del implementer

Estado: COMPLETO (ver gates al final).

## Diseño — dónde vive `PROVIDERS`, con la medición

Medición ANTES de escribir código (`grep -rn`, limpio):

```
$ grep -rn "models_config" ai/scripts/routing_core/*.py
ai/scripts/routing_core/catalog.py:203:    # added two more in models_config.py) — this key map is site (2) of 5 that must move
ai/scripts/routing_core/catalog.py:206:    # models_config.load_config's optional-key validation loop for the same two TOML
ai/scripts/routing_core/catalog.py:207:    # keys; (5) models_config.emit's preservation loop for the same two keys (without
ai/scripts/routing_core/catalog.py:211:    # models_config.py sites is read here (fine) but never survives a re-emit (data
ai/scripts/routing_core/catalog.py:698:    as-is (already validated against `DISCOVERABLE_PROVIDERS` at models_config load time);
ai/scripts/routing_core/service.py:121:        # models_config.py already uses for a value that is never itself serialized back
ai/scripts/routing_core/service.py:136:        # a config that skips models_config validation (e.g. a bare {} in a unit test),
```
Solo comentarios; ningún `import`. `catalog.py` no importa `models_config` hoy.

```
$ grep -n "^import\|^from" ai/scripts/models_config.py
import csv / json / re / sys / tomllib / os / tempfile
from pathlib import Path
```
Ningún import de `routing_core` a nivel de módulo (confirmado antes de mi cambio).
`detect_subscriptions` (`:250-267` original) y `auto_profile` (`:269-286` original) hacen
`from routing_core.catalog import probe_inventory` **adentro del cuerpo de la función**, con
docstring explícito: *"Lazy import (no routing_core dependency at module load)"*.

```
$ cat ai/scripts/routing_core/__init__.py
"""Trusted routing-v2 domain and adapters.  The package has no CLI imports."""
from .domain import (...)
from .service import RoutingService
from .store import RoutingStore
```
`routing_core/__init__.py` importa `.service` y `.store` **incondicionalmente**. `service.py`
importa `from .catalog import build_snapshot, ...`. `store.py` importa `sqlite3`, `pwd`,
`secrets`, etc. Consecuencia medida: **cualquier** import de un submódulo de `routing_core`
(`from routing_core.X import Y`, sin importar cuál `X`) ejecuta primero ese `__init__.py`, que
carga `sqlite3`/`subprocess`/toda la maquinaria de `service.py`+`store.py` — el costo exacto que
el docstring de `models_config.py:250-251` existe para evitar en el import a nivel de módulo.

**Conclusión, argumentada, no estética:** `PROVIDERS` no puede vivir dentro de `routing_core`
(pagaría ese costo en cada `import models_config`) ni dentro de `models_config.py` (invertiría
la dirección de dependencia que `routing_core/__init__.py` declara — "no CLI imports" — porque
`models_config.py` es un módulo de aplicación: lee `models.toml` de disco, hace `die()`). Vive en
un módulo nuevo, neutro, fuera de ambos: `ai/scripts/provider_registry.py`, con cero imports
propios más allá de `dataclasses` de la librería estándar. Desarrollo completo de la decisión:
`docs/adr/0042-provider-registry-single-source.md`.

## Los siete símbolos y su derivación

| # | Símbolo | Ubicación (post-cambio) | Deriva de |
|---|---|---|---|
| 1 | `_OPENCODE_PROVIDER_KEYS` | `routing_core/catalog.py:129` | `{p: spec.opencode_auth_key for p, spec in PROVIDERS.items()}` |
| 2 | `_OPENCODE_CLI_IDS` | `catalog.py:137` | `{p: spec.opencode_cli_id for p, spec in PROVIDERS.items()}` |
| 3 | `_PAIR_COMMANDS` | `catalog.py:165-172` | sin cambio de código — su mitad `opencode` ya derivaba de `_OPENCODE_CLI_IDS` desde 012/F-05, y ese ahora deriva transitivamente de `PROVIDERS` |
| 4 | `PROVIDER_BILLING_KIND` | `catalog.py:184` | `{p: spec.billing_kind for p, spec in PROVIDERS.items()}` |
| 5 | key map de `_configured_models` (ahora `_CATALOG_KEYS`, módulo-nivel) | `catalog.py:218` | `{p: spec.catalog_key for p, spec in PROVIDERS.items()}` |
| 6 | `DISCOVERABLE_PROVIDERS` | `models_config.py:55` | `set(provider_registry.PROVIDERS)` |
| 7 | `_MODEL_PREFERENCE_PROVIDERS` | `set_agents_app.py:106` | `tuple(provider_registry.PROVIDERS)` |

Fuente única: `ai/scripts/provider_registry.py` — `PROVIDERS: dict[str, ProviderSpec]`, cuatro
campos por proveedor (`opencode_auth_key`, `opencode_cli_id`, `billing_kind`, `catalog_key`),
orden de inserción `("openai-codex", "anthropic", "opencode-zen", "opencode-go")` — el orden es
el contrato que hace determinística la tupla #7 (nunca derivada de `DISCOVERABLE_PROVIDERS`, que
es un `set` sin orden).

## Conteo "seis" vs "siete", resuelto

La spec (`spec.md` PKG-1) narra "de seis entradas en lockstep manual a una fila" pero el context
pack mide y nombra **siete** símbolos. Resuelto así (y así quedó en ADR-0042):

- **Seis** son duplicados verdaderamente **manuales** — antes de este paquete, nada los ligaba
  entre sí salvo la disciplina humana: `_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`,
  `PROVIDER_BILLING_KIND`, la key map de `_configured_models`, `DISCOVERABLE_PROVIDERS`,
  `_MODEL_PREFERENCE_PROVIDERS`.
- El **séptimo**, `_PAIR_COMMANDS`, ya NO era un duplicado manual antes de este paquete: su
  mitad `opencode` deriva de `_OPENCODE_CLI_IDS` desde el repair F-05 de la feature 012
  (`catalog.py`, comprehension, no literal). Sigue siendo uno de los siete símbolos medidos
  porque también codifica identidad de proveedor (sus dos pares no-opencode) y porque, ahora,
  depende TRANSITIVAMENTE de `PROVIDERS` a través de `_OPENCODE_CLI_IDS` — pero no sumaba un
  duplicado nuevo que este paquete tuviera que eliminar.

"Seis" cuenta lo que había que dejar de duplicar a mano; "siete" cuenta todos los símbolos
medidos que ya no pueden divergir entre sí después de este paquete.

## Tabla AC → cambio → prueba

| AC | Cambio | archivo:línea | Prueba |
|---|---|---|---|
| AC-01 | `PROVIDERS: dict[str, ProviderSpec]`, módulo nuevo | `ai/scripts/provider_registry.py:1-70` | `test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals` (`tests/test_routing.py:4010`) |
| AC-01 | `_OPENCODE_PROVIDER_KEYS`/`_OPENCODE_CLI_IDS` derivadas | `catalog.py:129,137` | mismo test arriba + `test_ac02_ac03_credential_and_cli_id_maps_are_independently_addressable` (`:3127`, sin editar) |
| AC-01 | `PROVIDER_BILLING_KIND` derivada | `catalog.py:184` | mismo test arriba + `test_ac08_subscription_metered_map_is_provider_keyed_not_a_row_field` (`:3257`, sin editar) |
| AC-01 | `_CATALOG_KEYS` nueva (antes literal inline en `_configured_models`) | `catalog.py:211-218,236` | `test_adr0042_ac01_ac02_...` (`:4010`) |
| AC-01 | `DISCOVERABLE_PROVIDERS` derivada | `models_config.py:55` | mismo test arriba + `test_adr0034_ac10_discoverable_providers_lockstep_guard` (`:3192`, sin editar) |
| AC-01 | `_MODEL_PREFERENCE_PROVIDERS` derivada | `set_agents_app.py:106` | mismo test arriba + `test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_not_just_the_constant` (`:3958` post-inserción, sin editar) |
| AC-01b | guarda real: `_MODEL_PREFERENCE_PROVIDERS` contra la fuente (`_PAIR_COMMANDS` y `provider_registry.PROVIDERS`), nunca contra un literal en otro test | — | `test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source` (`tests/test_routing.py:4042`) |
| AC-02 | caracterización byte-idéntica de los siete símbolos | — | `test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals` (`:4010`) |
| AC-02 | tests preexistentes sin editar siguen verdes | — | ver sección siguiente |
| AC-03 | ADR-0042 primero, corrige `ADR-0034:124-126` con la medición, sin editar ADR-0034 | `docs/adr/0042-provider-registry-single-source.md` (sección "Decisión", punto 7), indexado en `docs/adr/README.md` | lectura directa del ADR (no hay test automatizado para prosa de ADR) |

## Prueba de que los tests preexistentes pasan SIN editarlos

```
$ python3 -m unittest tests.test_routing.RoutingTests.test_adr0034_ac10_discoverable_providers_lockstep_guard tests.test_routing.RoutingTests.test_ac02_ac03_credential_and_cli_id_maps_are_independently_addressable tests.test_routing.RoutingTests.test_ac08_subscription_metered_map_is_provider_keyed_not_a_row_field tests.test_routing.RoutingTests.test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_not_just_the_constant -v
test_adr0034_ac10_discoverable_providers_lockstep_guard ... ok
test_ac02_ac03_credential_and_cli_id_maps_are_independently_addressable ... ok
test_ac08_subscription_metered_map_is_provider_keyed_not_a_row_field ... ok
test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_not_just_the_constant ... ok

----------------------------------------------------------------------
Ran 4 tests in 0.010s

OK
```
`git diff tests/test_routing.py` (pegado más abajo) prueba que estas cuatro funciones no fueron
tocadas: el único cambio en ese archivo es el `import provider_registry` nuevo y el bloque de dos
funciones nuevas insertado entre `test_adr0034_m1_github_copilot_never_gets_an_audited_pair_even_
authenticated` y el comentario `# --------------------------------------------- 014-model-
preference-policy`.

## Por cada test nuevo: neutralizar, confirmar rojo, revertir

### `test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals`

Mutación: `provider_registry.py`, `opencode-go`'s `billing_kind` de `"subscription"` a
`"metered"` (vía script Python, no `git`).

```
$ python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals -v
...
AssertionError: {'ope[51 chars]ion', 'opencode-zen': 'metered', 'opencode-go': 'metered'} != {'ope[51 chars]ion', 'opencode-go': 'subscription', 'opencode-zen': 'metered'}
  {'anthropic': 'subscription',
   'openai-codex': 'subscription',
-  'opencode-go': 'metered',
+  'opencode-go': 'subscription',
   'opencode-zen': 'metered'}
----------------------------------------------------------------------
Ran 1 test in 0.004s
FAILED (failures=1)
```

Revert (`cp` desde el backup tomado antes de mutar, `diff` confirma identidad):

```
$ cp "$SCRATCH/backup/provider_registry.py.orig" ai/scripts/provider_registry.py
$ diff "$SCRATCH/backup/provider_registry.py.orig" ai/scripts/provider_registry.py && echo "REVERT_OK identical"
REVERT_OK identical
$ python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals -v
... ok
Ran 1 test in 0.002s
OK
```

### `test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source`

Dos mutaciones independientes de `set_agents_app.py` (una por cada cross-check del test),
cada una revertida con `cp` desde el backup antes de la siguiente.

**Mutación 1** — reproduce el defecto real que este paquete cierra: un `_MODEL_PREFERENCE_
PROVIDERS` hardcodeado, ahora con un proveedor menos (`opencode-go` faltante):

```
_MODEL_PREFERENCE_PROVIDERS = ("openai-codex", "anthropic", "opencode-zen")  # TEMP
```
```
$ python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v
FAIL
AssertionError: Items in the second set but not the first: 'opencode-go'
Ran 1 test in 0.004s
FAILED (failures=1)
```
(el cross-check 1, contra `_PAIR_COMMANDS`, ya detecta esto — nunca llega a evaluar el cross-check 2)

**Mutación 2** (tras revertir la 1) — mismo *set* de cuatro proveedores, orden distinto (prueba
que el cross-check 1, por sets, NO alcanza solo — hace falta el cross-check 2, por tupla):

```
_MODEL_PREFERENCE_PROVIDERS = ("anthropic", "openai-codex", "opencode-zen", "opencode-go")  # TEMP
```
```
$ python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v
FAIL
AssertionError: Tuples differ: ('anthropic', 'openai-codex', 'opencode-zen', 'opencode-go') != ('openai-codex', 'anthropic', 'opencode-zen', 'opencode-go')
Ran 1 test in 0.005s
FAILED (failures=1)
```

Revert final y verde:

```
$ cp "$SCRATCH/backup/set_agents_app.py.orig" ai/scripts/set_agents_app.py
$ diff "$SCRATCH/backup/set_agents_app.py.orig" ai/scripts/set_agents_app.py && echo "REVERT_OK identical"
REVERT_OK identical
$ python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals -v
... ok
... ok
Ran 2 tests in 0.004s
OK
```

Nota: el backup `set_agents_app.py.orig` usado para el revert de la Mutación 2 es el mismo
tomado ANTES de la Mutación 1 (archivo intacto de mi propio cambio de implementación, sin
ninguna de las dos mutaciones de prueba) — verificado con `diff` contra el estado real tras cada
revert, no asumido.

## Un defecto real que este mismo ejercicio encontró (y por qué el gate completo importa)

La primera redacción de mi corrección al comentario de `set_agents_app.py:92-104` (el no-goal de
AC-06) escribió el símbolo `PROVIDER_BILLING_KIND` **en texto libre, dos veces**, para explicar
por qué seguía sin usarse. Eso rompió `test_ac06_no_provider_billing_kind_reference_in_new_code`
(`tests/test_routing.py:4817-4820`, doctrina de 014-model-preference-policy, sin relación
aparente con este paquete) — un `assertNotIn("PROVIDER_BILLING_KIND", ...)` literal sobre el
archivo completo, que no distingue código de comentario. Lo encontró la corrida completa de
`tests.test_routing` (258 tests, ver abajo), no ninguna corrida dirigida mía. Corregido
reescribiendo el comentario para nombrar la tabla por perífrasis ("the catalog module's own
billing-kind classification table"), como ya hacía el comentario ORIGINAL antes de mi edición —
nunca por símbolo literal. Re-verificado:

```
$ grep -n "PROVIDER_BILLING_KIND" ai/scripts/set_agents_app.py
(sin salida)
$ python3 -m unittest tests.test_routing.RoutingTests.test_ac06_no_provider_billing_kind_reference_in_new_code -v
... ok
Ran 1 test in 0.003s
OK
```

## Gates

### `python3 -m unittest tests.test_routing -v` (módulo completo, corrida directa, PRE-fix)

Esta corrida es la que encontró el defecto real de arriba (`PROVIDER_BILLING_KIND` en
comentario) — corrió ANTES del arreglo, con el archivo de prueba nuevo ya en su lugar:

```
Ran 258 tests in 152.674s
FAILED (failures=1)
FAIL: test_ac06_no_provider_billing_kind_reference_in_new_code
```

No volví a correr el módulo completo (258 tests, ~2.5 min) una segunda vez tras el arreglo —
sería redundante con el gate 1 de abajo, que corre `tests/test_routing.py` íntegro como parte
de `discover`. Lo que sí corrí, dirigido, post-fix: el test que había fallado
(`test_ac06_no_provider_billing_kind_reference_in_new_code`, verde, pegado arriba) y los seis
tests nuevos/afectados de esta sección (también verdes, pegados arriba). El veredicto real de
"todo `test_routing.py` en verde, con el arreglo puesto" es el que da el gate 1.

### Gate 1 — `heartbeat-run.py -- python3 -m unittest discover -s tests`

Corrida completa, con el arreglo de `test_ac06_no_provider_billing_kind_reference_in_new_code`
ya puesto (backgrounded vía `run_in_background`, seguida con `tail -f --pid=<heartbeat-run pid>`
para no perder salida — nunca `| tail -N` sin `-f`, ADR-0041):

```
$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest discover -s tests
[... heartbeats cada 20s mientras corre, ~515s totales ...]

exec
/bin/bash -lc "wc -l docs/specs/022-disponibilidad-real/evidence/P1-implementer.md docs/specs/022-disponibilidad-real/context/P1-registro-de-proveedores.md docs/specs/022-disponibilidad-real/spec.md docs/adr/0042-provider-registry-single-source.md && rg -n -C 5 'AC-01|AC-04' docs/specs/022-disponibilidad-real/spec.md && nl -ba ai/scripts/provider_registry.py && nl -ba ai/scripts/routing_core/catalog.py | sed -n '100,250p' && nl -ba ai/scripts/routing_core/catalog.py | sed -n '630,685p' && nl -ba ai/scripts/models_config.py | sed -n '35,75p' && nl -ba ai/scripts/set_agents_app.py | sed -n '80,125p' && nl -ba docs/adr/0042-provider-registry-single-source.md && nl -ba docs/adr/0034-*.md | sed -n '112,136p' && nl -ba tests/test_routing.py | sed -n '3985,4080p'" in /home/federico/SET-AGENTES
 succeeded in 0ms:
  308 docs/specs/022-disponibilidad-real/evidence/P1-implementer.md
  153 docs/specs/022-disponibilidad-real/context/P1-registro-de-proveedores.md
  211 docs/specs/022-disponibilidad-real/spec.md
  169 docs/adr/0042-provider-registry-single-source.md
  841 total
55-## Paquetes
56-
57-Cinco, **serializados**: A2 y A3 tocan el mismo archivo y paralelizarlos garantiza conflicto.
58-
59-### PKG-1 — `registro-de-proveedores`
60:- **AC-01**: un único `PROVIDERS: dict[str, ProviderSpec]` del que se **derivan**
61-  `_PAIR_COMMANDS`, `_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`,
62-  `DISCOVERABLE_PROVIDERS`, el key-map de `_configured_models` y **`_MODEL_PREFERENCE_PROVIDERS`
63-  (`set_agents_app.py:94`)** — el sexto duplicado, que la primera versión de esta spec no
64-  nombraba (F4 del challenge). Sumar un proveedor pasa de **seis** entradas en lockstep manual
65-  a **una fila**.
--
71-- **AC-02**: test de caracterización — las tablas derivadas son **byte-idénticas** a los
72-  literales de hoy. Este paquete no cambia comportamiento.
73-- **AC-03**: ADR-0042 corrige la afirmación de `ADR-0034:124-126` con la medición.
74-
75-### PKG-2 — `techo-catalogo-tri-estado`
76:- **AC-04**: el tri-estado aplica **solo a `opencode_zen`, `opencode_go` y a proveedores
77-  futuros sin key dedicada** — decisión de Federico (2026-08-12), no re-litigable.
78-  `[catalog].claude` y `[catalog].codex` **siguen siendo listas obligatorias no vacías**, como
79-  hoy. Razón medida por el challenge (F1): `models_config.py:130-136` las exige con `die`,
80-  `load_roles` (`:352-357`) las indexa sin fallback, y son las **únicas con filas curadas** en
81-  `routes.v1.toml` — si pasaran a auto, cada fila curada existente dispararía el
     1	"""ADR-0042 (022 PKG-1): the single audited provider registry.
     2	
     3	`PROVIDERS` is the ONE source `routing_core/catalog.py`, `models_config.py`, and
     4	`set_agents_app.py` derive their provider-keyed tables from -- before this module existed,
     5	those three files hand-maintained SIX independently-editable copies of the same four-provider
     6	universe (`_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, the key map
     7	inside `_configured_models`, `DISCOVERABLE_PROVIDERS`, `_MODEL_PREFERENCE_PROVIDERS`), synced
     8	only by human discipline. Adding a provider now means adding one `ProviderSpec` row here.
     9	
    10	Deliberately outside the `routing_core` package and importing NEITHER `routing_core` NOR
    11	`models_config`: `routing_core/__init__.py` unconditionally imports `.service`/`.store`
    12	(sqlite3, subprocess, ...) on ANY submodule import, and `models_config.py` documents (at
    13	`detect_subscriptions`/`auto_profile`) that it deliberately avoids a `routing_core` dependency
    14	at module load. A module with zero imports beyond `dataclasses` is importable at module scope
    15	by all three consumers without paying either cost -- see ADR-0042 for the measured alternatives
    16	this rejected.
    17	
    18	The dict's INSERTION ORDER is a real contract, not incidental: `set_agents_app.
    19	_MODEL_PREFERENCE_PROVIDERS` derives its ordered tuple from `tuple(PROVIDERS)`, and that order
    20	(`openai-codex, anthropic, opencode-zen, opencode-go`) surfaces in user-facing validation
    21	messages. Never reorder this dict without checking that consumer.
    22	"""
    23	
    24	from __future__ import annotations
    25	
    26	import dataclasses
    27	
    28	
    29	@dataclasses.dataclass(frozen=True)
    30	class ProviderSpec:
    31	    """One audited provider's identity across every axis this registry replaces.
    32	
    33	    `opencode_auth_key`: the credential display text `opencode auth list --pure` prints for
    34	    this provider (map 1 of 2, 012 AC-02) -- used ONLY at the credential-set membership check.
    35	    `opencode_cli_id`: the CLI argument for `opencode models <id>` AND the `<id>/<model>` line
    36	    prefix `_parse_opencode_models` strips (map 2 of 2) -- independently addressable from
    37	    `opencode_auth_key` (012 AC-02: the two coincide for `openai-codex`/`anthropic` but diverge
    38	    for the OpenCode-lane providers).
    39	    `billing_kind`: `"subscription"` or `"metered"` (012 AC-08 / ADR-0035), curated per
    40	    provider, never a `routes.v1.toml` row field.
    41	    `catalog_key`: the `[catalog]` table key in `models.toml` that curates this provider's
    42	    model allowlist (012 AC-04).
    43	    """
    44	
    45	    opencode_auth_key: str
    46	    opencode_cli_id: str
    47	    billing_kind: str
    48	    catalog_key: str
    49	
    50	
    51	# Order is a contract -- see module docstring. Values are unchanged from the pre-ADR-0042
    52	# literals (PKG-1 AC-02 characterization test pins this).
    53	PROVIDERS: dict[str, ProviderSpec] = {
    54	    "openai-codex": ProviderSpec(
    55	        opencode_auth_key="openai", opencode_cli_id="openai",
    56	        billing_kind="subscription", catalog_key="codex",
    57	    ),
    58	    "anthropic": ProviderSpec(
    59	        opencode_auth_key="anthropic", opencode_cli_id="anthropic",
    60	        billing_kind="subscription", catalog_key="claude",
    61	    ),
    62	    "opencode-zen": ProviderSpec(
    63	        opencode_auth_key="opencode zen", opencode_cli_id="opencode",
    64	        billing_kind="metered", catalog_key="opencode_zen",
    65	    ),
    66	    "opencode-go": ProviderSpec(
    67	        opencode_auth_key="opencode go", opencode_cli_id="opencode-go",
    68	        billing_kind="subscription", catalog_key="opencode_go",
    69	    ),
    70	}
   100	CANONICAL_MODEL: dict[tuple[str, str], str] = {("anthropic", short): canonical
   101	                                               for short, canonical in _ANTHROPIC_CANONICAL.items()}
   102	CANONICAL_MODEL.update({(provider, canonical): canonical
   103	                        for provider in ("opencode-zen", "opencode-go")
   104	                        for canonical in _ANTHROPIC_CANONICAL.values()})
   105	
   106	
   107	def canonical_model(provider: str, model: str) -> str:
   108	    """Curated cross-provider model-identity normalization (repair SEC-001). Resolves a
   109	    (provider, model) pair to a canonical id ONLY where CANONICAL_MODEL explicitly
   110	    curates the two as the same underlying model; every other pair maps to its own
   111	    `model` string unchanged (identity), so this can never widen equivalence beyond what
   112	    a human curated. Consumed by `_check_family_collisions` below (layer 1) and by
   113	    `service.py`'s REVIEW_MODEL_CONFLICT hard exclusion (layer 2, defense in depth)."""
   114	    return CANONICAL_MODEL.get((provider, model), model)
   115	
   116	
   117	# ADR-0042 (022 PKG-1): both maps below DERIVE from the single `PROVIDERS` registry now
   118	# (`provider_registry.py`) -- before this ADR they were two independently hand-edited
   119	# literals, one of six provider-keyed tables that had to move in lockstep by hand across
   120	# this file, `models_config.py`, and `set_agents_app.py`. Values are unchanged from the
   121	# pre-ADR-0042 literals (PKG-1 AC-02 characterization test pins this).
   122	#
   123	# Catalog provider -> credential display text `_parse_opencode_auth` yields (map 1 of 2,
   124	# 012 AC-02). Used ONLY at the credential-set membership check. For the two pre-existing
   125	# pairs this string happens to coincide with the CLI argument below; for opencode-zen/
   126	# opencode-go it does not ("opencode zen"/"opencode go", two-token display text, vs the
   127	# single-token CLI ids in _OPENCODE_CLI_IDS) — re-measured live against `opencode auth
   128	# list --pure` (docs/specs/012-discovered-inventory/spec.md AC-02).
   129	_OPENCODE_PROVIDER_KEYS = {provider: spec.opencode_auth_key for provider, spec in PROVIDERS.items()}
   130	# Catalog provider -> CLI argument for `opencode models <id>` AND the `<id>/<model>` line
   131	# prefix `_parse_opencode_models` strips (map 2 of 2, 012 AC-02). Independently
   132	# addressable from _OPENCODE_PROVIDER_KEYS above: using the CLI-id value at the
   133	# credential-membership check would search the two-token display-name set for a
   134	# single-token string and report the pair absent on every machine (the defect this
   135	# two-map split fixes; PI_MODEL_MAP is explicitly the wrong precedent here — it
   136	# translates a model name within one already-matched provider, a different axis).
   137	_OPENCODE_CLI_IDS = {provider: spec.opencode_cli_id for provider, spec in PROVIDERS.items()}
   138	# ADR-0034 (019 PKG-1) re-measurement, live, 2026-08-10 (opencode 1.18.14): `opencode
   139	# auth list --pure` shows 4 real credentials -- `opencode-go`, `openai`, `github-copilot`,
   140	# `opencode` (auth.json keys). `github-copilot` is authenticated but `opencode models
   141	# github-copilot --pure` (even with `--refresh`) answers `Error: Provider not found:
   142	# github-copilot` -- no CLI id verifies it, so NO pair is added for it here (M-1, fail-
   143	# closed: never hardcode an unverified CLI id, never derive one by a display-name-to-
   144	# CLI-id heuristic like "replace the space with a hyphen" -- that would have produced
   145	# the WRONG id for `opencode-go` itself, "opencode go" -> "opencode-go" only works by
   146	# coincidence for that one pair and fails silently for anything else). The opencode
   147	# `openai` provider (OAuth ChatGPT) is NOT a new provider either -- it is the display/CLI
   148	# name this table ALREADY maps to `openai-codex` (M-2); no duplicate catalog provider is
   149	# added for it. Detection is structural, not a separate code path: `github-copilot`
   150	# never appears in `_PAIR_COMMANDS`, so `_probe_pairs` never even queries it -- the rest
   151	# of the inventory is never at risk of an unverified id leaking in. `--route-doctor`
   152	# (PKG-2, ADR-0035) is the surface that will report "detected, unlistable" to a human;
   153	# nothing here needs to change for that report to be accurate.
   154	# The audited pair table is the single source of runtime/provider compatibility:
   155	# a pair absent here can never authenticate, list models, or appear in identities.
   156	# 012 AC-01: two OpenCode-lane pairs added, `opencode-zen`/`opencode-go` — real,
   157	# currently-authenticated OpenCode providers that were simply outside this table
   158	# (see docs/specs/012-discovered-inventory/spec.md AC-01). No other runtime gains a
   159	# pair; codex/claude-code/pi have no client for either and none is implied here.
   160	# Repair F-05 (012 repair): the opencode-lane argv is DERIVED from _OPENCODE_CLI_IDS
   161	# above, never a second hardcoded copy of the CLI id — a future change to one map with
   162	# the other left stale would otherwise silently leave a pair absent, undetected by any
   163	# test or the live gate (both only ever exercise whichever id `_PAIR_COMMANDS` itself
   164	# carries).
   165	_PAIR_COMMANDS = {
   166	    ("codex", "openai-codex"): (("codex", "login", "status"),),
   167	    ("claude-code", "anthropic"): (("claude", "auth", "status", "--json"),),
   168	    **{("opencode", provider): (("opencode", "auth", "list", "--pure"), ("opencode", "models", cli_id, "--pure"))
   169	       for provider, cli_id in _OPENCODE_CLI_IDS.items()},
   170	    ("pi", "openai-codex"): (pi_pinned_argv("--list-models"),),
   171	    ("pi", "anthropic"): (pi_pinned_argv("--list-models"),),
   172	}
   173	# 012 AC-08: billing-model distinction, curated per provider — NEVER a routes.v1.toml row
   174	# field (the row schema is closed, required_keys/optional_keys below; an extra key raises
   175	# CATALOG_INVALID for every provider, existing two included). OpenCode Go is a monthly
   176	# subscription; OpenCode Zen is metered/API-key (user clarification, logged at
   177	# ai/state/decisions-log.jsonl, slug opencode-zen-go-billing-model-distinto-no-mismo-pool).
   178	# ADR-0035 (019 PKG-2): completed to cover every DISCOVERABLE_PROVIDERS member --
   179	# `openai-codex`/`anthropic` are the curated subscription lanes (Codex CLI login, Claude
   180	# Code OAuth), `opencode-go` stays subscription, `opencode-zen` stays the sole metered
   181	# entry. This is the day `billing_rank` (below) starts reading it -- the previous "no
   182	# weighting/selection logic reads this map yet" note is retired.
   183	# ADR-0042 (022 PKG-1): derives from `PROVIDERS` now, same values as the pre-ADR-0042 literal.
   184	PROVIDER_BILLING_KIND = {provider: spec.billing_kind for provider, spec in PROVIDERS.items()}
   185	# ADR-0035 AC-12: the FREE-model convention is the `-free` suffix -- the exact same
   186	# pattern `inference._FAST_HINTS` already treats as a signal, never a second, competing
   187	# definition of "free" invented here.
   188	_FREE_MODEL_SUFFIX = re.compile(r"-free(\b|$)")
   189	
   190	
   191	def billing_rank(provider: str, model: str) -> int:
   192	    """ADR-0035 (AC-12): pure order-only signal, `0` (cheap: subscription or a free-suffixed
   193	    model) or `1` (metered, or a provider this map does not even know -- fail-closed toward
   194	    the expensive rank, never toward the cheap one for an unrecognized provider). Consulted
   195	    ONLY by `service.route`'s sort key, strictly between `TIER_ORDER` and `_bias_rank`
   196	    (ADR-0035 d.3) -- it can never affect which candidates reach the sort, only the order
   197	    among candidates that already survived every hard exclusion."""
   198	    if PROVIDER_BILLING_KIND.get(provider) == "subscription":
   199	        return 0
   200	    if isinstance(model, str) and _FREE_MODEL_SUFFIX.search(model):
   201	        return 0
   202	    return 1
   203	_ANSI = re.compile(r"\x1b\[[0-9;]*m")
   204	
   205	
   206	def allowed_probe(runtime: str, provider: str) -> tuple[tuple[str, ...], ...] | None:
   207	    """The closed, exact argv list for a pair; None for any unaudited pair."""
   208	    return _PAIR_COMMANDS.get((runtime, provider))
   209	
   210	
   211	# ADR-0042 (022 PKG-1): provider -> `[catalog]` TOML key, derived from `PROVIDERS` (same
   212	# values as the pre-ADR-0042 inline literal `_configured_models` used to carry). Site (2) of
   213	# the five-site lockstep `_configured_models` documents below -- the other four sites (the
   214	# `[catalog]` table itself, `build_snapshot`'s own comprehension, and the two
   215	# `models_config.py` sites) are a DIFFERENT, wider concern (012 AC-04: whether a TOML key
   216	# exists and round-trips through `load_config`/`emit` at all) and are out of this package's
   217	# scope -- only this key map derives from the single provider registry.
   218	_CATALOG_KEYS = {provider: spec.catalog_key for provider, spec in PROVIDERS.items()}
   219	
   220	
   221	def _configured_models(config: dict, provider: str) -> set[str]:
   222	    # 012 AC-04: same five-site lockstep as the pre-existing two providers (corrected
   223	    # 012 repair F-04: the original comment undercounted "three sites" — this package
   224	    # added two more in models_config.py) — this key map is site (2) of 5 that must move
   225	    # together with: (1) models.toml's [catalog] table itself; (2) this key map (now
   226	    # `_CATALOG_KEYS` above, ADR-0042); (3) build_snapshot's own `configured_models`
   227	    # comprehension (catalog.py, below); (4) models_config.load_config's optional-key
   228	    # validation loop for the same two TOML keys; (5) models_config.emit's preservation
   229	    # loop for the same two keys (without which a future `./setup-models.sh` write would
   230	    # silently drop the allowlist). Sites (4)/(5) exist because [catalog] has no
   231	    # closed-schema check the way [routing]/[permissions] do — an allowlist key present in
   232	    # the TOML but absent from both models_config.py sites is read here (fine) but never
   233	    # survives a re-emit (data loss). Extending only site (1) leaves the other providers'
   234	    # allowlists unreachable from code; extending only (1)-(3) without (4)/(5) leaves them
   235	    # reachable but silently unpersisted on the next wizard run.
   236	    key = _CATALOG_KEYS.get(provider)
   237	    models = config.get("catalog", {}).get(key, ()) if key else ()
   238	    if not isinstance(models, (list, tuple)):
   239	        return set()
   240	    return {item for item in models if isinstance(item, str) and item}
   241	
   242	
   243	def _clean_lines(text: str) -> list[str]:
   244	    return [line.strip() for line in _ANSI.sub("", text).splitlines() if line.strip()]
   245	
   246	
   247	def _parse_codex_login(stdout: str, stderr: str = "") -> bool:
   248	    """`codex login status` prints a plain-text state line (observed on stderr), never JSON."""
   249	    lines = _clean_lines(stdout) or _clean_lines(stderr)
   250	    return bool(lines) and lines[0].startswith("Logged in")
   630	
   631	def build_snapshot(catalog_path: Path, roster: list[dict], config: dict, digest=None) -> CatalogSnapshot:
   632	    from .domain import TIER_ORDER
   633	    try:
   634	        source = tomllib.loads(catalog_path.read_text(encoding="utf-8"))
   635	        if set(source) != {"catalog_version", "routes"} or source["catalog_version"] != 2 or not isinstance(source["routes"], list): raise ValueError
   636	        rows = source["routes"]
   637	        enabled = set(config["routing"]["enabled_providers"])
   638	        codex_efforts = set(config["catalog"].get("codex_effort", ()))
   639	        xhigh_ok = bool(config.get("routing", {}).get("xhigh_benchmarked", False))
   640	    except (OSError, tomllib.TOMLDecodeError, KeyError, TypeError, ValueError) as exc: raise RoutingError("CATALOG_INVALID") from exc
   641	    roster_names = {row["role"] for row in roster}; routes=[]; canonical_seen=set(); ids={}; route_runtimes={}
   642	    # 012 AC-04, site 3 of 5 (corrected 012 repair F-04 — see _configured_models's own
   643	    # comment above for the full five-site enumeration; moves in lockstep with
   644	    # models.toml's [catalog] table and _configured_models's key map at minimum):
   645	    # opencode-zen/opencode-go are listed here too, even though no routes.v1.toml row
   646	    # references them yet — this contract's non-goals paragraph states "no curated
   647	    # routes.v1.toml rows for the new models" (docs/specs/012-discovered-inventory/
   648	    # spec.md, "Non-goals of P2"; corrected 012 repair F-07 — this is NOT AC-11, which is
   649	    # the cache/decision-trail AC and says nothing about routes.v1.toml rows) — an absent
   650	    # provider's allowlist would otherwise be silently unreachable the moment a row for it
   651	    # is curated later.
   652	    configured_models = {provider: _configured_models(config, provider)
   653	                         for provider in ("openai-codex", "anthropic", "opencode-zen", "opencode-go")}
   654	    # Closed row schema: required keys plus the allowlisted-optional `runtimes` (contract 004 AC-12:
   655	    # never part of the canonical static-ID tuple; rows differing only in runtimes stay duplicates).
   656	    required_keys = {"provider", "model", "family", "effort", "tier", "roles", "tools", "curated_priority"}
   657	    optional_keys = {"runtimes"}
   658	    audited: dict[str, set[str]] = {}
   659	    for runtime, provider in _PAIR_COMMANDS:
   660	        audited.setdefault(provider, set()).add(runtime)
   661	    for row in rows:
   662	        try:
   663	            if not required_keys <= set(row) or set(row) - required_keys - optional_keys: raise ValueError
   664	            provider, model, family, effort, tier = (row[key] for key in ("provider", "model", "family", "effort", "tier"))
   665	            roles, tools = (_sorted_strings(row[key]) for key in ("roles", "tools"))
   666	            priority = row["curated_priority"]
   667	            if (provider not in enabled or model not in configured_models.get(provider, set())
   668	                    or not isinstance(tier, str) or tier not in TIER_ORDER
   669	                    or not isinstance(priority, int) or priority < 0 or not set(roles) <= roster_names): raise ValueError
   670	            if provider == "openai-codex" and (effort not in codex_efforts or (effort == "xhigh" and not xhigh_ok)): raise ValueError
   671	            if provider == "anthropic" and effort != "medium": raise ValueError
   672	            runtimes = None
   673	            if "runtimes" in row:
   674	                runtimes = _sorted_strings(row["runtimes"])
   675	                if not set(runtimes) <= audited.get(provider, set()): raise ValueError
   676	            rid = StaticRoute.identifier(2, provider, model, family, effort, (tier,), roles, tools, priority, digest or __import__("hashlib").sha256)
   677	            canon=(2, provider, model, family, effort, (tier,), roles, tools, priority)
   678	            if canon in canonical_seen: raise RoutingError("CATALOG_INVALID")
   679	            if rid in ids and ids[rid] != canon: raise RoutingError("CATALOG_COLLISION")
   680	            canonical_seen.add(canon); ids[rid]=canon
   681	            route = StaticRoute(2, provider, model, family, effort, tier, roles, tools, priority, rid)
   682	            routes.append(route); route_runtimes[rid] = set(runtimes) if runtimes else None
   683	        except (KeyError, TypeError, ValueError): raise RoutingError("CATALOG_INVALID")
   684	    # 012 AC-07: runs before any shared-id row set is accepted — trivially satisfied by
   685	    # today's six real rows (no shared model id across openai-codex/anthropic), meaningful
    35	READ_ONLY = {"coord-ro", "review-ro"}
    36	IMPLEMENT_DUTIES = {"implement"}
    37	REVIEW_DUTIES = {"audit", "judge"}
    38	AREA_FIELDS = ("claude", "codex", "codex_effort", "opencode")
    39	# The one role-override field that is NOT an area field: a lane-aware tier table
    40	# consumed only by load_role_tiers, never by resolve_role's base merge. Areas never
    41	# declare it — only [roles.<role>] may.
    42	TIER_FIELD = "tiers"
    43	RUNTIMES = ("opencode", "claude-code", "codex", "pi")
    44	MODEL_TIERS = ("fast", "balanced", "frontier")
    45	ROUTING_PROVIDERS = {"openai-codex", "anthropic"}
    46	# ADR-0029 (017 PKG-B2): providers eligible for SYNTHESIZED routes (probe-discovered,
    47	# tier/family inferred). Distinct from ROUTING_PROVIDERS on purpose — the curated
    48	# allowlist stays closed (immutable contract "probeable, never routable, by 012 alone");
    49	# this is the separate, opt-in surface that makes a discovered model routable.
    50	# ADR-0042 (022 PKG-1): derives from the single `provider_registry.PROVIDERS` registry now,
    51	# same four members as the pre-ADR-0042 literal. Order-insensitive on purpose (a `set`) --
    52	# `set_agents_app._MODEL_PREFERENCE_PROVIDERS` is the ORDERED sibling table, and it derives
    53	# from `provider_registry.PROVIDERS` directly rather than from this set, because a tuple
    54	# built from a set has no deterministic order across runs.
    55	DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)
    56	ROUTING_DEFAULTS = {
    57	    "enabled_providers": ["openai-codex", "anthropic"], "xhigh_benchmarked": False,
    58	    # ADR-0034 (019 PKG-1): "auto" is the new default -- discovered_providers derives
    59	    # from the live probed inventory (routing_core.catalog.resolve_discovered_providers)
    60	    # intersected with the audited _PAIR_COMMANDS provider set, never wider. An explicit
    61	    # [] still means "disabled entirely" and survives emit() (compared against this exact
    62	    # default value below, never truthiness, so [] no longer round-trips into "auto").
    63	    "discovered_providers": "auto",
    64	    "max_enabled": False, "fallback_limit": 1, "single_writer": True,
    65	    "sla": {"fast": {"checkpoint_minutes": 18, "cutoff_minutes": 20, "ceiling_minutes": 30}},
    66	    "budgets": {"direct_spawns": 0, "fast_spawns": 1, "scoped_spawns": 4,
    67	                "feature_spawns_per_package": 12, "incident_spawns": 2},
    68	}
    69	# Which subscription a resolved opencode model consumes, by provider prefix.
    70	# Extendable per repo via the optional [providers] table in models.toml.
    71	SUBSCRIPTION_BY_PREFIX = {
    72	    "openai": "openai",
    73	    "opencode": "zen",
    74	    "opencode-go": "zen",
    75	    "anthropic": "anthropic",
    80	#
    81	# One real, per-harness-install sibling config file next to `APP_CONFIG` (the same
    82	# private `STATE_DIR`, `write_app_config`'s own directory) -- NEVER routed through
    83	# `write_app_config` (its flat `key = value` serializer, `:XXX` below, corrupts a nested
    84	# preference table) or `app_config()` (its silent parse-failure-as-`{}` swallow). This
    85	# file gets its OWN dedicated, atomic, fail-closed round-trip: `atomic_write` (already
    86	# established for the Claude settings JSON writer above) for the temp-file+`os.replace`
    87	# discipline, and a small, purpose-built two-table TOML serializer for a schema this
    88	# contract fully owns and closes (`[preference]`, `[role_override]`) -- not a
    89	# general-purpose nested-TOML writer (round-2 finding R2-F-04).
    90	MODEL_PREFERENCE_PATH = STATE_DIR / "model-preference.toml"
    91	
    92	# The closed, four-provider universe `_PAIR_COMMANDS` already probes
    93	# (`routing_core/catalog.py:165-172`). ADR-0042 (022 PKG-1) supersedes this table's own
    94	# ORIGINAL "defined independently here, never importing" framing -- that framing was wider
    95	# than the reason it named: the AC-06 non-goal it cites (014-model-preference-policy,
    96	# test_ac06_no_provider_billing_kind_reference_in_new_code) is specifically about never
    97	# letting the catalog module's own billing-kind classification table decide provider
    98	# membership here, and that narrower non-goal still holds -- this tuple's derivation below
    99	# never reads that table (nor even names it -- see the test above, unchanged by this
   100	# package). What changed is provider IDENTITY: this tuple now derives from the single
   101	# `provider_registry.PROVIDERS` registry every provider-keyed table in the harness shares,
   102	# instead of being a second hand-maintained copy of the same four provider ids. Order is a
   103	# real contract (surfaces in pin/preference validation errors) --
   104	# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
   105	# because that sibling table is an unordered `set`.
   106	_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
   107	_MODEL_PREFERENCE_CLASSES = ("decision", "grunt", "build")
   108	# AC-02's inertness note: the six roles with real, live effect today (`### Honest scope`,
   109	# spec) -- every other in-scope role, and the whole `decision` class, is genuinely inert.
   110	_MODEL_PREFERENCE_LIVE_ROLES = frozenset({
   111	    "delta-reviewer", "finding-verifier", "package-reviewer", "security-auditor",
   112	    "implementer", "debugger",
   113	})
   114	
   115	
   116	class ModelPreferenceError(ValueError):
   117	    """AC-02: fail-closed error for the sibling `model-preference.toml` file/CLI -- a
   118	    malformed value never degrades to a silent default (round-1 F-07, round-3 R3-F-04)."""
   119	
   120	
   121	def _model_preference_die(message):
   122	    raise ModelPreferenceError(message)
   123	
   124	
   125	def _effective_preference_providers():
     1	# ADR-0042 — Un único `PROVIDERS` del que se derivan las siete tablas de proveedores
     2	
     3	- Estado: Accepted (2026-08-12). Feature 022-disponibilidad-real, PKG-1 (`P1-registro-de-proveedores`).
     4	  Corrige en parte una afirmación de ADR-0034 (Rejected alternatives) — no lo supersede: ADR-0034 sigue
     5	  vigente en todo lo demás.
     6	
     7	## Contexto
     8	
     9	Medición en vivo sobre el estado actual del repo (antes de este paquete), `grep -rn` de cada símbolo:
    10	
    11	| Símbolo | Ubicación | Qué codifica |
    12	|---|---|---|
    13	| `_OPENCODE_PROVIDER_KEYS` | `routing_core/catalog.py:116-117` | provider → texto de credencial que `opencode auth list --pure` imprime |
    14	| `_OPENCODE_CLI_IDS` | `catalog.py:125-126` | provider → argumento CLI de `opencode models <id>` |
    15	| `_PAIR_COMMANDS` | `catalog.py:154-161` | el universo auditado runtime×provider; su mitad `opencode` **ya** deriva de `_OPENCODE_CLI_IDS` (comprehension, no literal) desde 012/F-05 |
    16	| `PROVIDER_BILLING_KIND` | `catalog.py:172-173` | provider → `subscription`\|`metered` |
    17	| key map de `_configured_models` | `catalog.py:215-216` (dentro del cuerpo de la función) | provider → clave de `[catalog]` en `models.toml` |
    18	| `DISCOVERABLE_PROVIDERS` | `models_config.py:41` | el `set` de providers elegibles para `"auto"` (ADR-0034) |
    19	| `_MODEL_PREFERENCE_PROVIDERS` | `set_agents_app.py:94` | la `tuple` **ordenada** que valida pins/preferencias de modelo (ADR-0018/ADR-0034 AC-09) |
    20	
    21	Siete símbolos, medidos. **Seis** de ellos son duplicados *manuales* — hoy, agregar un proveedor
    22	exige tocar las seis tablas a mano y nada las liga entre sí salvo la disciplina humana. El séptimo,
    23	`_PAIR_COMMANDS`, ya era una excepción parcial: su mitad `opencode` deriva de `_OPENCODE_CLI_IDS`
    24	desde el repair F-05 de la feature 012 (`catalog.py:149-152`), así que no suma un duplicado nuevo —
    25	pero sigue siendo uno de los siete lugares que un proveedor nuevo toca, porque sus dos pares no-opencode
    26	(`codex`/`openai-codex`, `claude-code`/`anthropic`) usan los ids de provider como claves literales de
    27	dict. Esto resuelve la discrepancia de conteo entre la spec (que llama "seis" en la narrativa de
    28	`docs/specs/022-disponibilidad-real/spec.md` — "de seis entradas en lockstep manual a una fila" — y
    29	sin embargo nombra los siete en la tabla del context pack): **"seis" cuenta los duplicados
    30	verdaderamente manuales; "siete" cuenta todos los símbolos que este paquete mide y dejan de poder
    31	divergir entre sí**, uno de los cuales (`_PAIR_COMMANDS`) ya no era manual antes de este ADR.
    32	
    33	### La guarda que decía proteger el sexto duplicado no protegía nada
    34	
    35	`tests/test_routing.py:3965` (dentro de `test_adr0034_ac09_write_cli_validates_pins_against_the_
    36	effective_set_not_just_the_constant`, cuyo propósito real es otro: probar que el path de escritura
    37	degrada al set base cuando el probe vivo falla) hace
    38	`self.assertEqual(app._MODEL_PREFERENCE_PROVIDERS, ("openai-codex", "anthropic", "opencode-zen",
    39	"opencode-go"))` — compara contra un literal hardcodeado DENTRO de un `mock.patch` de otro test, no
    40	contra la fuente real. `tests/test_routing.py:3191-3200` (`test_adr0034_ac10_discoverable_providers_
    41	lockstep_guard`) sí es una guarda real, pero solo cubre `DISCOVERABLE_PROVIDERS` — nunca menciona
    42	`_MODEL_PREFERENCE_PROVIDERS`, a pesar de que el comentario de `set_agents_app.py:131-133` afirma
    43	que "AC-10's lockstep test" la cubre también. Es falso: la guarda falta exactamente en el símbolo
    44	que vive en otro archivo.
    45	
    46	### El no-goal de `set_agents_app.py:90-94` es más angosto que su propia redacción
    47	
    48	Texto literal (antes de este paquete):
    49	
    50	```
    51	# The closed, four-provider universe `_PAIR_COMMANDS` already probes
    52	# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
    53	# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
    54	```
    55	
    56	La razón nombrada explícitamente (`AC-06 non-goal`, ADR-0018/019 PKG-2 — nunca dejar que la
    57	clasificación *billing-kind* decida qué proveedores son válidos para pins/preferencias, un eje sin
    58	relación) es real y **no cambia** con este paquete: `_MODEL_PREFERENCE_PROVIDERS` sigue sin leer
    59	`PROVIDER_BILLING_KIND` en ningún punto de su derivación. Lo que la redacción generaliza de más es
    60	"never importing" — leído literalmente, prohíbe cualquier import compartido con el módulo de
    61	catálogo, no solo el de billing-kind. Y `:113-114` afirmaba además que el valor es "byte-identical
    62	to `models_config.DISCOVERABLE_PROVIDERS`" en un sentido que nunca fue literalmente cierto: uno es
    63	`set` (sin orden) y el otro una `tuple` ordenada — los VALORES coinciden, el tipo no.
    64	
    65	### Dónde puede vivir `PROVIDERS` — restricción dura medida
    66	
    67	- `catalog.py` no importa `models_config` en ningún punto (`grep -rn "models_config" routing_core/`
    68	  solo encuentra comentarios).
    69	- `models_config.py` importa `routing_core.catalog` **solo de forma perezosa, adentro de
    70	  funciones** (`detect_subscriptions:257`, `auto_profile:275`), con el docstring explícito
    71	  "Lazy import (no routing_core dependency at module load)".
    72	- Medido: `routing_core/__init__.py` hace `from .service import RoutingService` y `from .store
    73	  import RoutingStore` de forma incondicional — es decir, importar CUALQUIER submódulo de
    74	  `routing_core` (aunque sea uno hoja, sin relación con service/store) ejecuta primero ese
    75	  `__init__.py`, que carga `sqlite3`, `subprocess` (vía `catalog.py`, que `service.py` también
    76	  importa) y el resto de la maquinaria de rutina. Poner `PROVIDERS` dentro de `routing_core`
    77	  (aunque fuera un submódulo nuevo, hoja, sin relación con service/store) y hacer que
    78	  `models_config.py` lo importe a nivel de módulo paga exactamente ese costo en cada `import
    79	  models_config` — el mismo costo que la disciplina de `:250-251` existe para evitar.
    80	
    81	## Decisión
    82	
    83	1. **`PROVIDERS: dict[str, ProviderSpec]` vive en un módulo nuevo, neutro, fuera de
    84	   `routing_core`**: `ai/scripts/provider_registry.py`. Cero imports de `models_config` ni de
    85	   `routing_core` — solo `dataclasses` de la librería estándar. Es importable a nivel de módulo por
    86	   los tres consumidores sin pagar el costo de `routing_core/__init__.py` ni introducir una
    87	   dependencia de `routing_core` hacia el módulo de configuración de aplicación.
    88	   `ProviderSpec` tiene cuatro campos, uno por eje que hoy vive disperso:
    89	   `opencode_auth_key` (map 1, `_OPENCODE_PROVIDER_KEYS`), `opencode_cli_id` (map 2,
    90	   `_OPENCODE_CLI_IDS`), `billing_kind` (`PROVIDER_BILLING_KIND`), `catalog_key` (la clave de
    91	   `[catalog]` que hoy vive inline en `_configured_models`). El orden de inserción del dict es
    92	   **el contrato de orden** para `_MODEL_PREFERENCE_PROVIDERS`:
    93	   `("openai-codex", "anthropic", "opencode-zen", "opencode-go")`, preservado explícitamente
    94	   porque `tuple(dict)` sí respeta orden de inserción, mientras que `DISCOVERABLE_PROVIDERS`
    95	   (un `set`) nunca podría haberlo dado de forma determinística.
    96	2. **`catalog.py` deriva sus cuatro tablas de `PROVIDERS`** (`_OPENCODE_PROVIDER_KEYS`,
    97	   `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, y un nuevo `_CATALOG_KEYS` module-level que
    98	   reemplaza el dict inline de `_configured_models`) — mismos valores, ahora una sola fuente.
    99	   `_PAIR_COMMANDS` no cambia de código: su mitad `opencode` ya derivaba de `_OPENCODE_CLI_IDS`
   100	   (que ahora, transitivamente, deriva de `PROVIDERS`).
   101	3. **`models_config.DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)`** — mismo valor,
   102	   ahora derivado, tipo `set` sin cambios (nada que lo consume espera orden).
   103	4. **`set_agents_app._MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)`** — mismo
   104	   valor, mismo orden, ahora derivado en vez de una segunda tupla hardcodeada.
   105	5. **Guarda real nueva** (`tests/test_routing.py`, nueva función,
   106	   `test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source`): compara
   107	   `set_agents_app._MODEL_PREFERENCE_PROVIDERS` contra `_PAIR_COMMANDS`'s propio set de providers
   108	   (`{provider for _, provider in _PAIR_COMMANDS}`, el mismo universo auditado que la guarda de
   109	   `DISCOVERABLE_PROVIDERS` ya usa) **y** contra `tuple(provider_registry.PROVIDERS)` — la fuente
   110	   real, nunca un literal hardcodeado en otro test. `tests/test_routing.py:3965` no se toca: sigue
   111	   pasando (misma tupla, ahora derivada en vez de literal) como prueba incidental adicional, no
   112	   como la guarda.
   113	6. **Se supersede explícitamente la redacción general de `set_agents_app.py:90-94`** ("defined
   114	   independently here, never importing or referencing") por la más angosta que este ADR confirma:
   115	   `_MODEL_PREFERENCE_PROVIDERS` puede (y ahora sí) compartir fuente con el resto de las tablas de
   116	   proveedores, siempre que esa fuente nunca sea la clasificación billing-kind. El comentario se
   117	   reescribe para decir eso, no lo contrario. La afirmación de `:113-114` ("byte-identical to
   118	   `models_config.DISCOVERABLE_PROVIDERS`") se corrige a "mismos valores, misma fuente, tipos
   119	   distintos por diseño (tupla ordenada vs. set)".
   120	7. **Corrige `ADR-0034` "Rejected alternatives"** (`:124-126`): esa sección afirma que un futuro
   121	   `opencode models --refresh` que liste Copilot "activará la adopción automática sin tocar
   122	   código". Es inexacto incluso hoy: sin este paquete, activar Copilot habría exigido tocar las
   123	   seis tablas manuales medidas arriba. Con este paquete, exige exactamente **una** fila nueva en
   124	   `PROVIDERS` (`provider_registry.py`) — de la que las seis tablas se derivan solas — pero **no
   125	   es cero-touch**: sigue habiendo un ProviderSpec por escribir, con sus cuatro campos medidos a
   126	   mano (nunca inferidos), y `_PAIR_COMMANDS`, `routes.v1.toml` y el resto del universo auditado
   127	   igual exigen su propia validación explícita antes de que Copilot sea *routable* (fuera de
   128	   alcance de este paquete: eso es "probeable", no "routable", ADR-0029). No se edita ADR-0034
   129	   retroactivamente — esta sección deja la corrección por escrito y `docs/adr/README.md` marca la
   130	   relación.
   131	
   132	## Alternativas rechazadas
   133	
   134	- **`PROVIDERS` dentro de `routing_core/catalog.py` (o un submódulo nuevo de `routing_core`)**:
   135	  rechazado por la medición de arriba — cualquier import de un submódulo de `routing_core` ejecuta
   136	  `routing_core/__init__.py`, que carga `service.py`/`store.py` (sqlite3, subprocess) — exactamente
   137	  lo que `models_config.py:250-251`/`:275` existen para evitar en su import a nivel de módulo. Esto
   138	  no es una preferencia estética: es la razón medida y escrita que la disciplina ya documentaba
   139	  antes de este paquete.
   140	- **`PROVIDERS` dentro de `models_config.py`**: rechazado porque `routing_core` se declara
   141	  explícitamente "no CLI imports" (`routing_core/__init__.py:1`) y `models_config.py` es, en la
   142	  práctica, un módulo de aplicación (lee `models.toml` de disco, `die()` en error) — hacer que
   143	  `catalog.py` importe `models_config` invierte la dirección de dependencia que el propio paquete
   144	  declara, y no ganaría nada frente a un módulo neutro.
   145	- **Derivar `_MODEL_PREFERENCE_PROVIDERS` de `models_config.DISCOVERABLE_PROVIDERS`** en vez de la
   146	  fuente común: rechazado porque `DISCOVERABLE_PROVIDERS` es un `set` — derivar una tupla ordenada
   147	  de un `set` produce orden no determinístico entre corridas, y el orden de hoy
   148	  (`openai-codex, anthropic, opencode-zen, opencode-go`) llega a superficie de usuario (mensajes de
   149	  validación de pins).
   150	- **Editar `_PAIR_COMMANDS` para que sus dos pares no-opencode también deriven "más" de
   151	  `PROVIDERS`**: fuera de alcance — ya deriva lo que tenía que derivar (la mitad opencode, desde
   152	  012/F-05) y no es un duplicado manual; tocar su forma es el tipo de refactor oportunista que este
   153	  paquete evita (`catalog.py` no se reorganiza más allá de las cuatro tablas nombradas en AC-01).
   154	
   155	## Consecuencias
   156	
   157	- Agregar un proveedor nuevo pasa de tocar seis tablas manuales a agregar una fila `ProviderSpec` a
   158	  `PROVIDERS` — las cuatro tablas de `catalog.py`, `DISCOVERABLE_PROVIDERS` y
   159	  `_MODEL_PREFERENCE_PROVIDERS` se actualizan solas. `_PAIR_COMMANDS` sigue necesitando sus propios
   160	  argv auditados a mano para pares nuevos runtime×provider (nunca inferidos — fuera del alcance de
   161	  "derivar", que es sobre identidad de proveedor, no sobre comandos de probe).
   162	- Ningún comportamiento cambia: los siete valores derivados son byte-idénticos a los literales
   163	  previos (test de caracterización, PKG-1 AC-02).
   164	- `set_agents_app.py:90-94`/`:113-114` quedan reescritos para no contradecir al código.
   165	- `ADR-0034` no se edita; esta sección corrige su afirmación con la medición.
   166	
   167	## Evidencia
   168	
   169	`docs/specs/022-disponibilidad-real/evidence/P1-implementer.md`.
   112	    vuelve el arranque dependiente de un probe.
   113	
   114	## Alcance NO tocado por esta ADR
   115	
   116	`enabled_providers`, `routes.v1.toml` (6 filas curadas) y `ROUTING_PROVIDERS` no se amplían — la vía
   117	curada sigue cerrada. Billing/costo, `--route-doctor`, el panel de consola y el wizard son PKG-2
   118	(ADR-0035), no se adelantan acá. No se persisten listados descubiertos en `models.toml` (solo
   119	política y exclusiones, sin cambios de esa disciplina).
   120	
   121	## Rejected alternatives
   122	
   123	- **Hardcodear un CLI id para `github-copilot`**: violaría la regla fail-closed del proyecto —
   124	  "nunca inventar heurística sobre un provider no verificado". Un futuro `opencode models --refresh`
   125	  que sí lo liste activará la adopción automáticamente sin tocar código, porque `resolve_discovered_
   126	  providers` deriva de inventario real, no de una lista mantenida a mano.
   127	- **Agregar un segundo provider de catálogo `openai`** para separar la credencial OAuth de ChatGPT
   128	  del `openai-codex` existente: es la MISMA credencial y el MISMO CLI id ya cubierto por
   129	  `("opencode","openai-codex")` — un segundo provider sería un duplicado detectado por
   130	  `_check_family_collisions`/`SUBSCRIPTION_BY_PREFIX`, sin beneficio real.
   131	- **Promover tier por sufijo también en `"auto"`** (dejar `_FRONTIER_HINTS` vivo): un provider podría
   132	  auto-otorgarse trabajo de auditoría/planificación crítico nombrando su modelo `algo-max`; eliminado
   133	  por ser exactamente la inversión de "la inferencia solo puede quitar, nunca otorgar" (ADR-0029 d.3).
   134	
   135	## Evidencia
   136	
  3985	        # again -- proving the short-circuit is a real conditional, not a permanent skip.
  3986	        with mock.patch.object(app.models_config, "DISCOVERABLE_PROVIDERS",
  3987	                                frozenset({*app._MODEL_PREFERENCE_PROVIDERS, "a-future-only-provider"})):
  3988	            with mock.patch("routing_core.catalog.probe_inventory", return_value={}) as probe:
  3989	                with mock.patch("routing_core.catalog.resolve_discovered_providers", return_value=()):
  3990	                    app._effective_preference_providers()
  3991	            probe.assert_called_once()
  3992	
  3993	    def test_adr0034_m1_github_copilot_never_gets_an_audited_pair_even_authenticated(self):
  3994	        # M-1 (ADR-0034): github-copilot is authenticated in the live measurement but
  3995	        # unlistable -- no pair exists for it in `_PAIR_COMMANDS` at all, so "auto" can
  3996	        # never adopt it no matter what a (hypothetical, malformed) inventory claims.
  3997	        self.assertNotIn(("opencode", "github-copilot"), routing_catalog._PAIR_COMMANDS)
  3998	        config = json.loads(json.dumps(self.config))
  3999	        config["routing"]["discovered_providers"] = "auto"
  4000	        # Even if something upstream ever produced an inventory entry for it (never
  4001	        # possible through the real probe, which only iterates `_PAIR_COMMANDS`), the
  4002	        # derivation intersects against the audited provider set and drops it.
  4003	        bogus_inventory = {("opencode", "github-copilot"): {"some-copilot-model"}}
  4004	        self.assertEqual(routing_catalog.resolve_discovered_providers(config, bogus_inventory), ())
  4005	
  4006	    # ------------------------------------------------- ADR-0042 (022 PKG-1): single provider
  4007	    # registry -- `provider_registry.PROVIDERS` is now the one source every provider-keyed
  4008	    # table below derives from, replacing six independently hand-edited literal copies.
  4009	
  4010	    def test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals(self):
  4011	        # AC-02 characterization: every literal pasted here is the EXACT pre-ADR-0042 value
  4012	        # (copied from the removed literals, not imported from provider_registry or derived
  4013	        # in this test) -- proves the refactor changed plumbing, never values. Order matters
  4014	        # only for the tuple (AC-01's provider-registry insertion order contract); the dicts
  4015	        # and the set compare by value, so their literal order below is not significant.
  4016	        self.assertEqual(list(provider_registry.PROVIDERS),
  4017	                         ["openai-codex", "anthropic", "opencode-zen", "opencode-go"])
  4018	        self.assertEqual(routing_catalog._OPENCODE_PROVIDER_KEYS, {
  4019	            "openai-codex": "openai", "anthropic": "anthropic",
  4020	            "opencode-zen": "opencode zen", "opencode-go": "opencode go",
  4021	        })
  4022	        self.assertEqual(routing_catalog._OPENCODE_CLI_IDS, {
  4023	            "openai-codex": "openai", "anthropic": "anthropic",
  4024	            "opencode-zen": "opencode", "opencode-go": "opencode-go",
  4025	        })
  4026	        self.assertEqual(routing_catalog.PROVIDER_BILLING_KIND, {
  4027	            "openai-codex": "subscription", "anthropic": "subscription",
  4028	            "opencode-go": "subscription", "opencode-zen": "metered",
  4029	        })
  4030	        # AC-01's fifth symbol: the key map that used to live INLINE inside
  4031	        # `_configured_models` (a local dict literal, unreachable from outside the function)
  4032	        # is now the module-level `_CATALOG_KEYS`, itself derived from `PROVIDERS`.
  4033	        self.assertEqual(routing_catalog._CATALOG_KEYS, {
  4034	            "openai-codex": "codex", "anthropic": "claude",
  4035	            "opencode-zen": "opencode_zen", "opencode-go": "opencode_go",
  4036	        })
  4037	        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS,
  4038	                         {"openai-codex", "anthropic", "opencode-zen", "opencode-go"})
  4039	        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
  4040	                         ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
  4041	
  4042	    def test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source(self):
  4043	        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
  4044	        # lived at test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_
  4045	        # not_just_the_constant (below, :3965 pre-refactor line count) -- inside a
  4046	        # `mock.patch` block whose real purpose is testing the live-probe degrade path, and
  4047	        # it compared against ANOTHER hardcoded literal, never against the audited source.
  4048	        # That test is untouched by this package (still passes, proven by the full-suite
  4049	        # gate) and still is not the guard -- THIS is the guard: two independent
  4050	        # cross-checks against the real source, neither of them a second copy-pasted tuple.
  4051	        #
  4052	        # Cross-check 1: same pattern as test_adr0034_ac10_discoverable_providers_lockstep_
  4053	        # guard above (which only ever covered DISCOVERABLE_PROVIDERS, never this tuple,
  4054	        # despite set_agents_app.py's own comment claiming otherwise pre-ADR-0042) --
  4055	        # membership against `_PAIR_COMMANDS`'s own audited provider set.
  4056	        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
  4057	        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
  4058	        # Cross-check 2: against the ordered registry directly -- this is what makes a
  4059	        # FUTURE regression (someone re-introducing a hand-maintained tuple in
  4060	        # set_agents_app.py instead of deriving it) fail loudly the moment `PROVIDERS`
  4061	        # changes without a matching edit here, rather than silently agreeing with a second,
  4062	        # equally-stale literal (the exact defect this package exists to close).
  4063	        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
  4064	
  4065	    # --------------------------------------------- 014-model-preference-policy (AC-01..09)
  4066	
  4067	    _MP_TIERED_ROLES = frozenset({"debugger", "delta-reviewer", "finding-verifier",
  4068	                                   "implementer", "package-reviewer", "security-auditor"})
  4069	    _MP_LIVE_ROLES = _MP_TIERED_ROLES  # every tiered role is doctrine-invoked (AC-01 honest scope)
  4070	    _MP_DOCTRINE_FILES = (
  4071	        ROOT / "Global/_canonical/agents/orchestrator.md",
  4072	        ROOT / "Global/claude-code/agents/orchestrator.md",
  4073	        ROOT / "Global/opencode/agents/orchestrator.md",
  4074	        ROOT / "Global/codex/agents/orchestrator.toml",
  4075	    )
  4076	    _MP_TIERED_SENTENCE_RE = re.compile(
  4077	        r"`implementer`,\s*`debugger`,\s*`package-reviewer`,\s*`delta-reviewer`,\s*`security-auditor`,\s*and\s*"
  4078	        r"`finding-verifier`\s*are\s*\*\*tiered roles\*\*"
  4079	    )
  4080	

exec
/bin/bash -lc "sed -n '55,100p' docs/specs/022-disponibilidad-real/spec.md; sed -n '100,150p' docs/specs/022-disponibilidad-real/spec.md; nl -ba ai/scripts/set_agents_app.py | sed -n '84,128p'; nl -ba docs/adr/0042-provider-registry-single-source.md | sed -n '1,169p'; rg -n -C 2 'configured_models|_CATALOG_KEYS|_MODEL_PREFERENCE_PROVIDERS|PROVIDERS' tests ai/scripts --glob '*.py'; rg -n 'openai-codex.*anthropic.*opencode-zen.*opencode-go|opencode-go.*opencode-zen.*anthropic.*openai-codex' ai/scripts --glob '*.py'; git diff --no-ext-diff --check; git diff --no-ext-diff -- ai/scripts/models_config.py ai/scripts/routing_core/catalog.py ai/scripts/set_agents_app.py tests/test_routing.py docs/adr/README.md; git diff --no-index -- /dev/null ai/scripts/provider_registry.py || true; git diff --no-index -- /dev/null docs/adr/0042-provider-registry-single-source.md || true" in /home/federico/SET-AGENTES
 succeeded in 0ms:
## Paquetes

Cinco, **serializados**: A2 y A3 tocan el mismo archivo y paralelizarlos garantiza conflicto.

### PKG-1 — `registro-de-proveedores`
- **AC-01**: un único `PROVIDERS: dict[str, ProviderSpec]` del que se **derivan**
  `_PAIR_COMMANDS`, `_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`,
  `DISCOVERABLE_PROVIDERS`, el key-map de `_configured_models` y **`_MODEL_PREFERENCE_PROVIDERS`
  (`set_agents_app.py:94`)** — el sexto duplicado, que la primera versión de esta spec no
  nombraba (F4 del challenge). Sumar un proveedor pasa de **seis** entradas en lockstep manual
  a **una fila**.
  **Ojo con su test**: `tests/test_routing.py:3965` dice proteger ese literal pero lo compara
  contra **otro literal hardcodeado idéntico**, no contra `DISCOVERABLE_PROVIDERS`. Y el
  comentario de `set_agents_app.py:131-133` afirma lo contrario. Es exactamente el defecto que
  este paquete existe para eliminar, disfrazado de guarda: el test tiene que comparar contra la
  **fuente real**.
- **AC-02**: test de caracterización — las tablas derivadas son **byte-idénticas** a los
  literales de hoy. Este paquete no cambia comportamiento.
- **AC-03**: ADR-0042 corrige la afirmación de `ADR-0034:124-126` con la medición.

### PKG-2 — `techo-catalogo-tri-estado`
- **AC-04**: el tri-estado aplica **solo a `opencode_zen`, `opencode_go` y a proveedores
  futuros sin key dedicada** — decisión de Federico (2026-08-12), no re-litigable.
  `[catalog].claude` y `[catalog].codex` **siguen siendo listas obligatorias no vacías**, como
  hoy. Razón medida por el challenge (F1): `models_config.py:130-136` las exige con `die`,
  `load_roles` (`:352-357`) las indexa sin fallback, y son las **únicas con filas curadas** en
  `routes.v1.toml` — si pasaran a auto, cada fila curada existente dispararía el
  `CATALOG_CEILING_REQUIRED` de AC-06. `[catalog].codex_effort` queda **explícitamente fuera**:
  es una lista de efforts, no un techo de proveedor, y meterla en la redacción genérica
  "`<key>`" fue un error de la primera versión.
  Los tres estados: lista = techo curado, `[]` = veto, **ausente = auto**.
  **Del precedente de `[subscriptions]` (`models_config.py:361-382`) se toma la FORMA, no el
  manejo de error** (F1): allí "ausente" degrada a `WARN` y sigue, porque es validación de
  build; acá alimenta el filtrado en vivo del snapshot de ruteo, y AC-06 exige que el caso malo
  falle **fuerte y nombrado**. Importar la mansedumbre de subscriptions contradiría a AC-06. `_configured_models` se reemplaza por
  `resolve_ceiling(config, provider) -> ("curated", set) | ("auto", None) | ("veto", set())`,
  consumido por los **tres** sitios que hoy divergen: `_probe_pairs:467-469`,
  `_read_probe_cache:409` (que hoy re-intersecta y en modo auto dejaría el caché siempre vacío)
  y `build_snapshot:632-633,647`.
- **AC-05**: cuatro capas impiden que entre cualquier cosa, y cada una con su test: el par debe
  estar **auditado**; lo auto **nunca** entra al snapshot curado (sólo por la vía sintetizada,
  con `MODEL_METADATA_INFERRED`); billing desconocido ⇒ rank caro; y cap determinístico por
  proveedor + `[catalog].exclude` extendido a `provider:*`.
- **AC-06**: una fila curada de `routes.v1.toml` que apunte a un proveedor en auto/veto falla
  **fuerte y nombrada** (`CATALOG_CEILING_REQUIRED`), no con el genérico `CATALOG_INVALID`.


### PKG-3 — `liveness-real`
- **AC-07**: el de-auth se cierra **en la clave de caché, no con más subprocesos**: firma de
  credencial por runtime, toda `stat`/lectura local — codex (`~/.codex/auth.json`), claude-code
  (`~/.claude/.credentials.json`), pi (`pi_auth_provider_keys()` + `PI_PINNED_VERSION`),
  opencode (lo de hoy + mtime de los binarios `codex` y `claude`).
- **AC-08**: **las dos propiedades que hacen o rompen esto**, cada una con su test:
  *refresh de token* (cambia sólo token/expiry) ⇒ la firma **no** cambia y el caché sirve; y
  *logout / credencial borrada* ⇒ la firma **sí** cambia ⇒ probe fresco. Sin la primera,
  Claude Code renueva OAuth y probeás en cada decisión, hasta 60 s en pi
  (`PI_PROBE_MIN_TIMEOUT_SECONDS`).
- **AC-09**: la firma se compone de **presencia e identidad de credencial, nunca material y
  nunca campos que rotan**, hasheada, jamás logueada ni en el envelope — misma disciplina que
  `pi_auth_provider_keys`. Bump de `_CACHE_SCHEMA_VERSION` con su test: hoy no existe ninguno.
  **Límite estructural de claude-code, aceptado por Federico y a documentar en ADR-0043** (F2):
  `~/.claude/.credentials.json` **no tiene ningún campo de identidad de cuenta** — codex sí
  (`tokens.account_id`), y los únicos campos no-rotantes de claude (`scopes`,
  `subscriptionType`, `rateLimitTier`) no identifican una cuenta. Así que para claude-code la
  firma detecta **presencia y ausencia** (logout, credencial borrada, archivo ausente) pero
  **no** un cambio de una cuenta a otra del mismo plan sin pasar por logout. Detectarlo exigiría
  hashear el `refreshToken`, que esta feature **no** hace. El límite se declara; no se disimula.
  **Supuesto a validar temprano en este paquete, no de memoria** (F2): que `refreshToken`,
  `scopes` y `subscriptionType` efectivamente no roten en un refresh normal. Captura A/B del
  archivo antes y después de un refresh real; si rotan, la firma de claude-code hay que
  rediseñarla.
- **AC-10**: **una sola caché**, en la raíz del store (`store.py:326-334`), que ya tiene la
  disciplina de directorio privado y es la que usa la vía de decisión. Migran
  `set_agents_app.py:144,497,843,3133` y `models_config.py:258-259,277-278`. La legada se poda
  **con las mismas validaciones de seguridad** que `_write_probe_cache`.

### PKG-4 — `proveedores-del-usuario`
- **AC-11**: `~/.local/state/set-agentes/providers.toml` (precedente: `model-preference.toml`),
  con `origin` por entrada: `harness`, `discovered`, `user`. Los tres orígenes dejan de estar
  aplanados.
- **AC-12**: comandos `--provider-list|add|remove|verify`, sin tocar JSON a mano. El wizard de
  `setup_models.py` no se toca: remapea modelos de `models.toml`, es otra cosa.
- **AC-13**: el bloque `provider` de `opencode.json` pasa a **renderizarse** desde el registro,
  no a estar hardcodeado en `Global/_shared/opencode.json:5-23`. **Es lo que hace que quitar
  funcione.**
- **AC-14**: la poda por manifiesto se extiende de archivos a **subárboles JSON**
  (`opencode.json#/provider/ollama`), y **jamás** toca una clave que el harness no puso. Test
  obligatorio: un provider agregado a mano por el usuario sobrevive intacto a un install que
  poda otro.
- **AC-15**: siembra migratoria desde el `opencode.json` vivo que registra **todo** lo que haya
  bajo `provider.*` — decisión de Federico (F3): lo del harness como `origin=harness-legacy` y
  **cualquier provider que el usuario haya agregado a mano como `origin=user`**. Desde el primer
  arranque se puede listar, verificar y quitar desde `set-agents`, que es el pedido literal.
  La alternativa —registrar solo lo que el harness reconoce— dejaba invisibles los providers
  propios de quien ya tenía el harness instalado, que es el caso más probable.
  **A nadie le desaparece nada**: el registro declara qué hay y de dónde vino, nunca borra.

    84	# preference table) or `app_config()` (its silent parse-failure-as-`{}` swallow). This
    85	# file gets its OWN dedicated, atomic, fail-closed round-trip: `atomic_write` (already
    86	# established for the Claude settings JSON writer above) for the temp-file+`os.replace`
    87	# discipline, and a small, purpose-built two-table TOML serializer for a schema this
    88	# contract fully owns and closes (`[preference]`, `[role_override]`) -- not a
    89	# general-purpose nested-TOML writer (round-2 finding R2-F-04).
    90	MODEL_PREFERENCE_PATH = STATE_DIR / "model-preference.toml"
    91	
    92	# The closed, four-provider universe `_PAIR_COMMANDS` already probes
    93	# (`routing_core/catalog.py:165-172`). ADR-0042 (022 PKG-1) supersedes this table's own
    94	# ORIGINAL "defined independently here, never importing" framing -- that framing was wider
    95	# than the reason it named: the AC-06 non-goal it cites (014-model-preference-policy,
    96	# test_ac06_no_provider_billing_kind_reference_in_new_code) is specifically about never
    97	# letting the catalog module's own billing-kind classification table decide provider
    98	# membership here, and that narrower non-goal still holds -- this tuple's derivation below
    99	# never reads that table (nor even names it -- see the test above, unchanged by this
   100	# package). What changed is provider IDENTITY: this tuple now derives from the single
   101	# `provider_registry.PROVIDERS` registry every provider-keyed table in the harness shares,
   102	# instead of being a second hand-maintained copy of the same four provider ids. Order is a
   103	# real contract (surfaces in pin/preference validation errors) --
   104	# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
   105	# because that sibling table is an unordered `set`.
   106	_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
   107	_MODEL_PREFERENCE_CLASSES = ("decision", "grunt", "build")
   108	# AC-02's inertness note: the six roles with real, live effect today (`### Honest scope`,
   109	# spec) -- every other in-scope role, and the whole `decision` class, is genuinely inert.
   110	_MODEL_PREFERENCE_LIVE_ROLES = frozenset({
   111	    "delta-reviewer", "finding-verifier", "package-reviewer", "security-auditor",
   112	    "implementer", "debugger",
   113	})
   114	
   115	
   116	class ModelPreferenceError(ValueError):
   117	    """AC-02: fail-closed error for the sibling `model-preference.toml` file/CLI -- a
   118	    malformed value never degrades to a silent default (round-1 F-07, round-3 R3-F-04)."""
   119	
   120	
   121	def _model_preference_die(message):
   122	    raise ModelPreferenceError(message)
   123	
   124	
   125	def _effective_preference_providers():
   126	    """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
   127	    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
   128	    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
     1	# ADR-0042 — Un único `PROVIDERS` del que se derivan las siete tablas de proveedores
     2	
     3	- Estado: Accepted (2026-08-12). Feature 022-disponibilidad-real, PKG-1 (`P1-registro-de-proveedores`).
     4	  Corrige en parte una afirmación de ADR-0034 (Rejected alternatives) — no lo supersede: ADR-0034 sigue
     5	  vigente en todo lo demás.
     6	
     7	## Contexto
     8	
     9	Medición en vivo sobre el estado actual del repo (antes de este paquete), `grep -rn` de cada símbolo:
    10	
    11	| Símbolo | Ubicación | Qué codifica |
    12	|---|---|---|
    13	| `_OPENCODE_PROVIDER_KEYS` | `routing_core/catalog.py:116-117` | provider → texto de credencial que `opencode auth list --pure` imprime |
    14	| `_OPENCODE_CLI_IDS` | `catalog.py:125-126` | provider → argumento CLI de `opencode models <id>` |
    15	| `_PAIR_COMMANDS` | `catalog.py:154-161` | el universo auditado runtime×provider; su mitad `opencode` **ya** deriva de `_OPENCODE_CLI_IDS` (comprehension, no literal) desde 012/F-05 |
    16	| `PROVIDER_BILLING_KIND` | `catalog.py:172-173` | provider → `subscription`\|`metered` |
    17	| key map de `_configured_models` | `catalog.py:215-216` (dentro del cuerpo de la función) | provider → clave de `[catalog]` en `models.toml` |
    18	| `DISCOVERABLE_PROVIDERS` | `models_config.py:41` | el `set` de providers elegibles para `"auto"` (ADR-0034) |
    19	| `_MODEL_PREFERENCE_PROVIDERS` | `set_agents_app.py:94` | la `tuple` **ordenada** que valida pins/preferencias de modelo (ADR-0018/ADR-0034 AC-09) |
    20	
    21	Siete símbolos, medidos. **Seis** de ellos son duplicados *manuales* — hoy, agregar un proveedor
    22	exige tocar las seis tablas a mano y nada las liga entre sí salvo la disciplina humana. El séptimo,
    23	`_PAIR_COMMANDS`, ya era una excepción parcial: su mitad `opencode` deriva de `_OPENCODE_CLI_IDS`
    24	desde el repair F-05 de la feature 012 (`catalog.py:149-152`), así que no suma un duplicado nuevo —
    25	pero sigue siendo uno de los siete lugares que un proveedor nuevo toca, porque sus dos pares no-opencode
    26	(`codex`/`openai-codex`, `claude-code`/`anthropic`) usan los ids de provider como claves literales de
    27	dict. Esto resuelve la discrepancia de conteo entre la spec (que llama "seis" en la narrativa de
    28	`docs/specs/022-disponibilidad-real/spec.md` — "de seis entradas en lockstep manual a una fila" — y
    29	sin embargo nombra los siete en la tabla del context pack): **"seis" cuenta los duplicados
    30	verdaderamente manuales; "siete" cuenta todos los símbolos que este paquete mide y dejan de poder
    31	divergir entre sí**, uno de los cuales (`_PAIR_COMMANDS`) ya no era manual antes de este ADR.
    32	
    33	### La guarda que decía proteger el sexto duplicado no protegía nada
    34	
    35	`tests/test_routing.py:3965` (dentro de `test_adr0034_ac09_write_cli_validates_pins_against_the_
    36	effective_set_not_just_the_constant`, cuyo propósito real es otro: probar que el path de escritura
    37	degrada al set base cuando el probe vivo falla) hace
    38	`self.assertEqual(app._MODEL_PREFERENCE_PROVIDERS, ("openai-codex", "anthropic", "opencode-zen",
    39	"opencode-go"))` — compara contra un literal hardcodeado DENTRO de un `mock.patch` de otro test, no
    40	contra la fuente real. `tests/test_routing.py:3191-3200` (`test_adr0034_ac10_discoverable_providers_
    41	lockstep_guard`) sí es una guarda real, pero solo cubre `DISCOVERABLE_PROVIDERS` — nunca menciona
    42	`_MODEL_PREFERENCE_PROVIDERS`, a pesar de que el comentario de `set_agents_app.py:131-133` afirma
    43	que "AC-10's lockstep test" la cubre también. Es falso: la guarda falta exactamente en el símbolo
    44	que vive en otro archivo.
    45	
    46	### El no-goal de `set_agents_app.py:90-94` es más angosto que su propia redacción
    47	
    48	Texto literal (antes de este paquete):
    49	
    50	```
    51	# The closed, four-provider universe `_PAIR_COMMANDS` already probes
    52	# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
    53	# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
    54	```
    55	
    56	La razón nombrada explícitamente (`AC-06 non-goal`, ADR-0018/019 PKG-2 — nunca dejar que la
    57	clasificación *billing-kind* decida qué proveedores son válidos para pins/preferencias, un eje sin
    58	relación) es real y **no cambia** con este paquete: `_MODEL_PREFERENCE_PROVIDERS` sigue sin leer
    59	`PROVIDER_BILLING_KIND` en ningún punto de su derivación. Lo que la redacción generaliza de más es
    60	"never importing" — leído literalmente, prohíbe cualquier import compartido con el módulo de
    61	catálogo, no solo el de billing-kind. Y `:113-114` afirmaba además que el valor es "byte-identical
    62	to `models_config.DISCOVERABLE_PROVIDERS`" en un sentido que nunca fue literalmente cierto: uno es
    63	`set` (sin orden) y el otro una `tuple` ordenada — los VALORES coinciden, el tipo no.
    64	
    65	### Dónde puede vivir `PROVIDERS` — restricción dura medida
    66	
    67	- `catalog.py` no importa `models_config` en ningún punto (`grep -rn "models_config" routing_core/`
    68	  solo encuentra comentarios).
    69	- `models_config.py` importa `routing_core.catalog` **solo de forma perezosa, adentro de
    70	  funciones** (`detect_subscriptions:257`, `auto_profile:275`), con el docstring explícito
    71	  "Lazy import (no routing_core dependency at module load)".
    72	- Medido: `routing_core/__init__.py` hace `from .service import RoutingService` y `from .store
    73	  import RoutingStore` de forma incondicional — es decir, importar CUALQUIER submódulo de
    74	  `routing_core` (aunque sea uno hoja, sin relación con service/store) ejecuta primero ese
    75	  `__init__.py`, que carga `sqlite3`, `subprocess` (vía `catalog.py`, que `service.py` también
    76	  importa) y el resto de la maquinaria de rutina. Poner `PROVIDERS` dentro de `routing_core`
    77	  (aunque fuera un submódulo nuevo, hoja, sin relación con service/store) y hacer que
    78	  `models_config.py` lo importe a nivel de módulo paga exactamente ese costo en cada `import
    79	  models_config` — el mismo costo que la disciplina de `:250-251` existe para evitar.
    80	
    81	## Decisión
    82	
    83	1. **`PROVIDERS: dict[str, ProviderSpec]` vive en un módulo nuevo, neutro, fuera de
    84	   `routing_core`**: `ai/scripts/provider_registry.py`. Cero imports de `models_config` ni de
    85	   `routing_core` — solo `dataclasses` de la librería estándar. Es importable a nivel de módulo por
    86	   los tres consumidores sin pagar el costo de `routing_core/__init__.py` ni introducir una
    87	   dependencia de `routing_core` hacia el módulo de configuración de aplicación.
    88	   `ProviderSpec` tiene cuatro campos, uno por eje que hoy vive disperso:
    89	   `opencode_auth_key` (map 1, `_OPENCODE_PROVIDER_KEYS`), `opencode_cli_id` (map 2,
    90	   `_OPENCODE_CLI_IDS`), `billing_kind` (`PROVIDER_BILLING_KIND`), `catalog_key` (la clave de
    91	   `[catalog]` que hoy vive inline en `_configured_models`). El orden de inserción del dict es
    92	   **el contrato de orden** para `_MODEL_PREFERENCE_PROVIDERS`:
    93	   `("openai-codex", "anthropic", "opencode-zen", "opencode-go")`, preservado explícitamente
    94	   porque `tuple(dict)` sí respeta orden de inserción, mientras que `DISCOVERABLE_PROVIDERS`
    95	   (un `set`) nunca podría haberlo dado de forma determinística.
    96	2. **`catalog.py` deriva sus cuatro tablas de `PROVIDERS`** (`_OPENCODE_PROVIDER_KEYS`,
    97	   `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, y un nuevo `_CATALOG_KEYS` module-level que
    98	   reemplaza el dict inline de `_configured_models`) — mismos valores, ahora una sola fuente.
    99	   `_PAIR_COMMANDS` no cambia de código: su mitad `opencode` ya derivaba de `_OPENCODE_CLI_IDS`
   100	   (que ahora, transitivamente, deriva de `PROVIDERS`).
   101	3. **`models_config.DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)`** — mismo valor,
   102	   ahora derivado, tipo `set` sin cambios (nada que lo consume espera orden).
   103	4. **`set_agents_app._MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)`** — mismo
   104	   valor, mismo orden, ahora derivado en vez de una segunda tupla hardcodeada.
   105	5. **Guarda real nueva** (`tests/test_routing.py`, nueva función,
   106	   `test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source`): compara
   107	   `set_agents_app._MODEL_PREFERENCE_PROVIDERS` contra `_PAIR_COMMANDS`'s propio set de providers
   108	   (`{provider for _, provider in _PAIR_COMMANDS}`, el mismo universo auditado que la guarda de
   109	   `DISCOVERABLE_PROVIDERS` ya usa) **y** contra `tuple(provider_registry.PROVIDERS)` — la fuente
   110	   real, nunca un literal hardcodeado en otro test. `tests/test_routing.py:3965` no se toca: sigue
   111	   pasando (misma tupla, ahora derivada en vez de literal) como prueba incidental adicional, no
   112	   como la guarda.
   113	6. **Se supersede explícitamente la redacción general de `set_agents_app.py:90-94`** ("defined
   114	   independently here, never importing or referencing") por la más angosta que este ADR confirma:
   115	   `_MODEL_PREFERENCE_PROVIDERS` puede (y ahora sí) compartir fuente con el resto de las tablas de
   116	   proveedores, siempre que esa fuente nunca sea la clasificación billing-kind. El comentario se
   117	   reescribe para decir eso, no lo contrario. La afirmación de `:113-114` ("byte-identical to
   118	   `models_config.DISCOVERABLE_PROVIDERS`") se corrige a "mismos valores, misma fuente, tipos
   119	   distintos por diseño (tupla ordenada vs. set)".
   120	7. **Corrige `ADR-0034` "Rejected alternatives"** (`:124-126`): esa sección afirma que un futuro
   121	   `opencode models --refresh` que liste Copilot "activará la adopción automática sin tocar
   122	   código". Es inexacto incluso hoy: sin este paquete, activar Copilot habría exigido tocar las
   123	   seis tablas manuales medidas arriba. Con este paquete, exige exactamente **una** fila nueva en
   124	   `PROVIDERS` (`provider_registry.py`) — de la que las seis tablas se derivan solas — pero **no
   125	   es cero-touch**: sigue habiendo un ProviderSpec por escribir, con sus cuatro campos medidos a
   126	   mano (nunca inferidos), y `_PAIR_COMMANDS`, `routes.v1.toml` y el resto del universo auditado
   127	   igual exigen su propia validación explícita antes de que Copilot sea *routable* (fuera de
   128	   alcance de este paquete: eso es "probeable", no "routable", ADR-0029). No se edita ADR-0034
   129	   retroactivamente — esta sección deja la corrección por escrito y `docs/adr/README.md` marca la
   130	   relación.
   131	
   132	## Alternativas rechazadas
   133	
   134	- **`PROVIDERS` dentro de `routing_core/catalog.py` (o un submódulo nuevo de `routing_core`)**:
   135	  rechazado por la medición de arriba — cualquier import de un submódulo de `routing_core` ejecuta
   136	  `routing_core/__init__.py`, que carga `service.py`/`store.py` (sqlite3, subprocess) — exactamente
   137	  lo que `models_config.py:250-251`/`:275` existen para evitar en su import a nivel de módulo. Esto
   138	  no es una preferencia estética: es la razón medida y escrita que la disciplina ya documentaba
   139	  antes de este paquete.
   140	- **`PROVIDERS` dentro de `models_config.py`**: rechazado porque `routing_core` se declara
   141	  explícitamente "no CLI imports" (`routing_core/__init__.py:1`) y `models_config.py` es, en la
   142	  práctica, un módulo de aplicación (lee `models.toml` de disco, `die()` en error) — hacer que
   143	  `catalog.py` importe `models_config` invierte la dirección de dependencia que el propio paquete
   144	  declara, y no ganaría nada frente a un módulo neutro.
   145	- **Derivar `_MODEL_PREFERENCE_PROVIDERS` de `models_config.DISCOVERABLE_PROVIDERS`** en vez de la
   146	  fuente común: rechazado porque `DISCOVERABLE_PROVIDERS` es un `set` — derivar una tupla ordenada
   147	  de un `set` produce orden no determinístico entre corridas, y el orden de hoy
   148	  (`openai-codex, anthropic, opencode-zen, opencode-go`) llega a superficie de usuario (mensajes de
   149	  validación de pins).
   150	- **Editar `_PAIR_COMMANDS` para que sus dos pares no-opencode también deriven "más" de
   151	  `PROVIDERS`**: fuera de alcance — ya deriva lo que tenía que derivar (la mitad opencode, desde
   152	  012/F-05) y no es un duplicado manual; tocar su forma es el tipo de refactor oportunista que este
   153	  paquete evita (`catalog.py` no se reorganiza más allá de las cuatro tablas nombradas en AC-01).
   154	
   155	## Consecuencias
   156	
   157	- Agregar un proveedor nuevo pasa de tocar seis tablas manuales a agregar una fila `ProviderSpec` a
   158	  `PROVIDERS` — las cuatro tablas de `catalog.py`, `DISCOVERABLE_PROVIDERS` y
   159	  `_MODEL_PREFERENCE_PROVIDERS` se actualizan solas. `_PAIR_COMMANDS` sigue necesitando sus propios
   160	  argv auditados a mano para pares nuevos runtime×provider (nunca inferidos — fuera del alcance de
   161	  "derivar", que es sobre identidad de proveedor, no sobre comandos de probe).
   162	- Ningún comportamiento cambia: los siete valores derivados son byte-idénticos a los literales
   163	  previos (test de caracterización, PKG-1 AC-02).
   164	- `set_agents_app.py:90-94`/`:113-114` quedan reescritos para no contradecir al código.
   165	- `ADR-0034` no se edita; esta sección corrige su afirmación con la medición.
   166	
   167	## Evidencia
   168	
   169	`docs/specs/022-disponibilidad-real/evidence/P1-implementer.md`.
ai/scripts/routing_core/catalog.py-15-# derives from. Deliberately a plain top-level import, not a relative `.` one -- the module
ai/scripts/routing_core/catalog.py-16-# lives OUTSIDE `routing_core` on purpose (see its own docstring and ADR-0042's "Dónde puede
ai/scripts/routing_core/catalog.py:17:# vivir PROVIDERS" section): it has zero dependencies of its own, so importing it here costs
ai/scripts/routing_core/catalog.py-18-# nothing this package's other two consumers (`models_config.py`, `set_agents_app.py`) don't
ai/scripts/routing_core/catalog.py-19-# already pay to import it too.
ai/scripts/routing_core/catalog.py:20:from provider_registry import PROVIDERS
ai/scripts/routing_core/catalog.py-21-
ai/scripts/routing_core/catalog.py-22-# ADR-0006 (AM-2): probe cache is a filtering-only optimization. TTL bounds staleness;
--
ai/scripts/routing_core/catalog.py-115-
ai/scripts/routing_core/catalog.py-116-
ai/scripts/routing_core/catalog.py:117:# ADR-0042 (022 PKG-1): both maps below DERIVE from the single `PROVIDERS` registry now
ai/scripts/routing_core/catalog.py-118-# (`provider_registry.py`) -- before this ADR they were two independently hand-edited
ai/scripts/routing_core/catalog.py-119-# literals, one of six provider-keyed tables that had to move in lockstep by hand across
--
ai/scripts/routing_core/catalog.py-127-# single-token CLI ids in _OPENCODE_CLI_IDS) — re-measured live against `opencode auth
ai/scripts/routing_core/catalog.py-128-# list --pure` (docs/specs/012-discovered-inventory/spec.md AC-02).
ai/scripts/routing_core/catalog.py:129:_OPENCODE_PROVIDER_KEYS = {provider: spec.opencode_auth_key for provider, spec in PROVIDERS.items()}
ai/scripts/routing_core/catalog.py-130-# Catalog provider -> CLI argument for `opencode models <id>` AND the `<id>/<model>` line
ai/scripts/routing_core/catalog.py-131-# prefix `_parse_opencode_models` strips (map 2 of 2, 012 AC-02). Independently
--
ai/scripts/routing_core/catalog.py-135-# two-map split fixes; PI_MODEL_MAP is explicitly the wrong precedent here — it
ai/scripts/routing_core/catalog.py-136-# translates a model name within one already-matched provider, a different axis).
ai/scripts/routing_core/catalog.py:137:_OPENCODE_CLI_IDS = {provider: spec.opencode_cli_id for provider, spec in PROVIDERS.items()}
ai/scripts/routing_core/catalog.py-138-# ADR-0034 (019 PKG-1) re-measurement, live, 2026-08-10 (opencode 1.18.14): `opencode
ai/scripts/routing_core/catalog.py-139-# auth list --pure` shows 4 real credentials -- `opencode-go`, `openai`, `github-copilot`,
--
ai/scripts/routing_core/catalog.py-176-# subscription; OpenCode Zen is metered/API-key (user clarification, logged at
ai/scripts/routing_core/catalog.py-177-# ai/state/decisions-log.jsonl, slug opencode-zen-go-billing-model-distinto-no-mismo-pool).
ai/scripts/routing_core/catalog.py:178:# ADR-0035 (019 PKG-2): completed to cover every DISCOVERABLE_PROVIDERS member --
ai/scripts/routing_core/catalog.py-179-# `openai-codex`/`anthropic` are the curated subscription lanes (Codex CLI login, Claude
ai/scripts/routing_core/catalog.py-180-# Code OAuth), `opencode-go` stays subscription, `opencode-zen` stays the sole metered
ai/scripts/routing_core/catalog.py-181-# entry. This is the day `billing_rank` (below) starts reading it -- the previous "no
ai/scripts/routing_core/catalog.py-182-# weighting/selection logic reads this map yet" note is retired.
ai/scripts/routing_core/catalog.py:183:# ADR-0042 (022 PKG-1): derives from `PROVIDERS` now, same values as the pre-ADR-0042 literal.
ai/scripts/routing_core/catalog.py:184:PROVIDER_BILLING_KIND = {provider: spec.billing_kind for provider, spec in PROVIDERS.items()}
ai/scripts/routing_core/catalog.py-185-# ADR-0035 AC-12: the FREE-model convention is the `-free` suffix -- the exact same
ai/scripts/routing_core/catalog.py-186-# pattern `inference._FAST_HINTS` already treats as a signal, never a second, competing
--
ai/scripts/routing_core/catalog.py-209-
ai/scripts/routing_core/catalog.py-210-
ai/scripts/routing_core/catalog.py:211:# ADR-0042 (022 PKG-1): provider -> `[catalog]` TOML key, derived from `PROVIDERS` (same
ai/scripts/routing_core/catalog.py:212:# values as the pre-ADR-0042 inline literal `_configured_models` used to carry). Site (2) of
ai/scripts/routing_core/catalog.py:213:# the five-site lockstep `_configured_models` documents below -- the other four sites (the
ai/scripts/routing_core/catalog.py-214-# `[catalog]` table itself, `build_snapshot`'s own comprehension, and the two
ai/scripts/routing_core/catalog.py-215-# `models_config.py` sites) are a DIFFERENT, wider concern (012 AC-04: whether a TOML key
ai/scripts/routing_core/catalog.py-216-# exists and round-trips through `load_config`/`emit` at all) and are out of this package's
ai/scripts/routing_core/catalog.py-217-# scope -- only this key map derives from the single provider registry.
ai/scripts/routing_core/catalog.py:218:_CATALOG_KEYS = {provider: spec.catalog_key for provider, spec in PROVIDERS.items()}
ai/scripts/routing_core/catalog.py-219-
ai/scripts/routing_core/catalog.py-220-
ai/scripts/routing_core/catalog.py:221:def _configured_models(config: dict, provider: str) -> set[str]:
ai/scripts/routing_core/catalog.py-222-    # 012 AC-04: same five-site lockstep as the pre-existing two providers (corrected
ai/scripts/routing_core/catalog.py-223-    # 012 repair F-04: the original comment undercounted "three sites" — this package
ai/scripts/routing_core/catalog.py-224-    # added two more in models_config.py) — this key map is site (2) of 5 that must move
ai/scripts/routing_core/catalog.py-225-    # together with: (1) models.toml's [catalog] table itself; (2) this key map (now
ai/scripts/routing_core/catalog.py:226:    # `_CATALOG_KEYS` above, ADR-0042); (3) build_snapshot's own `configured_models`
ai/scripts/routing_core/catalog.py-227-    # comprehension (catalog.py, below); (4) models_config.load_config's optional-key
ai/scripts/routing_core/catalog.py-228-    # validation loop for the same two TOML keys; (5) models_config.emit's preservation
--
ai/scripts/routing_core/catalog.py-234-    # allowlists unreachable from code; extending only (1)-(3) without (4)/(5) leaves them
ai/scripts/routing_core/catalog.py-235-    # reachable but silently unpersisted on the next wizard run.
ai/scripts/routing_core/catalog.py:236:    key = _CATALOG_KEYS.get(provider)
ai/scripts/routing_core/catalog.py-237-    models = config.get("catalog", {}).get(key, ()) if key else ()
ai/scripts/routing_core/catalog.py-238-    if not isinstance(models, (list, tuple)):
--
ai/scripts/routing_core/catalog.py-405-    Only PAIRS PRESENT are ever positive (F06: negatives are never persisted,
ai/scripts/routing_core/catalog.py-406-    so a transient failure costs one retry, never the full TTL). Every entry
ai/scripts/routing_core/catalog.py:407:    is re-intersected with the live `_configured_models` catalog on read
ai/scripts/routing_core/catalog.py-408-    (F09), so a stale-but-key-matching cache can never widen the audited set.
ai/scripts/routing_core/catalog.py-409-    """
--
ai/scripts/routing_core/catalog.py-427-            if (runtime, provider) not in _PAIR_COMMANDS or not isinstance(models, list) or not all(isinstance(m, str) for m in models):
ai/scripts/routing_core/catalog.py-428-                return None
ai/scripts/routing_core/catalog.py:429:            intersected = set(models) & _configured_models(config, provider)
ai/scripts/routing_core/catalog.py-430-            if intersected:  # empty after intersection ⇒ treated as absent, retried like any negative
ai/scripts/routing_core/catalog.py-431-                out[(runtime, provider)] = intersected
--
ai/scripts/routing_core/catalog.py-485-        runtime, provider = pair
ai/scripts/routing_core/catalog.py-486-        commands = _PAIR_COMMANDS[pair]
ai/scripts/routing_core/catalog.py:487:        allowed = _configured_models(config, provider)
ai/scripts/routing_core/catalog.py-488-        if not allowed:
ai/scripts/routing_core/catalog.py-489-            continue
--
ai/scripts/routing_core/catalog.py-640-    except (OSError, tomllib.TOMLDecodeError, KeyError, TypeError, ValueError) as exc: raise RoutingError("CATALOG_INVALID") from exc
ai/scripts/routing_core/catalog.py-641-    roster_names = {row["role"] for row in roster}; routes=[]; canonical_seen=set(); ids={}; route_runtimes={}
ai/scripts/routing_core/catalog.py:642:    # 012 AC-04, site 3 of 5 (corrected 012 repair F-04 — see _configured_models's own
ai/scripts/routing_core/catalog.py-643-    # comment above for the full five-site enumeration; moves in lockstep with
ai/scripts/routing_core/catalog.py:644:    # models.toml's [catalog] table and _configured_models's key map at minimum):
ai/scripts/routing_core/catalog.py-645-    # opencode-zen/opencode-go are listed here too, even though no routes.v1.toml row
ai/scripts/routing_core/catalog.py-646-    # references them yet — this contract's non-goals paragraph states "no curated
--
ai/scripts/routing_core/catalog.py-650-    # provider's allowlist would otherwise be silently unreachable the moment a row for it
ai/scripts/routing_core/catalog.py-651-    # is curated later.
ai/scripts/routing_core/catalog.py:652:    configured_models = {provider: _configured_models(config, provider)
ai/scripts/routing_core/catalog.py-653-                         for provider in ("openai-codex", "anthropic", "opencode-zen", "opencode-go")}
ai/scripts/routing_core/catalog.py-654-    # Closed row schema: required keys plus the allowlisted-optional `runtimes` (contract 004 AC-12:
--
ai/scripts/routing_core/catalog.py-665-            roles, tools = (_sorted_strings(row[key]) for key in ("roles", "tools"))
ai/scripts/routing_core/catalog.py-666-            priority = row["curated_priority"]
ai/scripts/routing_core/catalog.py:667:            if (provider not in enabled or model not in configured_models.get(provider, set())
ai/scripts/routing_core/catalog.py-668-                    or not isinstance(tier, str) or tier not in TIER_ORDER
ai/scripts/routing_core/catalog.py-669-                    or not isinstance(priority, int) or priority < 0 or not set(roles) <= roster_names): raise ValueError
--
ai/scripts/routing_core/catalog.py-716-    audited `_PAIR_COMMANDS` provider set -- copilot and any other never-audited provider
ai/scripts/routing_core/catalog.py-717-    can never appear here no matter what a probe observes (M-1). An explicit list is used
ai/scripts/routing_core/catalog.py:718:    as-is (already validated against `DISCOVERABLE_PROVIDERS` at models_config load time);
ai/scripts/routing_core/catalog.py-719-    anything else (missing key, malformed value from a caller that skipped validation) is
ai/scripts/routing_core/catalog.py-720-    the empty tuple, fail-closed.
--
ai/scripts/routing_core/catalog.py-817-    """Every model the probe actually observed for `provider`, across runtimes.
ai/scripts/routing_core/catalog.py-818-
ai/scripts/routing_core/catalog.py:819:    Unlike `_configured_models` (curated ceiling, intersection semantics), this is
ai/scripts/routing_core/catalog.py-820-    the raw observed pool — the [catalog] allowlist already shaped what the probe
ai/scripts/routing_core/catalog.py-821-    could return for the two OpenCode-lane providers, and the synthesized-route
--
tests/test_discovered_routes.py-3-New tests only. The immutable contracts stay untouched and are re-asserted here
tests/test_discovered_routes.py-4-from THIS feature's angle: `build_snapshot` never widens (the effective path is a
tests/test_discovered_routes.py:5:separate entry point), `ROUTING_PROVIDERS` stays closed (discovered_providers is
tests/test_discovered_routes.py-6-a different key), and with the flag absent the composition is byte-identical.
tests/test_discovered_routes.py-7-"""
--
tests/test_discovered_routes.py-158-        # ADR-0034 (AC-01): the new default is the STRING "auto", not an empty list --
tests/test_discovered_routes.py-159-        # a repo that never touches [routing].discovered_providers now gets live
tests/test_discovered_routes.py:160:        # auto-adoption for free. The closed ROUTING_PROVIDERS allowlist (the curated
tests/test_discovered_routes.py-161-        # path) is untouched either way.
tests/test_discovered_routes.py-162-        config = models_config.load_config()
tests/test_discovered_routes.py-163-        self.assertEqual(config["routing"]["discovered_providers"], "auto")
tests/test_discovered_routes.py:164:        self.assertNotIn("opencode-zen", models_config.ROUTING_PROVIDERS)  # closed set untouched
tests/test_discovered_routes.py-165-
tests/test_discovered_routes.py-166-    def test_discovered_providers_accepts_auto_or_explicit_list_never_anything_else(self):
tests/test_discovered_routes.py:167:        # ADR-0034 (AC-01/AC-10): "auto" and any unique subset of DISCOVERABLE_PROVIDERS
tests/test_discovered_routes.py-168-        # are the only legal values -- github-copilot/openai (M-1/M-2) can never be typed
tests/test_discovered_routes.py-169-        # in, and a malformed value dies exactly like every other closed [routing] field.
--
ai/scripts/set_agents_app.py-99-# never reads that table (nor even names it -- see the test above, unchanged by this
ai/scripts/set_agents_app.py-100-# package). What changed is provider IDENTITY: this tuple now derives from the single
ai/scripts/set_agents_app.py:101:# `provider_registry.PROVIDERS` registry every provider-keyed table in the harness shares,
ai/scripts/set_agents_app.py-102-# instead of being a second hand-maintained copy of the same four provider ids. Order is a
ai/scripts/set_agents_app.py-103-# real contract (surfaces in pin/preference validation errors) --
ai/scripts/set_agents_app.py:104:# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
ai/scripts/set_agents_app.py-105-# because that sibling table is an unordered `set`.
ai/scripts/set_agents_app.py:106:_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
ai/scripts/set_agents_app.py-107-_MODEL_PREFERENCE_CLASSES = ("decision", "grunt", "build")
ai/scripts/set_agents_app.py-108-# AC-02's inertness note: the six roles with real, live effect today (`### Honest scope`,
--
ai/scripts/set_agents_app.py-124-
ai/scripts/set_agents_app.py-125-def _effective_preference_providers():
ai/scripts/set_agents_app.py:126:    """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
ai/scripts/set_agents_app.py:127:    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
ai/scripts/set_agents_app.py:128:    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
ai/scripts/set_agents_app.py-129-    purpose, ordered tuple vs. unordered set, never in membership) UNION whatever the
ai/scripts/set_agents_app.py-130-    CURRENT effective snapshot reports as routable, when that snapshot is cheaply
--
ai/scripts/set_agents_app.py-143-    which today it structurally cannot: every provider it can produce comes from
ai/scripts/set_agents_app.py-144-    `routing_core.catalog._PAIR_COMMANDS`'s own audited provider set, and that set is
ai/scripts/set_agents_app.py:145:    pinned equal to `models_config.DISCOVERABLE_PROVIDERS` (== `_MODEL_PREFERENCE_
ai/scripts/set_agents_app.py:146:    PROVIDERS`, guarded for real since ADR-0042 -- both are derived from
ai/scripts/set_agents_app.py:147:    `provider_registry.PROVIDERS`, and `test_adr0042_ac01b_...` in tests/test_routing.py
ai/scripts/set_agents_app.py-148-    compares this tuple against `_PAIR_COMMANDS`'s own provider set directly, never a
ai/scripts/set_agents_app.py-149-    second hardcoded literal). So the probe below is skipped whenever the
ai/scripts/set_agents_app.py:150:    base set already covers `DISCOVERABLE_PROVIDERS` -- a cheap, exact short-circuit
ai/scripts/set_agents_app.py-151-    that avoids paying a subprocess probe (each `_PAIR_COMMANDS` entry can block up to
ai/scripts/set_agents_app.py-152-    20s) on every `--model-pin-set`/`--model-preference-set` call for a union that
--
ai/scripts/set_agents_app.py-154-    `_PAIR_COMMANDS`'s audited set genuinely grows past the base set (e.g. a future
ai/scripts/set_agents_app.py-155-    ADR widens discovery beyond today's four providers)."""
ai/scripts/set_agents_app.py:156:    if set(models_config.DISCOVERABLE_PROVIDERS) <= set(_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py:157:        return _MODEL_PREFERENCE_PROVIDERS
ai/scripts/set_agents_app.py-158-    try:
ai/scripts/set_agents_app.py-159-        from routing_core.catalog import probe_inventory, resolve_discovered_providers
--
ai/scripts/set_agents_app.py-162-        live = resolve_discovered_providers(config, inventory)
ai/scripts/set_agents_app.py-163-    except Exception:
ai/scripts/set_agents_app.py:164:        return _MODEL_PREFERENCE_PROVIDERS
ai/scripts/set_agents_app.py:165:    return tuple(dict.fromkeys((*_MODEL_PREFERENCE_PROVIDERS, *live)))
ai/scripts/set_agents_app.py-166-
ai/scripts/set_agents_app.py-167-
ai/scripts/set_agents_app.py:168:def _validate_preference_providers(class_name, providers, valid_providers=_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py-169-    """AC-02 resolution states 1/2/4d: shared by the config-load path and the CLI write
ai/scripts/set_agents_app.py-170-    path (`load_model_preference`/`cmd_model_preference_set` below) -- a malformed value
--
ai/scripts/set_agents_app.py-253-
ai/scripts/set_agents_app.py-254-
ai/scripts/set_agents_app.py:255:def _validate_model_pin_entry(role, value, roster_roles, valid_providers=_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py-256-    """`valid_providers` (ADR-0034 AC-09): defaults to the static base set, so every
ai/scripts/set_agents_app.py-257-    boot-path caller (`load_model_pin`) stays network-free; the write-CLI
--
ai/scripts/set_agents_app.py-856-        config = models_config.load_config()
ai/scripts/set_agents_app.py-857-    except SystemExit:
ai/scripts/set_agents_app.py:858:        print("PROVIDERS_UNKNOWN models.toml inválido — corré ./build.sh --check")
ai/scripts/set_agents_app.py-859-        return 1
ai/scripts/set_agents_app.py-860-    inventory = probe_inventory(config, cache_root=STATE_DIR)
ai/scripts/set_agents_app.py-861-    pairs = {pair: models for pair, models in inventory.items() if models}
ai/scripts/set_agents_app.py-862-    if not pairs:
ai/scripts/set_agents_app.py:863:        print("PROVIDERS_NONE no se detectó ninguna suscripción activa (claude/codex/opencode/pi) — "
ai/scripts/set_agents_app.py-864-              "logueate en al menos una herramienta y volvé a correr set-agents --doctor-all")
ai/scripts/set_agents_app.py-865-    for (runtime, provider), models in sorted(pairs.items()):
--
tests/test_routing.py-1941-                os.environ["PATH"]=old
tests/test_routing.py-1942-
tests/test_routing.py:1943:    def test_probe_cache_read_reintersects_with_configured_models(self):
tests/test_routing.py-1944-        # F09: even a key-matching, byte-valid cache document can never widen the audited
tests/test_routing.py-1945-        # set beyond the live models.toml catalog — a foreign/stale model name is dropped.
--
tests/test_routing.py-3171-    def test_ac04_allowlist_ceiling_moved_in_lockstep_across_the_three_sites(self):
tests/test_routing.py-3172-        from routing_core import catalog as cat
tests/test_routing.py:3173:        # Site 2 (_configured_models's key map) reads exactly site 1 (models.toml's
tests/test_routing.py-3174-        # [catalog] table).
tests/test_routing.py:3175:        self.assertEqual(cat._configured_models(self.config, "opencode-zen"), set(self.config["catalog"]["opencode_zen"]))
tests/test_routing.py:3176:        self.assertEqual(cat._configured_models(self.config, "opencode-go"), set(self.config["catalog"]["opencode_go"]))
tests/test_routing.py-3177-        self.assertIn("kimi-k2.7-code", self.config["catalog"]["opencode_zen"])
tests/test_routing.py-3178-        self.assertIn("kimi-k2.7-code", self.config["catalog"]["opencode_go"])
--
tests/test_routing.py-3180-        # it: _probe_pairs intersects the parsed roster against `allowed` before returning.
tests/test_routing.py-3181-        narrow = json.loads(json.dumps(self.config)); narrow["catalog"]["opencode_zen"] = ["kimi-k2.7-code"]
tests/test_routing.py:3182:        self.assertEqual(cat._configured_models(narrow, "opencode-zen"), {"kimi-k2.7-code"})
tests/test_routing.py-3183-
tests/test_routing.py-3184-        def fake_run(argv, **kwargs):
--
tests/test_routing.py-3191-
tests/test_routing.py-3192-    def test_adr0034_ac10_discoverable_providers_lockstep_guard(self):
tests/test_routing.py:3193:        # ADR-0034 (019 PKG-1, AC-10): DISCOVERABLE_PROVIDERS must equal exactly the
tests/test_routing.py-3194-        # provider half of _PAIR_COMMANDS -- the guard that keeps `github-copilot` (M-1,
tests/test_routing.py-3195-        # unlistable) and a duplicate `openai` catalog provider (M-2, already covered by
--
tests/test_routing.py-3197-        # this test failing loudly first.
tests/test_routing.py-3198-        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
tests/test_routing.py:3199:        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS, pair_providers)
tests/test_routing.py:3200:        self.assertNotIn("github-copilot", models_config.DISCOVERABLE_PROVIDERS)
tests/test_routing.py:3201:        self.assertNotIn("openai", models_config.DISCOVERABLE_PROVIDERS)
tests/test_routing.py-3202-
tests/test_routing.py-3203-    def test_ac05_new_providers_are_probeable_not_routable_today(self):
tests/test_routing.py:3204:        # AC-05/AC-11 non-goal: no enabled_providers or ROUTING_PROVIDERS change -- a
tests/test_routing.py-3205-        # curated row for either new provider must still be rejected by build_snapshot
tests/test_routing.py-3206-        # today, proving discovery never silently becomes routability.
--
tests/test_routing.py-3211-            with self.assertRaises(routing.RoutingError):
tests/test_routing.py-3212-                routing_catalog.build_snapshot(path, self.roster, self.config)
tests/test_routing.py:3213:        self.assertNotIn("opencode-zen", models_config.ROUTING_PROVIDERS)
tests/test_routing.py:3214:        self.assertNotIn("opencode-go", models_config.ROUTING_PROVIDERS)
tests/test_routing.py-3215-        self.assertNotIn("opencode-zen", self.config["routing"]["enabled_providers"])
tests/test_routing.py-3216-        self.assertNotIn("opencode-go", self.config["routing"]["enabled_providers"])
--
tests/test_routing.py-3257-    def test_ac08_subscription_metered_map_is_provider_keyed_not_a_row_field(self):
tests/test_routing.py-3258-        from routing_core import catalog as cat
tests/test_routing.py:3259:        # ADR-0035 (019 PKG-2): completed to cover every DISCOVERABLE_PROVIDERS member --
tests/test_routing.py-3260-        # `openai-codex`/`anthropic` (curated subscription lanes) joined `opencode-go`
tests/test_routing.py-3261-        # (subscription); `opencode-zen` stays the sole metered entry. Previously this
--
tests/test_routing.py-3277-    def test_ac09_route_id_identity_is_provider_agnostic_for_a_synthetic_discovered_row(self):
tests/test_routing.py-3278-        # AC-09: a unit-level property proof, not an end-to-end routed path -- no route
tests/test_routing.py:3279:        # row, enabled_providers entry, or ROUTING_PROVIDERS entry exists for either new
tests/test_routing.py-3280-        # provider yet, so no live authorization can reach one.
tests/test_routing.py-3281-        from routing_core.domain import StaticRoute
--
tests/test_routing.py-3291-                                                         ("balanced",), ("implementer",), ("read", "write"), 10))
tests/test_routing.py-3292-        # No provider allowlist check inside the identity function itself -- it has no
tests/test_routing.py:3293:        # knowledge of _PAIR_COMMANDS, enabled_providers, or ROUTING_PROVIDERS, so even a
tests/test_routing.py-3294-        # totally unaudited provider string produces a well-formed id (never raises here;
tests/test_routing.py-3295-        # rejection, when it happens, is build_snapshot's job, not the identity function's).
--
tests/test_routing.py-3348-        for provider in ("opencode-zen", "opencode-go"):
tests/test_routing.py-3349-            pair = ("opencode", provider)
tests/test_routing.py:3350:            ceiling = live[provider] & cat._configured_models(self.config, provider)
tests/test_routing.py-3351-            probed_models = probed.get(pair, set())
tests/test_routing.py-3352-            # Repair F-02 (012 repair, high): `probed_models <= ceiling` passes trivially
--
tests/test_routing.py-3467-        # would otherwise reject the opencode-zen row for an unrelated reason and mask
tests/test_routing.py-3468-        # whether the collision check itself still works); nothing is written to disk and
tests/test_routing.py:3469:        # ROUTING_PROVIDERS is never touched.
tests/test_routing.py-3470-        extended = json.loads(json.dumps(self.config))
tests/test_routing.py-3471-        extended["routing"] = dict(extended["routing"], enabled_providers=extended["routing"]["enabled_providers"] + ["opencode-zen"])
--
tests/test_routing.py-3549-                         cat.canonical_model("opencode-zen", "claude-fable-5"))
tests/test_routing.py-3550-
tests/test_routing.py:3551:    def test_ac04_site3_configured_models_comprehension_is_load_bearing(self):
tests/test_routing.py-3552-        """Repair F-03 (012 repair, medium): the "lockstep across the three sites" test
tests/test_routing.py:3553:        exercised sites 1/2 only. Site 3 -- build_snapshot's own `configured_models`
tests/test_routing.py-3554-        comprehension -- is load-bearing (verified by mutation: reverting it to the
tests/test_routing.py-3555-        pre-package two-provider tuple leaves the rest of the suite green, since no route
tests/test_routing.py-3556-        row curates opencode-zen/opencode-go today, AC-05's own non-goal), but nothing
tests/test_routing.py-3557-        exercised it directly. `enabled_providers` is extended in an IN-MEMORY COPY of
tests/test_routing.py:3558:        config only -- never written to disk, never touching ROUTING_PROVIDERS -- which
tests/test_routing.py-3559-        isolates site 3 from the separate enabled_providers gate that would otherwise
tests/test_routing.py-3560-        mask its absence (the same masking problem F-01 fixed for the roster-coverage
--
tests/test_routing.py-3964-            # Even when the live probe blows up, the base constant still validates a
tests/test_routing.py-3965-            # legitimate provider -- never a crash, matching the documented degrade.
tests/test_routing.py:3966:            self.assertEqual(app._MODEL_PREFERENCE_PROVIDERS, ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
tests/test_routing.py:3967:        wider = (*app._MODEL_PREFERENCE_PROVIDERS, "a-future-live-only-provider")
tests/test_routing.py-3968-        validated = app._validate_preference_providers("grunt", ["a-future-live-only-provider"], wider)
tests/test_routing.py-3969-        self.assertEqual(validated, ("a-future-live-only-provider",))
--
tests/test_routing.py-3972-
tests/test_routing.py-3973-    def test_adr0034_f05_repair_effective_providers_short_circuits_when_base_covers_discoverable(self):
tests/test_routing.py:3974:        # F-05 (P1 repair): `_MODEL_PREFERENCE_PROVIDERS` already covers
tests/test_routing.py:3975:        # `models_config.DISCOVERABLE_PROVIDERS` today (AC-10's own lockstep test), so the
tests/test_routing.py-3976-        # union in `_effective_preference_providers` is a structural no-op -- yet it used
tests/test_routing.py-3977-        # to pay a live `probe_inventory` subprocess probe (each `_PAIR_COMMANDS` entry
--
tests/test_routing.py-3980-        import set_agents_app as app
tests/test_routing.py-3981-        with mock.patch("routing_core.catalog.probe_inventory", return_value={}) as probe:
tests/test_routing.py:3982:            self.assertEqual(app._effective_preference_providers(), app._MODEL_PREFERENCE_PROVIDERS)
tests/test_routing.py-3983-            probe.assert_not_called()
tests/test_routing.py-3984-        # Only once the audited set genuinely grows past the base set does the probe run
tests/test_routing.py-3985-        # again -- proving the short-circuit is a real conditional, not a permanent skip.
tests/test_routing.py:3986:        with mock.patch.object(app.models_config, "DISCOVERABLE_PROVIDERS",
tests/test_routing.py:3987:                                frozenset({*app._MODEL_PREFERENCE_PROVIDERS, "a-future-only-provider"})):
tests/test_routing.py-3988-            with mock.patch("routing_core.catalog.probe_inventory", return_value={}) as probe:
tests/test_routing.py-3989-                with mock.patch("routing_core.catalog.resolve_discovered_providers", return_value=()):
--
tests/test_routing.py-4005-
tests/test_routing.py-4006-    # ------------------------------------------------- ADR-0042 (022 PKG-1): single provider
tests/test_routing.py:4007:    # registry -- `provider_registry.PROVIDERS` is now the one source every provider-keyed
tests/test_routing.py-4008-    # table below derives from, replacing six independently hand-edited literal copies.
tests/test_routing.py-4009-
--
tests/test_routing.py-4014-        # only for the tuple (AC-01's provider-registry insertion order contract); the dicts
tests/test_routing.py-4015-        # and the set compare by value, so their literal order below is not significant.
tests/test_routing.py:4016:        self.assertEqual(list(provider_registry.PROVIDERS),
tests/test_routing.py-4017-                         ["openai-codex", "anthropic", "opencode-zen", "opencode-go"])
tests/test_routing.py-4018-        self.assertEqual(routing_catalog._OPENCODE_PROVIDER_KEYS, {
--
tests/test_routing.py-4029-        })
tests/test_routing.py-4030-        # AC-01's fifth symbol: the key map that used to live INLINE inside
tests/test_routing.py:4031:        # `_configured_models` (a local dict literal, unreachable from outside the function)
tests/test_routing.py:4032:        # is now the module-level `_CATALOG_KEYS`, itself derived from `PROVIDERS`.
tests/test_routing.py:4033:        self.assertEqual(routing_catalog._CATALOG_KEYS, {
tests/test_routing.py-4034-            "openai-codex": "codex", "anthropic": "claude",
tests/test_routing.py-4035-            "opencode-zen": "opencode_zen", "opencode-go": "opencode_go",
tests/test_routing.py-4036-        })
tests/test_routing.py:4037:        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS,
tests/test_routing.py-4038-                         {"openai-codex", "anthropic", "opencode-zen", "opencode-go"})
tests/test_routing.py:4039:        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
tests/test_routing.py-4040-                         ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
tests/test_routing.py-4041-
tests/test_routing.py-4042-    def test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source(self):
tests/test_routing.py:4043:        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
tests/test_routing.py-4044-        # lived at test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_
tests/test_routing.py-4045-        # not_just_the_constant (below, :3965 pre-refactor line count) -- inside a
--
tests/test_routing.py-4051-        #
tests/test_routing.py-4052-        # Cross-check 1: same pattern as test_adr0034_ac10_discoverable_providers_lockstep_
tests/test_routing.py:4053:        # guard above (which only ever covered DISCOVERABLE_PROVIDERS, never this tuple,
tests/test_routing.py-4054-        # despite set_agents_app.py's own comment claiming otherwise pre-ADR-0042) --
tests/test_routing.py-4055-        # membership against `_PAIR_COMMANDS`'s own audited provider set.
tests/test_routing.py-4056-        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
tests/test_routing.py:4057:        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
tests/test_routing.py-4058-        # Cross-check 2: against the ordered registry directly -- this is what makes a
tests/test_routing.py-4059-        # FUTURE regression (someone re-introducing a hand-maintained tuple in
tests/test_routing.py:4060:        # set_agents_app.py instead of deriving it) fail loudly the moment `PROVIDERS`
tests/test_routing.py-4061-        # changes without a matching edit here, rather than silently agreeing with a second,
tests/test_routing.py-4062-        # equally-stale literal (the exact defect this package exists to close).
tests/test_routing.py:4063:        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
tests/test_routing.py-4064-
tests/test_routing.py-4065-    # --------------------------------------------- 014-model-preference-policy (AC-01..09)
--
ai/scripts/setup_models.py-436-            # routable set from the live probed inventory), "lista manual" (the previous
ai/scripts/setup_models.py-437-            # per-provider toggle, but candidates now come from the AUDITED set --
ai/scripts/setup_models.py:438:            # `models_config.DISCOVERABLE_PROVIDERS`, never a literal tuple, so a future
ai/scripts/setup_models.py-439-            # fifth audited provider appears here without touching this file), or
ai/scripts/setup_models.py-440-            # "ninguno" (explicit `[]`, ADR-0034 point 1: total opt-out that survives
--
ai/scripts/setup_models.py-459-                print("OK: discovered_providers = [] (auto-adopción desactivada)")
ai/scripts/setup_models.py-460-            else:
ai/scripts/setup_models.py:461:                candidates = sorted(models_config.DISCOVERABLE_PROVIDERS)
ai/scripts/setup_models.py-462-                manual_current = list(current) if isinstance(current, (list, tuple)) else []
ai/scripts/setup_models.py-463-                options = []
--
ai/scripts/models_config.py-43-RUNTIMES = ("opencode", "claude-code", "codex", "pi")
ai/scripts/models_config.py-44-MODEL_TIERS = ("fast", "balanced", "frontier")
ai/scripts/models_config.py:45:ROUTING_PROVIDERS = {"openai-codex", "anthropic"}
ai/scripts/models_config.py-46-# ADR-0029 (017 PKG-B2): providers eligible for SYNTHESIZED routes (probe-discovered,
ai/scripts/models_config.py:47:# tier/family inferred). Distinct from ROUTING_PROVIDERS on purpose — the curated
ai/scripts/models_config.py-48-# allowlist stays closed (immutable contract "probeable, never routable, by 012 alone");
ai/scripts/models_config.py-49-# this is the separate, opt-in surface that makes a discovered model routable.
ai/scripts/models_config.py:50:# ADR-0042 (022 PKG-1): derives from the single `provider_registry.PROVIDERS` registry now,
ai/scripts/models_config.py-51-# same four members as the pre-ADR-0042 literal. Order-insensitive on purpose (a `set`) --
ai/scripts/models_config.py:52:# `set_agents_app._MODEL_PREFERENCE_PROVIDERS` is the ORDERED sibling table, and it derives
ai/scripts/models_config.py:53:# from `provider_registry.PROVIDERS` directly rather than from this set, because a tuple
ai/scripts/models_config.py-54-# built from a set has no deterministic order across runs.
ai/scripts/models_config.py:55:DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)
ai/scripts/models_config.py-56-ROUTING_DEFAULTS = {
ai/scripts/models_config.py-57-    "enabled_providers": ["openai-codex", "anthropic"], "xhigh_benchmarked": False,
--
ai/scripts/models_config.py-151-    # 012 discovered-inventory AC-04: optional declared allowlist ceiling for the two
ai/scripts/models_config.py-152-    # probeable-only OpenCode-lane providers (routing_core/catalog.py consumes these via
ai/scripts/models_config.py:153:    # _configured_models; absent entirely is valid — a repo with neither subscription
ai/scripts/models_config.py-154-    # simply never populates them, and probe_inventory then finds nothing configured).
ai/scripts/models_config.py-155-    for key in ("opencode_zen", "opencode_go"):
--
ai/scripts/models_config.py-229-    if discovered != "auto" and (
ai/scripts/models_config.py-230-            not isinstance(discovered, list) or len(discovered) != len(set(discovered))
ai/scripts/models_config.py:231:            or not all(isinstance(x, str) and x in DISCOVERABLE_PROVIDERS for x in discovered)):
ai/scripts/models_config.py-232-        die("models.toml: [routing].discovered_providers must be \"auto\" or a unique list within the audited provider set")
ai/scripts/models_config.py:233:    if (not isinstance(routing["enabled_providers"], list) or not routing["enabled_providers"] or not all(isinstance(x, str) and x in ROUTING_PROVIDERS for x in routing["enabled_providers"])
ai/scripts/models_config.py-234-            or len(routing["enabled_providers"]) != len(set(routing["enabled_providers"]))
ai/scripts/models_config.py-235-            or not isinstance(routing["xhigh_benchmarked"], bool) or routing["max_enabled"] is not False
--
tests/test_models_wizard_ui.py-109-        # ADR-0035 (AC-16): option 7 is now a three-way policy picker (auto/manual/none);
tests/test_models_wizard_ui.py-110-        # "Lista manual" (index 1) still offers the per-provider toggle, but its
tests/test_models_wizard_ui.py:111:        # candidates come from `models_config.DISCOVERABLE_PROVIDERS` (sorted:
tests/test_models_wizard_ui.py-112-        # anthropic, openai-codex, opencode-go, opencode-zen), not a literal tuple --
tests/test_models_wizard_ui.py-113-        # index 3 in that sorted list is `opencode-zen`.
--
tests/test_models_wizard_ui.py-115-            setup_models.tui.Selected(6),   # Proveedores descubiertos
tests/test_models_wizard_ui.py-116-            setup_models.tui.Selected(1),   # Lista manual
tests/test_models_wizard_ui.py:117:            setup_models.tui.Selected(3),   # opencode-zen (sorted DISCOVERABLE_PROVIDERS)
tests/test_models_wizard_ui.py-118-            setup_models.tui.Selected(4),   # Salir sin guardar
tests/test_models_wizard_ui.py-119-        ])
--
ai/scripts/provider_registry.py-1-"""ADR-0042 (022 PKG-1): the single audited provider registry.
ai/scripts/provider_registry.py-2-
ai/scripts/provider_registry.py:3:`PROVIDERS` is the ONE source `routing_core/catalog.py`, `models_config.py`, and
ai/scripts/provider_registry.py-4-`set_agents_app.py` derive their provider-keyed tables from -- before this module existed,
ai/scripts/provider_registry.py-5-those three files hand-maintained SIX independently-editable copies of the same four-provider
ai/scripts/provider_registry.py-6-universe (`_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, the key map
ai/scripts/provider_registry.py:7:inside `_configured_models`, `DISCOVERABLE_PROVIDERS`, `_MODEL_PREFERENCE_PROVIDERS`), synced
ai/scripts/provider_registry.py-8-only by human discipline. Adding a provider now means adding one `ProviderSpec` row here.
ai/scripts/provider_registry.py-9-
--
ai/scripts/provider_registry.py-17-
ai/scripts/provider_registry.py-18-The dict's INSERTION ORDER is a real contract, not incidental: `set_agents_app.
ai/scripts/provider_registry.py:19:_MODEL_PREFERENCE_PROVIDERS` derives its ordered tuple from `tuple(PROVIDERS)`, and that order
ai/scripts/provider_registry.py-20-(`openai-codex, anthropic, opencode-zen, opencode-go`) surfaces in user-facing validation
ai/scripts/provider_registry.py-21-messages. Never reorder this dict without checking that consumer.
--
ai/scripts/provider_registry.py-51-# Order is a contract -- see module docstring. Values are unchanged from the pre-ADR-0042
ai/scripts/provider_registry.py-52-# literals (PKG-1 AC-02 characterization test pins this).
ai/scripts/provider_registry.py:53:PROVIDERS: dict[str, ProviderSpec] = {
ai/scripts/provider_registry.py-54-    "openai-codex": ProviderSpec(
ai/scripts/provider_registry.py-55-        opencode_auth_key="openai", opencode_cli_id="openai",
ai/scripts/routing_core/catalog.py:653:                         for provider in ("openai-codex", "anthropic", "opencode-zen", "opencode-go")}
ai/scripts/provider_registry.py:20:(`openai-codex, anthropic, opencode-zen, opencode-go`) surfaces in user-facing validation
diff --git i/ai/scripts/models_config.py w/ai/scripts/models_config.py
index 4df6cd6..f758247 100644
--- i/ai/scripts/models_config.py
+++ w/ai/scripts/models_config.py
@@ -16,6 +16,15 @@ import os
 import tempfile
 from pathlib import Path
 
+# ADR-0042 (022 PKG-1): the single provider registry. Deliberately NOT a `routing_core`
+# import -- `provider_registry.py` has zero dependencies of its own (no `routing_core`, no
+# `models_config`), so importing it here at module scope costs nothing, unlike
+# `routing_core.catalog` (see `detect_subscriptions`/`auto_profile` below, both of which
+# still lazily import that ONE module for a real reason: any `routing_core` submodule
+# import runs `routing_core/__init__.py`, which loads `service.py`/`store.py` -- sqlite3,
+# subprocess -- at import time).
+import provider_registry
+
 ROOT = Path(__file__).resolve().parents[2]
 LANES = ("go-zen", "zen", "local")
 PERMISSION_PROFILES = ("guarded", "yolo")
@@ -38,7 +47,12 @@ ROUTING_PROVIDERS = {"openai-codex", "anthropic"}
 # tier/family inferred). Distinct from ROUTING_PROVIDERS on purpose — the curated
 # allowlist stays closed (immutable contract "probeable, never routable, by 012 alone");
 # this is the separate, opt-in surface that makes a discovered model routable.
-DISCOVERABLE_PROVIDERS = {"openai-codex", "anthropic", "opencode-zen", "opencode-go"}
+# ADR-0042 (022 PKG-1): derives from the single `provider_registry.PROVIDERS` registry now,
+# same four members as the pre-ADR-0042 literal. Order-insensitive on purpose (a `set`) --
+# `set_agents_app._MODEL_PREFERENCE_PROVIDERS` is the ORDERED sibling table, and it derives
+# from `provider_registry.PROVIDERS` directly rather than from this set, because a tuple
+# built from a set has no deterministic order across runs.
+DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)
 ROUTING_DEFAULTS = {
     "enabled_providers": ["openai-codex", "anthropic"], "xhigh_benchmarked": False,
     # ADR-0034 (019 PKG-1): "auto" is the new default -- discovered_providers derives
diff --git i/ai/scripts/routing_core/catalog.py w/ai/scripts/routing_core/catalog.py
index 2b784f1..16f990a 100644
--- i/ai/scripts/routing_core/catalog.py
+++ w/ai/scripts/routing_core/catalog.py
@@ -11,6 +11,13 @@ import time
 import tomllib
 from pathlib import Path
 from .domain import CatalogSnapshot, RoutingError, StaticRoute, _sorted_strings
+# ADR-0042 (022 PKG-1): the single provider registry every provider-keyed table in this file
+# derives from. Deliberately a plain top-level import, not a relative `.` one -- the module
+# lives OUTSIDE `routing_core` on purpose (see its own docstring and ADR-0042's "Dónde puede
+# vivir PROVIDERS" section): it has zero dependencies of its own, so importing it here costs
+# nothing this package's other two consumers (`models_config.py`, `set_agents_app.py`) don't
+# already pay to import it too.
+from provider_registry import PROVIDERS
 
 # ADR-0006 (AM-2): probe cache is a filtering-only optimization. TTL bounds staleness;
 # the invalidation key covers uid and the canonical catalog/routing config.
@@ -107,14 +114,19 @@ def canonical_model(provider: str, model: str) -> str:
     return CANONICAL_MODEL.get((provider, model), model)
 
 
+# ADR-0042 (022 PKG-1): both maps below DERIVE from the single `PROVIDERS` registry now
+# (`provider_registry.py`) -- before this ADR they were two independently hand-edited
+# literals, one of six provider-keyed tables that had to move in lockstep by hand across
+# this file, `models_config.py`, and `set_agents_app.py`. Values are unchanged from the
+# pre-ADR-0042 literals (PKG-1 AC-02 characterization test pins this).
+#
 # Catalog provider -> credential display text `_parse_opencode_auth` yields (map 1 of 2,
 # 012 AC-02). Used ONLY at the credential-set membership check. For the two pre-existing
 # pairs this string happens to coincide with the CLI argument below; for opencode-zen/
 # opencode-go it does not ("opencode zen"/"opencode go", two-token display text, vs the
 # single-token CLI ids in _OPENCODE_CLI_IDS) — re-measured live against `opencode auth
 # list --pure` (docs/specs/012-discovered-inventory/spec.md AC-02).
-_OPENCODE_PROVIDER_KEYS = {"openai-codex": "openai", "anthropic": "anthropic",
-                           "opencode-zen": "opencode zen", "opencode-go": "opencode go"}
+_OPENCODE_PROVIDER_KEYS = {provider: spec.opencode_auth_key for provider, spec in PROVIDERS.items()}
 # Catalog provider -> CLI argument for `opencode models <id>` AND the `<id>/<model>` line
 # prefix `_parse_opencode_models` strips (map 2 of 2, 012 AC-02). Independently
 # addressable from _OPENCODE_PROVIDER_KEYS above: using the CLI-id value at the
@@ -122,8 +134,7 @@ _OPENCODE_PROVIDER_KEYS = {"openai-codex": "openai", "anthropic": "anthropic",
 # single-token string and report the pair absent on every machine (the defect this
 # two-map split fixes; PI_MODEL_MAP is explicitly the wrong precedent here — it
 # translates a model name within one already-matched provider, a different axis).
-_OPENCODE_CLI_IDS = {"openai-codex": "openai", "anthropic": "anthropic",
-                     "opencode-zen": "opencode", "opencode-go": "opencode-go"}
+_OPENCODE_CLI_IDS = {provider: spec.opencode_cli_id for provider, spec in PROVIDERS.items()}
 # ADR-0034 (019 PKG-1) re-measurement, live, 2026-08-10 (opencode 1.18.14): `opencode
 # auth list --pure` shows 4 real credentials -- `opencode-go`, `openai`, `github-copilot`,
 # `opencode` (auth.json keys). `github-copilot` is authenticated but `opencode models
@@ -169,8 +180,8 @@ _PAIR_COMMANDS = {
 # Code OAuth), `opencode-go` stays subscription, `opencode-zen` stays the sole metered
 # entry. This is the day `billing_rank` (below) starts reading it -- the previous "no
 # weighting/selection logic reads this map yet" note is retired.
-PROVIDER_BILLING_KIND = {"openai-codex": "subscription", "anthropic": "subscription",
-                         "opencode-go": "subscription", "opencode-zen": "metered"}
+# ADR-0042 (022 PKG-1): derives from `PROVIDERS` now, same values as the pre-ADR-0042 literal.
+PROVIDER_BILLING_KIND = {provider: spec.billing_kind for provider, spec in PROVIDERS.items()}
 # ADR-0035 AC-12: the FREE-model convention is the `-free` suffix -- the exact same
 # pattern `inference._FAST_HINTS` already treats as a signal, never a second, competing
 # definition of "free" invented here.
@@ -197,23 +208,32 @@ def allowed_probe(runtime: str, provider: str) -> tuple[tuple[str, ...], ...] |
     return _PAIR_COMMANDS.get((runtime, provider))
 
 
+# ADR-0042 (022 PKG-1): provider -> `[catalog]` TOML key, derived from `PROVIDERS` (same
+# values as the pre-ADR-0042 inline literal `_configured_models` used to carry). Site (2) of
+# the five-site lockstep `_configured_models` documents below -- the other four sites (the
+# `[catalog]` table itself, `build_snapshot`'s own comprehension, and the two
+# `models_config.py` sites) are a DIFFERENT, wider concern (012 AC-04: whether a TOML key
+# exists and round-trips through `load_config`/`emit` at all) and are out of this package's
+# scope -- only this key map derives from the single provider registry.
+_CATALOG_KEYS = {provider: spec.catalog_key for provider, spec in PROVIDERS.items()}
+
+
 def _configured_models(config: dict, provider: str) -> set[str]:
     # 012 AC-04: same five-site lockstep as the pre-existing two providers (corrected
     # 012 repair F-04: the original comment undercounted "three sites" — this package
     # added two more in models_config.py) — this key map is site (2) of 5 that must move
-    # together with: (1) models.toml's [catalog] table itself; (2) this key map; (3)
-    # build_snapshot's own `configured_models` comprehension (catalog.py, below); (4)
-    # models_config.load_config's optional-key validation loop for the same two TOML
-    # keys; (5) models_config.emit's preservation loop for the same two keys (without
-    # which a future `./setup-models.sh` write would silently drop the allowlist). Sites
-    # (4)/(5) exist because [catalog] has no closed-schema check the way [routing]/
-    # [permissions] do — an allowlist key present in the TOML but absent from both
-    # models_config.py sites is read here (fine) but never survives a re-emit (data
-    # loss). Extending only site (1) leaves the other providers' allowlists unreachable
-    # from code; extending only (1)-(3) without (4)/(5) leaves them reachable but
-    # silently unpersisted on the next wizard run.
-    key = {"openai-codex": "codex", "anthropic": "claude",
-           "opencode-zen": "opencode_zen", "opencode-go": "opencode_go"}.get(provider)
+    # together with: (1) models.toml's [catalog] table itself; (2) this key map (now
+    # `_CATALOG_KEYS` above, ADR-0042); (3) build_snapshot's own `configured_models`
+    # comprehension (catalog.py, below); (4) models_config.load_config's optional-key
+    # validation loop for the same two TOML keys; (5) models_config.emit's preservation
+    # loop for the same two keys (without which a future `./setup-models.sh` write would
+    # silently drop the allowlist). Sites (4)/(5) exist because [catalog] has no
+    # closed-schema check the way [routing]/[permissions] do — an allowlist key present in
+    # the TOML but absent from both models_config.py sites is read here (fine) but never
+    # survives a re-emit (data loss). Extending only site (1) leaves the other providers'
+    # allowlists unreachable from code; extending only (1)-(3) without (4)/(5) leaves them
+    # reachable but silently unpersisted on the next wizard run.
+    key = _CATALOG_KEYS.get(provider)
     models = config.get("catalog", {}).get(key, ()) if key else ()
     if not isinstance(models, (list, tuple)):
         return set()
diff --git i/ai/scripts/set_agents_app.py w/ai/scripts/set_agents_app.py
index 33cac42..8714593 100644
--- i/ai/scripts/set_agents_app.py
+++ w/ai/scripts/set_agents_app.py
@@ -30,6 +30,7 @@ sys.path.insert(0, str(Path(__file__).resolve().parent))
 sys.modules.setdefault("set_agents_app", sys.modules[__name__])
 import coord_policy
 import models_config
+import provider_registry
 import routing
 import set_agents_spawn
 import tui
@@ -89,9 +90,20 @@ def _project_root_or_harness() -> Path:
 MODEL_PREFERENCE_PATH = STATE_DIR / "model-preference.toml"
 
 # The closed, four-provider universe `_PAIR_COMMANDS` already probes
-# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
-# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
-_MODEL_PREFERENCE_PROVIDERS = ("openai-codex", "anthropic", "opencode-zen", "opencode-go")
+# (`routing_core/catalog.py:165-172`). ADR-0042 (022 PKG-1) supersedes this table's own
+# ORIGINAL "defined independently here, never importing" framing -- that framing was wider
+# than the reason it named: the AC-06 non-goal it cites (014-model-preference-policy,
+# test_ac06_no_provider_billing_kind_reference_in_new_code) is specifically about never
+# letting the catalog module's own billing-kind classification table decide provider
+# membership here, and that narrower non-goal still holds -- this tuple's derivation below
+# never reads that table (nor even names it -- see the test above, unchanged by this
+# package). What changed is provider IDENTITY: this tuple now derives from the single
+# `provider_registry.PROVIDERS` registry every provider-keyed table in the harness shares,
+# instead of being a second hand-maintained copy of the same four provider ids. Order is a
+# real contract (surfaces in pin/preference validation errors) --
+# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
+# because that sibling table is an unordered `set`.
+_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
 _MODEL_PREFERENCE_CLASSES = ("decision", "grunt", "build")
 # AC-02's inertness note: the six roles with real, live effect today (`### Honest scope`,
 # spec) -- every other in-scope role, and the whole `decision` class, is genuinely inert.
@@ -112,7 +124,9 @@ def _model_preference_die(message):
 
 def _effective_preference_providers():
     """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
-    -- byte-identical to `models_config.DISCOVERABLE_PROVIDERS`) UNION whatever the
+    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
+    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
+    purpose, ordered tuple vs. unordered set, never in membership) UNION whatever the
     CURRENT effective snapshot reports as routable, when that snapshot is cheaply
     resolvable -- a live probe, never network/credential material. Deliberately never
     called from `load_model_preference`/`load_model_pin` (the boot path, `_read_model_
@@ -129,7 +143,10 @@ def _effective_preference_providers():
     which today it structurally cannot: every provider it can produce comes from
     `routing_core.catalog._PAIR_COMMANDS`'s own audited provider set, and that set is
     pinned equal to `models_config.DISCOVERABLE_PROVIDERS` (== `_MODEL_PREFERENCE_
-    PROVIDERS`, AC-10's lockstep test). So the probe below is skipped whenever the
+    PROVIDERS`, guarded for real since ADR-0042 -- both are derived from
+    `provider_registry.PROVIDERS`, and `test_adr0042_ac01b_...` in tests/test_routing.py
+    compares this tuple against `_PAIR_COMMANDS`'s own provider set directly, never a
+    second hardcoded literal). So the probe below is skipped whenever the
     base set already covers `DISCOVERABLE_PROVIDERS` -- a cheap, exact short-circuit
     that avoids paying a subprocess probe (each `_PAIR_COMMANDS` entry can block up to
     20s) on every `--model-pin-set`/`--model-preference-set` call for a union that
diff --git i/docs/adr/README.md w/docs/adr/README.md
index 0ab282c..9b060fa 100644
--- i/docs/adr/README.md
+++ w/docs/adr/README.md
@@ -46,3 +46,4 @@
 | [0039](0039-reopen-directed-counter-reset.md) | `reopen` resetea exactamente el contador que lo bloqueó, estructurado en el blocker, nunca inferido del texto | Accepted | 2026-08-11 | — | — |
 | [0040](0040-honest-digest-shared-liveness-predicate.md) | Tablero honesto: `feature_is_live` un solo predicado en `model.py` (`final_state == "DONE"` exacto), umbral de estancamiento como constante única, sección `## Necesita tu decisión` con tope de dos menciones por bloqueada | Accepted | 2026-08-12 | — | — |
 | [0041](0041-build-check-verifies-global.md) | `build.sh --check` compara `Global/` de verdad contra una generación forzada a `--profile go-zen`, distingue self-scaffold de árbol generado en su salida, y fija por escrito que `--check` corre siempre antes que la suite cuando ambos son evidencia de gate | Accepted | 2026-08-12 | — | — |
+| [0042](0042-provider-registry-single-source.md) | Un único `PROVIDERS: dict[str, ProviderSpec]` (`ai/scripts/provider_registry.py`) del que se derivan las siete tablas de proveedores hoy en lockstep manual; corrige la afirmación de ADR-0034 "Rejected alternatives" sobre Copilot con la medición | Accepted | 2026-08-12 | Corrige en parte 0034 (afirmación de Rejected alternatives sobre Copilot, no-goal de la spec 022 — 0034 no se edita) | — |
diff --git i/tests/test_routing.py w/tests/test_routing.py
index 68e76da..fd54acf 100644
--- i/tests/test_routing.py
+++ w/tests/test_routing.py
@@ -20,6 +20,7 @@ ROOT=Path(__file__).resolve().parents[1]
 sys.path.insert(0,str(ROOT/"ai/scripts"))
 import claude_code_spawn
 import models_config
+import provider_registry
 import routing
 import set_agents_app
 import set_agents_spawn
@@ -4002,6 +4003,65 @@ class RoutingTests(unittest.TestCase):
         bogus_inventory = {("opencode", "github-copilot"): {"some-copilot-model"}}
         self.assertEqual(routing_catalog.resolve_discovered_providers(config, bogus_inventory), ())
 
+    # ------------------------------------------------- ADR-0042 (022 PKG-1): single provider
+    # registry -- `provider_registry.PROVIDERS` is now the one source every provider-keyed
+    # table below derives from, replacing six independently hand-edited literal copies.
+
+    def test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals(self):
+        # AC-02 characterization: every literal pasted here is the EXACT pre-ADR-0042 value
+        # (copied from the removed literals, not imported from provider_registry or derived
+        # in this test) -- proves the refactor changed plumbing, never values. Order matters
+        # only for the tuple (AC-01's provider-registry insertion order contract); the dicts
+        # and the set compare by value, so their literal order below is not significant.
+        self.assertEqual(list(provider_registry.PROVIDERS),
+                         ["openai-codex", "anthropic", "opencode-zen", "opencode-go"])
+        self.assertEqual(routing_catalog._OPENCODE_PROVIDER_KEYS, {
+            "openai-codex": "openai", "anthropic": "anthropic",
+            "opencode-zen": "opencode zen", "opencode-go": "opencode go",
+        })
+        self.assertEqual(routing_catalog._OPENCODE_CLI_IDS, {
+            "openai-codex": "openai", "anthropic": "anthropic",
+            "opencode-zen": "opencode", "opencode-go": "opencode-go",
+        })
+        self.assertEqual(routing_catalog.PROVIDER_BILLING_KIND, {
+            "openai-codex": "subscription", "anthropic": "subscription",
+            "opencode-go": "subscription", "opencode-zen": "metered",
+        })
+        # AC-01's fifth symbol: the key map that used to live INLINE inside
+        # `_configured_models` (a local dict literal, unreachable from outside the function)
+        # is now the module-level `_CATALOG_KEYS`, itself derived from `PROVIDERS`.
+        self.assertEqual(routing_catalog._CATALOG_KEYS, {
+            "openai-codex": "codex", "anthropic": "claude",
+            "opencode-zen": "opencode_zen", "opencode-go": "opencode_go",
+        })
+        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS,
+                         {"openai-codex", "anthropic", "opencode-zen", "opencode-go"})
+        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
+                         ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
+
+    def test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source(self):
+        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
+        # lived at test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_
+        # not_just_the_constant (below, :3965 pre-refactor line count) -- inside a
+        # `mock.patch` block whose real purpose is testing the live-probe degrade path, and
+        # it compared against ANOTHER hardcoded literal, never against the audited source.
+        # That test is untouched by this package (still passes, proven by the full-suite
+        # gate) and still is not the guard -- THIS is the guard: two independent
+        # cross-checks against the real source, neither of them a second copy-pasted tuple.
+        #
+        # Cross-check 1: same pattern as test_adr0034_ac10_discoverable_providers_lockstep_
+        # guard above (which only ever covered DISCOVERABLE_PROVIDERS, never this tuple,
+        # despite set_agents_app.py's own comment claiming otherwise pre-ADR-0042) --
+        # membership against `_PAIR_COMMANDS`'s own audited provider set.
+        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
+        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
+        # Cross-check 2: against the ordered registry directly -- this is what makes a
+        # FUTURE regression (someone re-introducing a hand-maintained tuple in
+        # set_agents_app.py instead of deriving it) fail loudly the moment `PROVIDERS`
+        # changes without a matching edit here, rather than silently agreeing with a second,
+        # equally-stale literal (the exact defect this package exists to close).
+        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
+
     # --------------------------------------------- 014-model-preference-policy (AC-01..09)
 
     _MP_TIERED_ROLES = frozenset({"debugger", "delta-reviewer", "finding-verifier",
diff --git 1/ai/scripts/provider_registry.py 2/ai/scripts/provider_registry.py
new file mode 100644
index 0000000..243b51d
--- /dev/null
+++ 2/ai/scripts/provider_registry.py
@@ -0,0 +1,70 @@
+"""ADR-0042 (022 PKG-1): the single audited provider registry.
+
+`PROVIDERS` is the ONE source `routing_core/catalog.py`, `models_config.py`, and
+`set_agents_app.py` derive their provider-keyed tables from -- before this module existed,
+those three files hand-maintained SIX independently-editable copies of the same four-provider
+universe (`_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, the key map
+inside `_configured_models`, `DISCOVERABLE_PROVIDERS`, `_MODEL_PREFERENCE_PROVIDERS`), synced
+only by human discipline. Adding a provider now means adding one `ProviderSpec` row here.
+
+Deliberately outside the `routing_core` package and importing NEITHER `routing_core` NOR
+`models_config`: `routing_core/__init__.py` unconditionally imports `.service`/`.store`
+(sqlite3, subprocess, ...) on ANY submodule import, and `models_config.py` documents (at
+`detect_subscriptions`/`auto_profile`) that it deliberately avoids a `routing_core` dependency
+at module load. A module with zero imports beyond `dataclasses` is importable at module scope
+by all three consumers without paying either cost -- see ADR-0042 for the measured alternatives
+this rejected.
+
+The dict's INSERTION ORDER is a real contract, not incidental: `set_agents_app.
+_MODEL_PREFERENCE_PROVIDERS` derives its ordered tuple from `tuple(PROVIDERS)`, and that order
+(`openai-codex, anthropic, opencode-zen, opencode-go`) surfaces in user-facing validation
+messages. Never reorder this dict without checking that consumer.
+"""
+
+from __future__ import annotations
+
+import dataclasses
+
+
+@dataclasses.dataclass(frozen=True)
+class ProviderSpec:
+    """One audited provider's identity across every axis this registry replaces.
+
+    `opencode_auth_key`: the credential display text `opencode auth list --pure` prints for
+    this provider (map 1 of 2, 012 AC-02) -- used ONLY at the credential-set membership check.
+    `opencode_cli_id`: the CLI argument for `opencode models <id>` AND the `<id>/<model>` line
+    prefix `_parse_opencode_models` strips (map 2 of 2) -- independently addressable from
+    `opencode_auth_key` (012 AC-02: the two coincide for `openai-codex`/`anthropic` but diverge
+    for the OpenCode-lane providers).
+    `billing_kind`: `"subscription"` or `"metered"` (012 AC-08 / ADR-0035), curated per
+    provider, never a `routes.v1.toml` row field.
+    `catalog_key`: the `[catalog]` table key in `models.toml` that curates this provider's
+    model allowlist (012 AC-04).
+    """
+
+    opencode_auth_key: str
+    opencode_cli_id: str
+    billing_kind: str
+    catalog_key: str
+
+
+# Order is a contract -- see module docstring. Values are unchanged from the pre-ADR-0042
+# literals (PKG-1 AC-02 characterization test pins this).
+PROVIDERS: dict[str, ProviderSpec] = {
+    "openai-codex": ProviderSpec(
+        opencode_auth_key="openai", opencode_cli_id="openai",
+        billing_kind="subscription", catalog_key="codex",
+    ),
+    "anthropic": ProviderSpec(
+        opencode_auth_key="anthropic", opencode_cli_id="anthropic",
+        billing_kind="subscription", catalog_key="claude",
+    ),
+    "opencode-zen": ProviderSpec(
+        opencode_auth_key="opencode zen", opencode_cli_id="opencode",
+        billing_kind="metered", catalog_key="opencode_zen",
+    ),
+    "opencode-go": ProviderSpec(
+        opencode_auth_key="opencode go", opencode_cli_id="opencode-go",
+        billing_kind="subscription", catalog_key="opencode_go",
+    ),
+}
diff --git 1/docs/adr/0042-provider-registry-single-source.md 2/docs/adr/0042-provider-registry-single-source.md
new file mode 100644
index 0000000..b03b513
--- /dev/null
+++ 2/docs/adr/0042-provider-registry-single-source.md
@@ -0,0 +1,169 @@
+# ADR-0042 — Un único `PROVIDERS` del que se derivan las siete tablas de proveedores
+
+- Estado: Accepted (2026-08-12). Feature 022-disponibilidad-real, PKG-1 (`P1-registro-de-proveedores`).
+  Corrige en parte una afirmación de ADR-0034 (Rejected alternatives) — no lo supersede: ADR-0034 sigue
+  vigente en todo lo demás.
+
+## Contexto
+
+Medición en vivo sobre el estado actual del repo (antes de este paquete), `grep -rn` de cada símbolo:
+
+| Símbolo | Ubicación | Qué codifica |
+|---|---|---|
+| `_OPENCODE_PROVIDER_KEYS` | `routing_core/catalog.py:116-117` | provider → texto de credencial que `opencode auth list --pure` imprime |
+| `_OPENCODE_CLI_IDS` | `catalog.py:125-126` | provider → argumento CLI de `opencode models <id>` |
+| `_PAIR_COMMANDS` | `catalog.py:154-161` | el universo auditado runtime×provider; su mitad `opencode` **ya** deriva de `_OPENCODE_CLI_IDS` (comprehension, no literal) desde 012/F-05 |
+| `PROVIDER_BILLING_KIND` | `catalog.py:172-173` | provider → `subscription`\|`metered` |
+| key map de `_configured_models` | `catalog.py:215-216` (dentro del cuerpo de la función) | provider → clave de `[catalog]` en `models.toml` |
+| `DISCOVERABLE_PROVIDERS` | `models_config.py:41` | el `set` de providers elegibles para `"auto"` (ADR-0034) |
+| `_MODEL_PREFERENCE_PROVIDERS` | `set_agents_app.py:94` | la `tuple` **ordenada** que valida pins/preferencias de modelo (ADR-0018/ADR-0034 AC-09) |
+
+Siete símbolos, medidos. **Seis** de ellos son duplicados *manuales* — hoy, agregar un proveedor
+exige tocar las seis tablas a mano y nada las liga entre sí salvo la disciplina humana. El séptimo,
+`_PAIR_COMMANDS`, ya era una excepción parcial: su mitad `opencode` deriva de `_OPENCODE_CLI_IDS`
+desde el repair F-05 de la feature 012 (`catalog.py:149-152`), así que no suma un duplicado nuevo —
+pero sigue siendo uno de los siete lugares que un proveedor nuevo toca, porque sus dos pares no-opencode
+(`codex`/`openai-codex`, `claude-code`/`anthropic`) usan los ids de provider como claves literales de
+dict. Esto resuelve la discrepancia de conteo entre la spec (que llama "seis" en la narrativa de
+`docs/specs/022-disponibilidad-real/spec.md` — "de seis entradas en lockstep manual a una fila" — y
+sin embargo nombra los siete en la tabla del context pack): **"seis" cuenta los duplicados
+verdaderamente manuales; "siete" cuenta todos los símbolos que este paquete mide y dejan de poder
+divergir entre sí**, uno de los cuales (`_PAIR_COMMANDS`) ya no era manual antes de este ADR.
+
+### La guarda que decía proteger el sexto duplicado no protegía nada
+
+`tests/test_routing.py:3965` (dentro de `test_adr0034_ac09_write_cli_validates_pins_against_the_
+effective_set_not_just_the_constant`, cuyo propósito real es otro: probar que el path de escritura
+degrada al set base cuando el probe vivo falla) hace
+`self.assertEqual(app._MODEL_PREFERENCE_PROVIDERS, ("openai-codex", "anthropic", "opencode-zen",
+"opencode-go"))` — compara contra un literal hardcodeado DENTRO de un `mock.patch` de otro test, no
+contra la fuente real. `tests/test_routing.py:3191-3200` (`test_adr0034_ac10_discoverable_providers_
+lockstep_guard`) sí es una guarda real, pero solo cubre `DISCOVERABLE_PROVIDERS` — nunca menciona
+`_MODEL_PREFERENCE_PROVIDERS`, a pesar de que el comentario de `set_agents_app.py:131-133` afirma
+que "AC-10's lockstep test" la cubre también. Es falso: la guarda falta exactamente en el símbolo
+que vive en otro archivo.
+
+### El no-goal de `set_agents_app.py:90-94` es más angosto que su propia redacción
+
+Texto literal (antes de este paquete):
+
+```
+# The closed, four-provider universe `_PAIR_COMMANDS` already probes
+# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
+# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
+```
+
+La razón nombrada explícitamente (`AC-06 non-goal`, ADR-0018/019 PKG-2 — nunca dejar que la
+clasificación *billing-kind* decida qué proveedores son válidos para pins/preferencias, un eje sin
+relación) es real y **no cambia** con este paquete: `_MODEL_PREFERENCE_PROVIDERS` sigue sin leer
+`PROVIDER_BILLING_KIND` en ningún punto de su derivación. Lo que la redacción generaliza de más es
+"never importing" — leído literalmente, prohíbe cualquier import compartido con el módulo de
+catálogo, no solo el de billing-kind. Y `:113-114` afirmaba además que el valor es "byte-identical
+to `models_config.DISCOVERABLE_PROVIDERS`" en un sentido que nunca fue literalmente cierto: uno es
+`set` (sin orden) y el otro una `tuple` ordenada — los VALORES coinciden, el tipo no.
+
+### Dónde puede vivir `PROVIDERS` — restricción dura medida
+
+- `catalog.py` no importa `models_config` en ningún punto (`grep -rn "models_config" routing_core/`
+  solo encuentra comentarios).
+- `models_config.py` importa `routing_core.catalog` **solo de forma perezosa, adentro de
+  funciones** (`detect_subscriptions:257`, `auto_profile:275`), con el docstring explícito
+  "Lazy import (no routing_core dependency at module load)".
+- Medido: `routing_core/__init__.py` hace `from .service import RoutingService` y `from .store
+  import RoutingStore` de forma incondicional — es decir, importar CUALQUIER submódulo de
+  `routing_core` (aunque sea uno hoja, sin relación con service/store) ejecuta primero ese
+  `__init__.py`, que carga `sqlite3`, `subprocess` (vía `catalog.py`, que `service.py` también
+  importa) y el resto de la maquinaria de rutina. Poner `PROVIDERS` dentro de `routing_core`
+  (aunque fuera un submódulo nuevo, hoja, sin relación con service/store) y hacer que
+  `models_config.py` lo importe a nivel de módulo paga exactamente ese costo en cada `import
+  models_config` — el mismo costo que la disciplina de `:250-251` existe para evitar.
+
+## Decisión
+
+1. **`PROVIDERS: dict[str, ProviderSpec]` vive en un módulo nuevo, neutro, fuera de
+   `routing_core`**: `ai/scripts/provider_registry.py`. Cero imports de `models_config` ni de
+   `routing_core` — solo `dataclasses` de la librería estándar. Es importable a nivel de módulo por
+   los tres consumidores sin pagar el costo de `routing_core/__init__.py` ni introducir una
+   dependencia de `routing_core` hacia el módulo de configuración de aplicación.
+   `ProviderSpec` tiene cuatro campos, uno por eje que hoy vive disperso:
+   `opencode_auth_key` (map 1, `_OPENCODE_PROVIDER_KEYS`), `opencode_cli_id` (map 2,
+   `_OPENCODE_CLI_IDS`), `billing_kind` (`PROVIDER_BILLING_KIND`), `catalog_key` (la clave de
+   `[catalog]` que hoy vive inline en `_configured_models`). El orden de inserción del dict es
+   **el contrato de orden** para `_MODEL_PREFERENCE_PROVIDERS`:
+   `("openai-codex", "anthropic", "opencode-zen", "opencode-go")`, preservado explícitamente
+   porque `tuple(dict)` sí respeta orden de inserción, mientras que `DISCOVERABLE_PROVIDERS`
+   (un `set`) nunca podría haberlo dado de forma determinística.
+2. **`catalog.py` deriva sus cuatro tablas de `PROVIDERS`** (`_OPENCODE_PROVIDER_KEYS`,
+   `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, y un nuevo `_CATALOG_KEYS` module-level que
+   reemplaza el dict inline de `_configured_models`) — mismos valores, ahora una sola fuente.
+   `_PAIR_COMMANDS` no cambia de código: su mitad `opencode` ya derivaba de `_OPENCODE_CLI_IDS`
+   (que ahora, transitivamente, deriva de `PROVIDERS`).
+3. **`models_config.DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)`** — mismo valor,
+   ahora derivado, tipo `set` sin cambios (nada que lo consume espera orden).
+4. **`set_agents_app._MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)`** — mismo
+   valor, mismo orden, ahora derivado en vez de una segunda tupla hardcodeada.
+5. **Guarda real nueva** (`tests/test_routing.py`, nueva función,
+   `test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source`): compara
+   `set_agents_app._MODEL_PREFERENCE_PROVIDERS` contra `_PAIR_COMMANDS`'s propio set de providers
+   (`{provider for _, provider in _PAIR_COMMANDS}`, el mismo universo auditado que la guarda de
+   `DISCOVERABLE_PROVIDERS` ya usa) **y** contra `tuple(provider_registry.PROVIDERS)` — la fuente
+   real, nunca un literal hardcodeado en otro test. `tests/test_routing.py:3965` no se toca: sigue
+   pasando (misma tupla, ahora derivada en vez de literal) como prueba incidental adicional, no
+   como la guarda.
+6. **Se supersede explícitamente la redacción general de `set_agents_app.py:90-94`** ("defined
+   independently here, never importing or referencing") por la más angosta que este ADR confirma:
+   `_MODEL_PREFERENCE_PROVIDERS` puede (y ahora sí) compartir fuente con el resto de las tablas de
+   proveedores, siempre que esa fuente nunca sea la clasificación billing-kind. El comentario se
+   reescribe para decir eso, no lo contrario. La afirmación de `:113-114` ("byte-identical to
+   `models_config.DISCOVERABLE_PROVIDERS`") se corrige a "mismos valores, misma fuente, tipos
+   distintos por diseño (tupla ordenada vs. set)".
+7. **Corrige `ADR-0034` "Rejected alternatives"** (`:124-126`): esa sección afirma que un futuro
+   `opencode models --refresh` que liste Copilot "activará la adopción automática sin tocar
+   código". Es inexacto incluso hoy: sin este paquete, activar Copilot habría exigido tocar las
+   seis tablas manuales medidas arriba. Con este paquete, exige exactamente **una** fila nueva en
+   `PROVIDERS` (`provider_registry.py`) — de la que las seis tablas se derivan solas — pero **no
+   es cero-touch**: sigue habiendo un ProviderSpec por escribir, con sus cuatro campos medidos a
+   mano (nunca inferidos), y `_PAIR_COMMANDS`, `routes.v1.toml` y el resto del universo auditado
+   igual exigen su propia validación explícita antes de que Copilot sea *routable* (fuera de
+   alcance de este paquete: eso es "probeable", no "routable", ADR-0029). No se edita ADR-0034
+   retroactivamente — esta sección deja la corrección por escrito y `docs/adr/README.md` marca la
+   relación.
+
+## Alternativas rechazadas
+
+- **`PROVIDERS` dentro de `routing_core/catalog.py` (o un submódulo nuevo de `routing_core`)**:
+  rechazado por la medición de arriba — cualquier import de un submódulo de `routing_core` ejecuta
+  `routing_core/__init__.py`, que carga `service.py`/`store.py` (sqlite3, subprocess) — exactamente
+  lo que `models_config.py:250-251`/`:275` existen para evitar en su import a nivel de módulo. Esto
+  no es una preferencia estética: es la razón medida y escrita que la disciplina ya documentaba
+  antes de este paquete.
+- **`PROVIDERS` dentro de `models_config.py`**: rechazado porque `routing_core` se declara
+  explícitamente "no CLI imports" (`routing_core/__init__.py:1`) y `models_config.py` es, en la
+  práctica, un módulo de aplicación (lee `models.toml` de disco, `die()` en error) — hacer que
+  `catalog.py` importe `models_config` invierte la dirección de dependencia que el propio paquete
+  declara, y no ganaría nada frente a un módulo neutro.
+- **Derivar `_MODEL_PREFERENCE_PROVIDERS` de `models_config.DISCOVERABLE_PROVIDERS`** en vez de la
+  fuente común: rechazado porque `DISCOVERABLE_PROVIDERS` es un `set` — derivar una tupla ordenada
+  de un `set` produce orden no determinístico entre corridas, y el orden de hoy
+  (`openai-codex, anthropic, opencode-zen, opencode-go`) llega a superficie de usuario (mensajes de
+  validación de pins).
+- **Editar `_PAIR_COMMANDS` para que sus dos pares no-opencode también deriven "más" de
+  `PROVIDERS`**: fuera de alcance — ya deriva lo que tenía que derivar (la mitad opencode, desde
+  012/F-05) y no es un duplicado manual; tocar su forma es el tipo de refactor oportunista que este
+  paquete evita (`catalog.py` no se reorganiza más allá de las cuatro tablas nombradas en AC-01).
+
+## Consecuencias
+
+- Agregar un proveedor nuevo pasa de tocar seis tablas manuales a agregar una fila `ProviderSpec` a
+  `PROVIDERS` — las cuatro tablas de `catalog.py`, `DISCOVERABLE_PROVIDERS` y
+  `_MODEL_PREFERENCE_PROVIDERS` se actualizan solas. `_PAIR_COMMANDS` sigue necesitando sus propios
+  argv auditados a mano para pares nuevos runtime×provider (nunca inferidos — fuera del alcance de
+  "derivar", que es sobre identidad de proveedor, no sobre comandos de probe).
+- Ningún comportamiento cambia: los siete valores derivados son byte-idénticos a los literales
+  previos (test de caracterización, PKG-1 AC-02).
+- `set_agents_app.py:90-94`/`:113-114` quedan reescritos para no contradecir al código.
+- `ADR-0034` no se edita; esta sección corrige su afirmación con la medición.
+
+## Evidencia
+
+`docs/specs/022-disponibilidad-real/evidence/P1-implementer.md`.

codex
La inspección está mostrando una distinción importante: el test nuevo compara consumidores entre sí y contra el registro en tiempo de ejecución. Ahora voy a ejecutar la prueba dirigida en verde y después aplicar la “mordida” solicitada sobre la fuente, con backup/restauración por `cp`, para comprobar si realmente detecta una fuente rota.
exec
/bin/bash -lc 'ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v' in /home/federico/SET-AGENTES
 succeeded in 108ms:
test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals) ... ok
test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 2 tests in 0.004s

OK

exec
/bin/bash -lc "cp ai/scripts/provider_registry.py /tmp/p1-provider_registry.py.orig
perl -0pi -e 's/\"opencode-go\": ProviderSpec\\(/\"opencode-go-broken\": ProviderSpec(/' ai/scripts/provider_registry.py
rg -n 'opencode-go|opencode-go-broken' ai/scripts/provider_registry.py" in /home/federico/SET-AGENTES
 succeeded in 0ms:
20:(`openai-codex, anthropic, opencode-zen, opencode-go`) surfaces in user-facing validation
66:    "opencode-go-broken": ProviderSpec(
67:        opencode_auth_key="opencode go", opencode_cli_id="opencode-go",

exec
/bin/bash -lc 'ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v' in /home/federico/SET-AGENTES
 succeeded in 80ms:
test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.002s

OK

exec
/bin/bash -lc 'cp /tmp/p1-provider_registry.py.orig ai/scripts/provider_registry.py
diff /tmp/p1-provider_registry.py.orig ai/scripts/provider_registry.py
ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v' in /home/federico/SET-AGENTES
 succeeded in 87ms:
test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.002s

OK

exec
/bin/bash -lc "nl -ba ai/scripts/set_agents_app.py | sed -n '120,150p'
rg -n -C 1 'defined independently|never importing|byte-identical to|same VALUES|same values|PROVIDER_BILLING_KIND' ai/scripts/set_agents_app.py ai/scripts/models_config.py ai/scripts/routing_core/catalog.py ai/scripts/provider_registry.py docs/adr/0042-provider-registry-single-source.md
rg -n 'provider_registry\\.PROVIDERS|from provider_registry import PROVIDERS|tuple\\(PROVIDERS\\)|set\\(provider_registry\\.PROVIDERS\\)|_MODEL_PREFERENCE_PROVIDERS' ai/scripts tests/test_routing.py --glob '*.py'
nl -ba tests/test_routing.py | sed -n '3178,3210p'
nl -ba tests/test_routing.py | sed -n '4006,4065p'
git status --short
git diff --no-ext-diff --check" in /home/federico/SET-AGENTES
 succeeded in 0ms:
   120	
   121	def _model_preference_die(message):
   122	    raise ModelPreferenceError(message)
   123	
   124	
   125	def _effective_preference_providers():
   126	    """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
   127	    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
   128	    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
   129	    purpose, ordered tuple vs. unordered set, never in membership) UNION whatever the
   130	    CURRENT effective snapshot reports as routable, when that snapshot is cheaply
   131	    resolvable -- a live probe, never network/credential material. Deliberately never
   132	    called from `load_model_preference`/`load_model_pin` (the boot path, `_read_model_
   133	    preference_raw`'s callers): probing there would make a plain `models.toml` read
   134	    depend on a live opencode probe, which the spec explicitly forbids. Only the WRITE
   135	    CLI paths below (`cmd_model_preference_set`, `cmd_model_pin_set`, ...) -- explicit,
   136	    interactive user actions where the probe's cost is acceptable -- use this. ANY
   137	    failure (missing routing_core state, a broken probe, an unreadable models.toml)
   138	    degrades silently to the base set alone, never a crash: a wider live set is a
   139	    convenience, never a requirement for pins/preferences to keep working.
   140	
   141	    F-05 (P1 repair): the union only ever has an observable effect on a day
   142	    `resolve_discovered_providers` can return a provider OUTSIDE the base set --
   143	    which today it structurally cannot: every provider it can produce comes from
   144	    `routing_core.catalog._PAIR_COMMANDS`'s own audited provider set, and that set is
   145	    pinned equal to `models_config.DISCOVERABLE_PROVIDERS` (== `_MODEL_PREFERENCE_
   146	    PROVIDERS`, guarded for real since ADR-0042 -- both are derived from
   147	    `provider_registry.PROVIDERS`, and `test_adr0042_ac01b_...` in tests/test_routing.py
   148	    compares this tuple against `_PAIR_COMMANDS`'s own provider set directly, never a
   149	    second hardcoded literal). So the probe below is skipped whenever the
   150	    base set already covers `DISCOVERABLE_PROVIDERS` -- a cheap, exact short-circuit
docs/adr/0042-provider-registry-single-source.md-15-| `_PAIR_COMMANDS` | `catalog.py:154-161` | el universo auditado runtime×provider; su mitad `opencode` **ya** deriva de `_OPENCODE_CLI_IDS` (comprehension, no literal) desde 012/F-05 |
docs/adr/0042-provider-registry-single-source.md:16:| `PROVIDER_BILLING_KIND` | `catalog.py:172-173` | provider → `subscription`\|`metered` |
docs/adr/0042-provider-registry-single-source.md-17-| key map de `_configured_models` | `catalog.py:215-216` (dentro del cuerpo de la función) | provider → clave de `[catalog]` en `models.toml` |
--
docs/adr/0042-provider-registry-single-source.md-51-# The closed, four-provider universe `_PAIR_COMMANDS` already probes
docs/adr/0042-provider-registry-single-source.md:52:# (`routing_core/catalog.py:133-140`) -- defined independently here, never importing or
docs/adr/0042-provider-registry-single-source.md-53-# referencing the catalog module's own billing-kind classification table (AC-06 non-goal).
--
docs/adr/0042-provider-registry-single-source.md-58-relación) es real y **no cambia** con este paquete: `_MODEL_PREFERENCE_PROVIDERS` sigue sin leer
docs/adr/0042-provider-registry-single-source.md:59:`PROVIDER_BILLING_KIND` en ningún punto de su derivación. Lo que la redacción generaliza de más es
docs/adr/0042-provider-registry-single-source.md:60:"never importing" — leído literalmente, prohíbe cualquier import compartido con el módulo de
docs/adr/0042-provider-registry-single-source.md-61-catálogo, no solo el de billing-kind. Y `:113-114` afirmaba además que el valor es "byte-identical
--
docs/adr/0042-provider-registry-single-source.md-89-   `opencode_auth_key` (map 1, `_OPENCODE_PROVIDER_KEYS`), `opencode_cli_id` (map 2,
docs/adr/0042-provider-registry-single-source.md:90:   `_OPENCODE_CLI_IDS`), `billing_kind` (`PROVIDER_BILLING_KIND`), `catalog_key` (la clave de
docs/adr/0042-provider-registry-single-source.md-91-   `[catalog]` que hoy vive inline en `_configured_models`). El orden de inserción del dict es
--
docs/adr/0042-provider-registry-single-source.md-96-2. **`catalog.py` deriva sus cuatro tablas de `PROVIDERS`** (`_OPENCODE_PROVIDER_KEYS`,
docs/adr/0042-provider-registry-single-source.md:97:   `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, y un nuevo `_CATALOG_KEYS` module-level que
docs/adr/0042-provider-registry-single-source.md-98-   reemplaza el dict inline de `_configured_models`) — mismos valores, ahora una sola fuente.
--
docs/adr/0042-provider-registry-single-source.md-113-6. **Se supersede explícitamente la redacción general de `set_agents_app.py:90-94`** ("defined
docs/adr/0042-provider-registry-single-source.md:114:   independently here, never importing or referencing") por la más angosta que este ADR confirma:
docs/adr/0042-provider-registry-single-source.md-115-   `_MODEL_PREFERENCE_PROVIDERS` puede (y ahora sí) compartir fuente con el resto de las tablas de
docs/adr/0042-provider-registry-single-source.md-116-   proveedores, siempre que esa fuente nunca sea la clasificación billing-kind. El comentario se
docs/adr/0042-provider-registry-single-source.md:117:   reescribe para decir eso, no lo contrario. La afirmación de `:113-114` ("byte-identical to
docs/adr/0042-provider-registry-single-source.md-118-   `models_config.DISCOVERABLE_PROVIDERS`") se corrige a "mismos valores, misma fuente, tipos
--
ai/scripts/provider_registry.py-5-those three files hand-maintained SIX independently-editable copies of the same four-provider
ai/scripts/provider_registry.py:6:universe (`_OPENCODE_PROVIDER_KEYS`, `_OPENCODE_CLI_IDS`, `PROVIDER_BILLING_KIND`, the key map
ai/scripts/provider_registry.py-7-inside `_configured_models`, `DISCOVERABLE_PROVIDERS`, `_MODEL_PREFERENCE_PROVIDERS`), synced
--
ai/scripts/set_agents_app.py-93-# (`routing_core/catalog.py:165-172`). ADR-0042 (022 PKG-1) supersedes this table's own
ai/scripts/set_agents_app.py:94:# ORIGINAL "defined independently here, never importing" framing -- that framing was wider
ai/scripts/set_agents_app.py-95-# than the reason it named: the AC-06 non-goal it cites (014-model-preference-policy,
--
ai/scripts/set_agents_app.py-126-    """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
ai/scripts/set_agents_app.py:127:    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
ai/scripts/set_agents_app.py-128-    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
--
ai/scripts/routing_core/catalog.py-80-# from this map, a model id is already its own canonical form — this never invents an
ai/scripts/routing_core/catalog.py:81:# equivalence beyond what is explicitly curated here, same discipline as PROVIDER_BILLING_KIND.
ai/scripts/routing_core/catalog.py-82-#
--
ai/scripts/routing_core/catalog.py-182-# weighting/selection logic reads this map yet" note is retired.
ai/scripts/routing_core/catalog.py:183:# ADR-0042 (022 PKG-1): derives from `PROVIDERS` now, same values as the pre-ADR-0042 literal.
ai/scripts/routing_core/catalog.py:184:PROVIDER_BILLING_KIND = {provider: spec.billing_kind for provider, spec in PROVIDERS.items()}
ai/scripts/routing_core/catalog.py-185-# ADR-0035 AC-12: the FREE-model convention is the `-free` suffix -- the exact same
--
ai/scripts/routing_core/catalog.py-197-    among candidates that already survived every hard exclusion."""
ai/scripts/routing_core/catalog.py:198:    if PROVIDER_BILLING_KIND.get(provider) == "subscription":
ai/scripts/routing_core/catalog.py-199-        return 0
--
ai/scripts/routing_core/catalog.py-772-            "models_listable": len(models),
ai/scripts/routing_core/catalog.py:773:            "billing": PROVIDER_BILLING_KIND.get(provider, "unknown"),
ai/scripts/routing_core/catalog.py-774-            "detected_unlistable": False,
tests/test_routing.py:3966:            self.assertEqual(app._MODEL_PREFERENCE_PROVIDERS, ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
tests/test_routing.py:3967:        wider = (*app._MODEL_PREFERENCE_PROVIDERS, "a-future-live-only-provider")
tests/test_routing.py:3974:        # F-05 (P1 repair): `_MODEL_PREFERENCE_PROVIDERS` already covers
tests/test_routing.py:3982:            self.assertEqual(app._effective_preference_providers(), app._MODEL_PREFERENCE_PROVIDERS)
tests/test_routing.py:3987:                                frozenset({*app._MODEL_PREFERENCE_PROVIDERS, "a-future-only-provider"})):
tests/test_routing.py:4007:    # registry -- `provider_registry.PROVIDERS` is now the one source every provider-keyed
tests/test_routing.py:4016:        self.assertEqual(list(provider_registry.PROVIDERS),
tests/test_routing.py:4039:        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
tests/test_routing.py:4043:        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
tests/test_routing.py:4057:        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
tests/test_routing.py:4063:        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
ai/scripts/routing_core/catalog.py:20:from provider_registry import PROVIDERS
ai/scripts/set_agents_app.py:101:# `provider_registry.PROVIDERS` registry every provider-keyed table in the harness shares,
ai/scripts/set_agents_app.py:104:# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
ai/scripts/set_agents_app.py:106:_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
ai/scripts/set_agents_app.py:126:    """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
ai/scripts/set_agents_app.py:128:    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
ai/scripts/set_agents_app.py:147:    `provider_registry.PROVIDERS`, and `test_adr0042_ac01b_...` in tests/test_routing.py
ai/scripts/set_agents_app.py:156:    if set(models_config.DISCOVERABLE_PROVIDERS) <= set(_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py:157:        return _MODEL_PREFERENCE_PROVIDERS
ai/scripts/set_agents_app.py:164:        return _MODEL_PREFERENCE_PROVIDERS
ai/scripts/set_agents_app.py:165:    return tuple(dict.fromkeys((*_MODEL_PREFERENCE_PROVIDERS, *live)))
ai/scripts/set_agents_app.py:168:def _validate_preference_providers(class_name, providers, valid_providers=_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py:255:def _validate_model_pin_entry(role, value, roster_roles, valid_providers=_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/models_config.py:50:# ADR-0042 (022 PKG-1): derives from the single `provider_registry.PROVIDERS` registry now,
ai/scripts/models_config.py:52:# `set_agents_app._MODEL_PREFERENCE_PROVIDERS` is the ORDERED sibling table, and it derives
ai/scripts/models_config.py:53:# from `provider_registry.PROVIDERS` directly rather than from this set, because a tuple
ai/scripts/models_config.py:55:DISCOVERABLE_PROVIDERS = set(provider_registry.PROVIDERS)
ai/scripts/provider_registry.py:7:inside `_configured_models`, `DISCOVERABLE_PROVIDERS`, `_MODEL_PREFERENCE_PROVIDERS`), synced
ai/scripts/provider_registry.py:19:_MODEL_PREFERENCE_PROVIDERS` derives its ordered tuple from `tuple(PROVIDERS)`, and that order
  3178	        self.assertIn("kimi-k2.7-code", self.config["catalog"]["opencode_go"])
  3179	        # A model outside the declared ceiling can never surface, even if the CLI reports
  3180	        # it: _probe_pairs intersects the parsed roster against `allowed` before returning.
  3181	        narrow = json.loads(json.dumps(self.config)); narrow["catalog"]["opencode_zen"] = ["kimi-k2.7-code"]
  3182	        self.assertEqual(cat._configured_models(narrow, "opencode-zen"), {"kimi-k2.7-code"})
  3183	
  3184	        def fake_run(argv, **kwargs):
  3185	            if argv[1:4] == ("auth", "list", "--pure"):
  3186	                return types.SimpleNamespace(returncode=0, stdout="●  OpenCode Zen api\n", stderr="")
  3187	            return types.SimpleNamespace(returncode=0, stdout="opencode/kimi-k2.7-code\nopencode/glm-5.2\n", stderr="")
  3188	        with mock.patch.object(routing_catalog.subprocess, "run", side_effect=fake_run):
  3189	            result = routing_catalog.probe_inventory(narrow, cache_root=None, pairs=[("opencode", "opencode-zen")])
  3190	        self.assertEqual(result[("opencode", "opencode-zen")], {"kimi-k2.7-code"})  # glm-5.2 dropped, never invented
  3191	
  3192	    def test_adr0034_ac10_discoverable_providers_lockstep_guard(self):
  3193	        # ADR-0034 (019 PKG-1, AC-10): DISCOVERABLE_PROVIDERS must equal exactly the
  3194	        # provider half of _PAIR_COMMANDS -- the guard that keeps `github-copilot` (M-1,
  3195	        # unlistable) and a duplicate `openai` catalog provider (M-2, already covered by
  3196	        # `openai-codex`) from ever being addable to the "auto"-adopted universe without
  3197	        # this test failing loudly first.
  3198	        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
  3199	        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS, pair_providers)
  3200	        self.assertNotIn("github-copilot", models_config.DISCOVERABLE_PROVIDERS)
  3201	        self.assertNotIn("openai", models_config.DISCOVERABLE_PROVIDERS)
  3202	
  3203	    def test_ac05_new_providers_are_probeable_not_routable_today(self):
  3204	        # AC-05/AC-11 non-goal: no enabled_providers or ROUTING_PROVIDERS change -- a
  3205	        # curated row for either new provider must still be rejected by build_snapshot
  3206	        # today, proving discovery never silently becomes routability.
  3207	        base = dict(provider="opencode-zen", model="kimi-k2.7-code", family="kimi", effort="medium",
  3208	                    tier="balanced", roles=["implementer"], tools=["read", "write"], curated_priority=1)
  3209	        with tempfile.TemporaryDirectory() as td:
  3210	            path = self._write_catalog(td, [base])
  4006	    # ------------------------------------------------- ADR-0042 (022 PKG-1): single provider
  4007	    # registry -- `provider_registry.PROVIDERS` is now the one source every provider-keyed
  4008	    # table below derives from, replacing six independently hand-edited literal copies.
  4009	
  4010	    def test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals(self):
  4011	        # AC-02 characterization: every literal pasted here is the EXACT pre-ADR-0042 value
  4012	        # (copied from the removed literals, not imported from provider_registry or derived
  4013	        # in this test) -- proves the refactor changed plumbing, never values. Order matters
  4014	        # only for the tuple (AC-01's provider-registry insertion order contract); the dicts
  4015	        # and the set compare by value, so their literal order below is not significant.
  4016	        self.assertEqual(list(provider_registry.PROVIDERS),
  4017	                         ["openai-codex", "anthropic", "opencode-zen", "opencode-go"])
  4018	        self.assertEqual(routing_catalog._OPENCODE_PROVIDER_KEYS, {
  4019	            "openai-codex": "openai", "anthropic": "anthropic",
  4020	            "opencode-zen": "opencode zen", "opencode-go": "opencode go",
  4021	        })
  4022	        self.assertEqual(routing_catalog._OPENCODE_CLI_IDS, {
  4023	            "openai-codex": "openai", "anthropic": "anthropic",
  4024	            "opencode-zen": "opencode", "opencode-go": "opencode-go",
  4025	        })
  4026	        self.assertEqual(routing_catalog.PROVIDER_BILLING_KIND, {
  4027	            "openai-codex": "subscription", "anthropic": "subscription",
  4028	            "opencode-go": "subscription", "opencode-zen": "metered",
  4029	        })
  4030	        # AC-01's fifth symbol: the key map that used to live INLINE inside
  4031	        # `_configured_models` (a local dict literal, unreachable from outside the function)
  4032	        # is now the module-level `_CATALOG_KEYS`, itself derived from `PROVIDERS`.
  4033	        self.assertEqual(routing_catalog._CATALOG_KEYS, {
  4034	            "openai-codex": "codex", "anthropic": "claude",
  4035	            "opencode-zen": "opencode_zen", "opencode-go": "opencode_go",
  4036	        })
  4037	        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS,
  4038	                         {"openai-codex", "anthropic", "opencode-zen", "opencode-go"})
  4039	        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
  4040	                         ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
  4041	
  4042	    def test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source(self):
  4043	        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
  4044	        # lived at test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_
  4045	        # not_just_the_constant (below, :3965 pre-refactor line count) -- inside a
  4046	        # `mock.patch` block whose real purpose is testing the live-probe degrade path, and
  4047	        # it compared against ANOTHER hardcoded literal, never against the audited source.
  4048	        # That test is untouched by this package (still passes, proven by the full-suite
  4049	        # gate) and still is not the guard -- THIS is the guard: two independent
  4050	        # cross-checks against the real source, neither of them a second copy-pasted tuple.
  4051	        #
  4052	        # Cross-check 1: same pattern as test_adr0034_ac10_discoverable_providers_lockstep_
  4053	        # guard above (which only ever covered DISCOVERABLE_PROVIDERS, never this tuple,
  4054	        # despite set_agents_app.py's own comment claiming otherwise pre-ADR-0042) --
  4055	        # membership against `_PAIR_COMMANDS`'s own audited provider set.
  4056	        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
  4057	        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
  4058	        # Cross-check 2: against the ordered registry directly -- this is what makes a
  4059	        # FUTURE regression (someone re-introducing a hand-maintained tuple in
  4060	        # set_agents_app.py instead of deriving it) fail loudly the moment `PROVIDERS`
  4061	        # changes without a matching edit here, rather than silently agreeing with a second,
  4062	        # equally-stale literal (the exact defect this package exists to close).
  4063	        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
  4064	
  4065	    # --------------------------------------------- 014-model-preference-policy (AC-01..09)
 M ai/scripts/models_config.py
 M ai/scripts/routing_core/catalog.py
 M ai/scripts/set_agents_app.py
 M ai/state/STATUS.md
 M ai/state/decisions-log.jsonl
 M docs/adr/README.md
 M "docs/notas/00 - Proyecto.md"
 M docs/specs/022-disponibilidad-real/spec.md
 M tests/test_routing.py
?? ai/scripts/provider_registry.py
?? ai/state/features/022-disponibilidad-real.json
?? docs/adr/0042-provider-registry-single-source.md
?? "docs/notas/decisiones/2026-08-13 el-probe-dice-vivo-y-la-inferencia-dice-token-vencido.md"
?? "docs/notas/decisiones/2026-08-13 ruteo-nombra-el-runtime-que-realmente-ejecuta.md"
?? docs/notas/features/022-disponibilidad-real.md
?? docs/notas/features/022-disponibilidad-real/
?? docs/specs/022-disponibilidad-real/bitacora.md
?? docs/specs/022-disponibilidad-real/context/
?? docs/specs/022-disponibilidad-real/evidence/

codex
La mordida ya confirmó un problema: al renombrar una clave de `PROVIDERS`, el test que debía ponerse rojo siguió verde. Antes de cerrar, voy a verificar de forma separada el contrato de orden: qué test sí falla si cambia el orden, y voy a restaurar inmediatamente el archivo.
exec
/bin/bash -lc "cp ai/scripts/provider_registry.py /tmp/p1-provider_registry-order.py.orig
perl -0pi -e 's/\"openai-codex\": ProviderSpec\\(/\"P1_ORDER_TEMP\": ProviderSpec\\(/; s/\"anthropic\": ProviderSpec\\(/\"openai-codex\": ProviderSpec\\(/; s/\"P1_ORDER_TEMP\": ProviderSpec\\(/\"anthropic\": ProviderSpec\\(/' ai/scripts/provider_registry.py
nl -ba ai/scripts/provider_registry.py | sed -n '50,70p'" in /home/federico/SET-AGENTES
 succeeded in 0ms:
    50	
    51	# Order is a contract -- see module docstring. Values are unchanged from the pre-ADR-0042
    52	# literals (PKG-1 AC-02 characterization test pins this).
    53	PROVIDERS: dict[str, ProviderSpec] = {
    54	    "anthropic": ProviderSpec(
    55	        opencode_auth_key="openai", opencode_cli_id="openai",
    56	        billing_kind="subscription", catalog_key="codex",
    57	    ),
    58	    "openai-codex": ProviderSpec(
    59	        opencode_auth_key="anthropic", opencode_cli_id="anthropic",
    60	        billing_kind="subscription", catalog_key="claude",
    61	    ),
    62	    "opencode-zen": ProviderSpec(
    63	        opencode_auth_key="opencode zen", opencode_cli_id="opencode",
    64	        billing_kind="metered", catalog_key="opencode_zen",
    65	    ),
    66	    "opencode-go": ProviderSpec(
    67	        opencode_auth_key="opencode go", opencode_cli_id="opencode-go",
    68	        billing_kind="subscription", catalog_key="opencode_go",
    69	    ),
    70	}

exec
/bin/bash -lc 'ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals -v' in /home/federico/SET-AGENTES
 exited 1 in 83ms:
test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals) ... FAIL

======================================================================
FAIL: test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "/home/federico/SET-AGENTES/tests/test_routing.py", line 4016, in test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals
    self.assertEqual(list(provider_registry.PROVIDERS),
    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                     ["openai-codex", "anthropic", "opencode-zen", "opencode-go"])
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
AssertionError: Lists differ: ['anthropic', 'openai-codex', 'opencode-zen', 'opencode-go'] != ['openai-codex', 'anthropic', 'opencode-zen', 'opencode-go']

First differing element 0:
'anthropic'
'openai-codex'

- ['anthropic', 'openai-codex', 'opencode-zen', 'opencode-go']
?  -------------

+ ['openai-codex', 'anthropic', 'opencode-zen', 'opencode-go']
?                  +++++++++++++


----------------------------------------------------------------------
Ran 1 test in 0.004s

FAILED (failures=1)

exec
/bin/bash -lc 'cp /tmp/p1-provider_registry-order.py.orig ai/scripts/provider_registry.py
diff /tmp/p1-provider_registry-order.py.orig ai/scripts/provider_registry.py
ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v' in /home/federico/SET-AGENTES
 succeeded in 82ms:
test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals) ... ok
test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 2 tests in 0.004s

OK

exec
/bin/bash -lc "rg -n -C 2 '_PAIR_COMMANDS' ai/scripts --glob '*.py'; rg -n -C 2 'tuple\\(provider_registry\\.PROVIDERS\\)|_MODEL_PREFERENCE_PROVIDERS' ai/scripts/set_agents_app.py tests/test_routing.py" in /home/federico/SET-AGENTES
 succeeded in 0ms:
ai/scripts/set_agents_app.py-90-MODEL_PREFERENCE_PATH = STATE_DIR / "model-preference.toml"
ai/scripts/set_agents_app.py-91-
ai/scripts/set_agents_app.py:92:# The closed, four-provider universe `_PAIR_COMMANDS` already probes
ai/scripts/set_agents_app.py-93-# (`routing_core/catalog.py:165-172`). ADR-0042 (022 PKG-1) supersedes this table's own
ai/scripts/set_agents_app.py-94-# ORIGINAL "defined independently here, never importing" framing -- that framing was wider
--
ai/scripts/set_agents_app.py-142-    `resolve_discovered_providers` can return a provider OUTSIDE the base set --
ai/scripts/set_agents_app.py-143-    which today it structurally cannot: every provider it can produce comes from
ai/scripts/set_agents_app.py:144:    `routing_core.catalog._PAIR_COMMANDS`'s own audited provider set, and that set is
ai/scripts/set_agents_app.py-145-    pinned equal to `models_config.DISCOVERABLE_PROVIDERS` (== `_MODEL_PREFERENCE_
ai/scripts/set_agents_app.py-146-    PROVIDERS`, guarded for real since ADR-0042 -- both are derived from
ai/scripts/set_agents_app.py-147-    `provider_registry.PROVIDERS`, and `test_adr0042_ac01b_...` in tests/test_routing.py
ai/scripts/set_agents_app.py:148:    compares this tuple against `_PAIR_COMMANDS`'s own provider set directly, never a
ai/scripts/set_agents_app.py-149-    second hardcoded literal). So the probe below is skipped whenever the
ai/scripts/set_agents_app.py-150-    base set already covers `DISCOVERABLE_PROVIDERS` -- a cheap, exact short-circuit
ai/scripts/set_agents_app.py:151:    that avoids paying a subprocess probe (each `_PAIR_COMMANDS` entry can block up to
ai/scripts/set_agents_app.py-152-    20s) on every `--model-pin-set`/`--model-preference-set` call for a union that
ai/scripts/set_agents_app.py-153-    cannot widen anything yet. The probe only actually runs again once
ai/scripts/set_agents_app.py:154:    `_PAIR_COMMANDS`'s audited set genuinely grows past the base set (e.g. a future
ai/scripts/set_agents_app.py-155-    ADR widens discovery beyond today's four providers)."""
ai/scripts/set_agents_app.py-156-    if set(models_config.DISCOVERABLE_PROVIDERS) <= set(_MODEL_PREFERENCE_PROVIDERS):
--
ai/scripts/routing_core/catalog.py-148-# name this table ALREADY maps to `openai-codex` (M-2); no duplicate catalog provider is
ai/scripts/routing_core/catalog.py-149-# added for it. Detection is structural, not a separate code path: `github-copilot`
ai/scripts/routing_core/catalog.py:150:# never appears in `_PAIR_COMMANDS`, so `_probe_pairs` never even queries it -- the rest
ai/scripts/routing_core/catalog.py-151-# of the inventory is never at risk of an unverified id leaking in. `--route-doctor`
ai/scripts/routing_core/catalog.py-152-# (PKG-2, ADR-0035) is the surface that will report "detected, unlistable" to a human;
--
ai/scripts/routing_core/catalog.py-161-# above, never a second hardcoded copy of the CLI id — a future change to one map with
ai/scripts/routing_core/catalog.py-162-# the other left stale would otherwise silently leave a pair absent, undetected by any
ai/scripts/routing_core/catalog.py:163:# test or the live gate (both only ever exercise whichever id `_PAIR_COMMANDS` itself
ai/scripts/routing_core/catalog.py-164-# carries).
ai/scripts/routing_core/catalog.py:165:_PAIR_COMMANDS = {
ai/scripts/routing_core/catalog.py-166-    ("codex", "openai-codex"): (("codex", "login", "status"),),
ai/scripts/routing_core/catalog.py-167-    ("claude-code", "anthropic"): (("claude", "auth", "status", "--json"),),
--
ai/scripts/routing_core/catalog.py-206-def allowed_probe(runtime: str, provider: str) -> tuple[tuple[str, ...], ...] | None:
ai/scripts/routing_core/catalog.py-207-    """The closed, exact argv list for a pair; None for any unaudited pair."""
ai/scripts/routing_core/catalog.py:208:    return _PAIR_COMMANDS.get((runtime, provider))
ai/scripts/routing_core/catalog.py-209-
ai/scripts/routing_core/catalog.py-210-
--
ai/scripts/routing_core/catalog.py-425-        for name, models in doc["pairs"].items():
ai/scripts/routing_core/catalog.py-426-            runtime, _, provider = str(name).partition("|")
ai/scripts/routing_core/catalog.py:427:            if (runtime, provider) not in _PAIR_COMMANDS or not isinstance(models, list) or not all(isinstance(m, str) for m in models):
ai/scripts/routing_core/catalog.py-428-                return None
ai/scripts/routing_core/catalog.py-429-            intersected = set(models) & _configured_models(config, provider)
--
ai/scripts/routing_core/catalog.py-484-    for pair in pairs:
ai/scripts/routing_core/catalog.py-485-        runtime, provider = pair
ai/scripts/routing_core/catalog.py:486:        commands = _PAIR_COMMANDS[pair]
ai/scripts/routing_core/catalog.py-487-        allowed = _configured_models(config, provider)
ai/scripts/routing_core/catalog.py-488-        if not allowed:
--
ai/scripts/routing_core/catalog.py-572-    now = time.time() if now is None else now
ai/scripts/routing_core/catalog.py-573-    if pairs is not None:
ai/scripts/routing_core/catalog.py:574:        selected = [pair for pair in pairs if pair in _PAIR_COMMANDS]
ai/scripts/routing_core/catalog.py-575-        return _probe_pairs(config, selected, timeout)
ai/scripts/routing_core/catalog.py:576:    all_pairs = list(_PAIR_COMMANDS)
ai/scripts/routing_core/catalog.py-577-    use_cache = cache_root is not None and _validate_cache_dir(Path(cache_root))
ai/scripts/routing_core/catalog.py-578-    auth_signature = _live_opencode_auth_signature(timeout) if use_cache else ""
--
ai/scripts/routing_core/catalog.py-657-    optional_keys = {"runtimes"}
ai/scripts/routing_core/catalog.py-658-    audited: dict[str, set[str]] = {}
ai/scripts/routing_core/catalog.py:659:    for runtime, provider in _PAIR_COMMANDS:
ai/scripts/routing_core/catalog.py-660-        audited.setdefault(provider, set()).add(runtime)
ai/scripts/routing_core/catalog.py-661-    for row in rows:
--
ai/scripts/routing_core/catalog.py-714-
ai/scripts/routing_core/catalog.py-715-    `"auto"` derives from the PROBED inventory (never a static list) intersected with the
ai/scripts/routing_core/catalog.py:716:    audited `_PAIR_COMMANDS` provider set -- copilot and any other never-audited provider
ai/scripts/routing_core/catalog.py-717-    can never appear here no matter what a probe observes (M-1). An explicit list is used
ai/scripts/routing_core/catalog.py-718-    as-is (already validated against `DISCOVERABLE_PROVIDERS` at models_config load time);
--
ai/scripts/routing_core/catalog.py-722-    raw = (config.get("routing") or {}).get("discovered_providers") if isinstance(config, dict) else None
ai/scripts/routing_core/catalog.py-723-    if raw == "auto":
ai/scripts/routing_core/catalog.py:724:        audited_providers = {provider for _, provider in _PAIR_COMMANDS}
ai/scripts/routing_core/catalog.py-725-        # `inventory` never stores an empty-model pair (`_probe_pairs`'s own `if models:`
ai/scripts/routing_core/catalog.py-726-        # guard), so key presence alone already means "live" -- no extra truthiness check.
--
ai/scripts/routing_core/catalog.py-759-    except (OSError, subprocess.TimeoutExpired, RoutingError):
ai/scripts/routing_core/catalog.py-760-        raw_auth = set()
ai/scripts/routing_core/catalog.py:761:    opencode_pairs = [pair for pair in _PAIR_COMMANDS if pair[0] == "opencode"]
ai/scripts/routing_core/catalog.py-762-    # Always fresh, never cached, never written -- `pairs=` bypasses the cache entirely
ai/scripts/routing_core/catalog.py-763-    # in `probe_inventory` itself (its `if pairs is not None:` branch).
--
ai/scripts/routing_core/catalog.py-840-    twin is ever added; `[catalog].exclude` entries (`"provider:model"` strings in
ai/scripts/routing_core/catalog.py-841-    models.toml) veto individual discovered ids; providers outside the audited
ai/scripts/routing_core/catalog.py:842:    `_PAIR_COMMANDS` set are never synthesized, no matter what the config says.
ai/scripts/routing_core/catalog.py-843-
ai/scripts/routing_core/catalog.py-844-    ADR-0034: `providers` comes from `resolve_discovered_providers` -- the single
--
ai/scripts/routing_core/catalog.py-858-            exclusions.add(tuple(item.split(":", 1)))
ai/scripts/routing_core/catalog.py-859-    audited: dict[str, set[str]] = {}
ai/scripts/routing_core/catalog.py:860:    for runtime, prov in _PAIR_COMMANDS:
ai/scripts/routing_core/catalog.py-861-        audited.setdefault(prov, set()).add(runtime)
ai/scripts/routing_core/catalog.py-862-    routes = list(base.routes)
--
ai/scripts/models_config.py-58-    # ADR-0034 (019 PKG-1): "auto" is the new default -- discovered_providers derives
ai/scripts/models_config.py-59-    # from the live probed inventory (routing_core.catalog.resolve_discovered_providers)
ai/scripts/models_config.py:60:    # intersected with the audited _PAIR_COMMANDS provider set, never wider. An explicit
ai/scripts/models_config.py-61-    # [] still means "disabled entirely" and survives emit() (compared against this exact
ai/scripts/models_config.py-62-    # default value below, never truthiness, so [] no longer round-trips into "auto").
tests/test_routing.py-3964-            # Even when the live probe blows up, the base constant still validates a
tests/test_routing.py-3965-            # legitimate provider -- never a crash, matching the documented degrade.
tests/test_routing.py:3966:            self.assertEqual(app._MODEL_PREFERENCE_PROVIDERS, ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
tests/test_routing.py:3967:        wider = (*app._MODEL_PREFERENCE_PROVIDERS, "a-future-live-only-provider")
tests/test_routing.py-3968-        validated = app._validate_preference_providers("grunt", ["a-future-live-only-provider"], wider)
tests/test_routing.py-3969-        self.assertEqual(validated, ("a-future-live-only-provider",))
--
tests/test_routing.py-3972-
tests/test_routing.py-3973-    def test_adr0034_f05_repair_effective_providers_short_circuits_when_base_covers_discoverable(self):
tests/test_routing.py:3974:        # F-05 (P1 repair): `_MODEL_PREFERENCE_PROVIDERS` already covers
tests/test_routing.py-3975-        # `models_config.DISCOVERABLE_PROVIDERS` today (AC-10's own lockstep test), so the
tests/test_routing.py-3976-        # union in `_effective_preference_providers` is a structural no-op -- yet it used
--
tests/test_routing.py-3980-        import set_agents_app as app
tests/test_routing.py-3981-        with mock.patch("routing_core.catalog.probe_inventory", return_value={}) as probe:
tests/test_routing.py:3982:            self.assertEqual(app._effective_preference_providers(), app._MODEL_PREFERENCE_PROVIDERS)
tests/test_routing.py-3983-            probe.assert_not_called()
tests/test_routing.py-3984-        # Only once the audited set genuinely grows past the base set does the probe run
tests/test_routing.py-3985-        # again -- proving the short-circuit is a real conditional, not a permanent skip.
tests/test_routing.py-3986-        with mock.patch.object(app.models_config, "DISCOVERABLE_PROVIDERS",
tests/test_routing.py:3987:                                frozenset({*app._MODEL_PREFERENCE_PROVIDERS, "a-future-only-provider"})):
tests/test_routing.py-3988-            with mock.patch("routing_core.catalog.probe_inventory", return_value={}) as probe:
tests/test_routing.py-3989-                with mock.patch("routing_core.catalog.resolve_discovered_providers", return_value=()):
--
tests/test_routing.py-4037-        self.assertEqual(models_config.DISCOVERABLE_PROVIDERS,
tests/test_routing.py-4038-                         {"openai-codex", "anthropic", "opencode-zen", "opencode-go"})
tests/test_routing.py:4039:        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS,
tests/test_routing.py-4040-                         ("openai-codex", "anthropic", "opencode-zen", "opencode-go"))
tests/test_routing.py-4041-
tests/test_routing.py-4042-    def test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source(self):
tests/test_routing.py:4043:        # AC-01b: before this ADR, the ONLY assertion touching `_MODEL_PREFERENCE_PROVIDERS`
tests/test_routing.py-4044-        # lived at test_adr0034_ac09_write_cli_validates_pins_against_the_effective_set_
tests/test_routing.py-4045-        # not_just_the_constant (below, :3965 pre-refactor line count) -- inside a
--
tests/test_routing.py-4055-        # membership against `_PAIR_COMMANDS`'s own audited provider set.
tests/test_routing.py-4056-        pair_providers = {provider for _, provider in routing_catalog._PAIR_COMMANDS}
tests/test_routing.py:4057:        self.assertEqual(set(set_agents_app._MODEL_PREFERENCE_PROVIDERS), pair_providers)
tests/test_routing.py-4058-        # Cross-check 2: against the ordered registry directly -- this is what makes a
tests/test_routing.py-4059-        # FUTURE regression (someone re-introducing a hand-maintained tuple in
--
tests/test_routing.py-4061-        # changes without a matching edit here, rather than silently agreeing with a second,
tests/test_routing.py-4062-        # equally-stale literal (the exact defect this package exists to close).
tests/test_routing.py:4063:        self.assertEqual(set_agents_app._MODEL_PREFERENCE_PROVIDERS, tuple(provider_registry.PROVIDERS))
tests/test_routing.py-4064-
tests/test_routing.py-4065-    # --------------------------------------------- 014-model-preference-policy (AC-01..09)
--
ai/scripts/set_agents_app.py-102-# instead of being a second hand-maintained copy of the same four provider ids. Order is a
ai/scripts/set_agents_app.py-103-# real contract (surfaces in pin/preference validation errors) --
ai/scripts/set_agents_app.py:104:# `tuple(provider_registry.PROVIDERS)`, never `tuple(models_config.DISCOVERABLE_PROVIDERS)`,
ai/scripts/set_agents_app.py-105-# because that sibling table is an unordered `set`.
ai/scripts/set_agents_app.py:106:_MODEL_PREFERENCE_PROVIDERS = tuple(provider_registry.PROVIDERS)
ai/scripts/set_agents_app.py-107-_MODEL_PREFERENCE_CLASSES = ("decision", "grunt", "build")
ai/scripts/set_agents_app.py-108-# AC-02's inertness note: the six roles with real, live effect today (`### Honest scope`,
--
ai/scripts/set_agents_app.py-124-
ai/scripts/set_agents_app.py-125-def _effective_preference_providers():
ai/scripts/set_agents_app.py:126:    """ADR-0034 (AC-09): the base audited set (`_MODEL_PREFERENCE_PROVIDERS`, unchanged
ai/scripts/set_agents_app.py-127-    -- same VALUES as `models_config.DISCOVERABLE_PROVIDERS`, both derived from the single
ai/scripts/set_agents_app.py-128-    `provider_registry.PROVIDERS` registry since ADR-0042; the two differ in TYPE on
--
ai/scripts/set_agents_app.py-154-    `_PAIR_COMMANDS`'s audited set genuinely grows past the base set (e.g. a future
ai/scripts/set_agents_app.py-155-    ADR widens discovery beyond today's four providers)."""
ai/scripts/set_agents_app.py:156:    if set(models_config.DISCOVERABLE_PROVIDERS) <= set(_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py:157:        return _MODEL_PREFERENCE_PROVIDERS
ai/scripts/set_agents_app.py-158-    try:
ai/scripts/set_agents_app.py-159-        from routing_core.catalog import probe_inventory, resolve_discovered_providers
--
ai/scripts/set_agents_app.py-162-        live = resolve_discovered_providers(config, inventory)
ai/scripts/set_agents_app.py-163-    except Exception:
ai/scripts/set_agents_app.py:164:        return _MODEL_PREFERENCE_PROVIDERS
ai/scripts/set_agents_app.py:165:    return tuple(dict.fromkeys((*_MODEL_PREFERENCE_PROVIDERS, *live)))
ai/scripts/set_agents_app.py-166-
ai/scripts/set_agents_app.py-167-
ai/scripts/set_agents_app.py:168:def _validate_preference_providers(class_name, providers, valid_providers=_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py-169-    """AC-02 resolution states 1/2/4d: shared by the config-load path and the CLI write
ai/scripts/set_agents_app.py-170-    path (`load_model_preference`/`cmd_model_preference_set` below) -- a malformed value
--
ai/scripts/set_agents_app.py-253-
ai/scripts/set_agents_app.py-254-
ai/scripts/set_agents_app.py:255:def _validate_model_pin_entry(role, value, roster_roles, valid_providers=_MODEL_PREFERENCE_PROVIDERS):
ai/scripts/set_agents_app.py-256-    """`valid_providers` (ADR-0034 AC-09): defaults to the static base set, so every
ai/scripts/set_agents_app.py-257-    boot-path caller (`load_model_pin`) stays network-free; the write-CLI

codex
VEREDICTO: revision_required

ID: F1  
SEVERIDAD: critical  
DONDE: tests/test_routing.py:4056-4063  
QUE: La guarda AC-01b no detecta una `PROVIDERS` rota: compara únicamente valores que se rederivan de esa misma fuente; agregá una expectativa independiente y estable del universo/orden esperado, y conservá la mordida que la haga fallar.  
PRUEBA: muté `opencode-go` a `opencode-go-broken` en `provider_registry.py` y ejecuté el test dirigido. Debía ponerse rojo; quedó verde.

$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v

test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.002s

OK

Restauré con `cp`; `diff` no emitió salida y terminó en 0. Verde tras restaurar:

$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v

test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.002s

OK

PUNTO 1 — sin hallazgo. `catalog.py:652-653` mantiene un duplicado manual, pero es el “site 3 of 5” de la política de techos de catálogo, no uno de los siete símbolos de AC-01. AC-01 nombra el key-map de `_configured_models`, que sí deriva en `_CATALOG_KEYS` (`catalog.py:211-218`). AC-04 asigna explícitamente a P2 el reemplazo de `_configured_models` y su consumo en `build_snapshot` (`spec.md:76-90`). Si mañana se agrega una fila al registro, P1 actualiza los seis duplicados manuales y el séptimo transitorio; la selección del techo de catálogo seguirá requiriendo P2. No corresponde adelantar ese cambio en P1.

PUNTO 2 — el consumidor con contrato de orden es `_MODEL_PREFERENCE_PROVIDERS` (`set_agents_app.py:102-106`), usado también al construir la tupla efectiva (`:165`). El test de caracterización `test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals` cubre el orden del registro y de la tupla. Al romperlo, quedó rojo:

$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals -v

test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals) ... FAIL

======================================================================
FAIL: test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "/home/federico/SET-AGENTES/tests/test_routing.py", line 4016, in test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals
    self.assertEqual(list(provider_registry.PROVIDERS),
AssertionError: Lists differ: ['anthropic', 'openai-codex', 'opencode-zen', 'opencode-go'] != ['openai-codex', 'anthropic', 'opencode-zen', 'opencode-go']

----------------------------------------------------------------------
Ran 1 test in 0.004s

FAILED (failures=1)

PUNTO 3 — sin hallazgo. ADR-0042 supersede explícitamente el no-goal anterior (`docs/adr/0042-provider-registry-single-source.md:113-119`), corrige la afirmación de ADR-0034 con la medición (`:120-129`), y los comentarios de código son coherentes con la derivación y la diferencia de tipos (`set_agents_app.py:92-106`, `:125-129`).

PUNTO 4 — falló la mordida requerida y queda cubierto por F1. El test nuevo prueba que `set_agents_app` deriva del registro, pero no que el registro conserve el contenido correcto.
tokens used
88.483
VEREDICTO: revision_required

ID: F1  
SEVERIDAD: critical  
DONDE: tests/test_routing.py:4056-4063  
QUE: La guarda AC-01b no detecta una `PROVIDERS` rota: compara únicamente valores que se rederivan de esa misma fuente; agregá una expectativa independiente y estable del universo/orden esperado, y conservá la mordida que la haga fallar.  
PRUEBA: muté `opencode-go` a `opencode-go-broken` en `provider_registry.py` y ejecuté el test dirigido. Debía ponerse rojo; quedó verde.

$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v

test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.002s

OK

Restauré con `cp`; `diff` no emitió salida y terminó en 0. Verde tras restaurar:

$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source -v

test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source (tests.test_routing.RoutingTests.test_adr0042_ac01b_model_preference_providers_is_guarded_against_the_real_source) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.002s

OK

PUNTO 1 — sin hallazgo. `catalog.py:652-653` mantiene un duplicado manual, pero es el “site 3 of 5” de la política de techos de catálogo, no uno de los siete símbolos de AC-01. AC-01 nombra el key-map de `_configured_models`, que sí deriva en `_CATALOG_KEYS` (`catalog.py:211-218`). AC-04 asigna explícitamente a P2 el reemplazo de `_configured_models` y su consumo en `build_snapshot` (`spec.md:76-90`). Si mañana se agrega una fila al registro, P1 actualiza los seis duplicados manuales y el séptimo transitorio; la selección del techo de catálogo seguirá requiriendo P2. No corresponde adelantar ese cambio en P1.

PUNTO 2 — el consumidor con contrato de orden es `_MODEL_PREFERENCE_PROVIDERS` (`set_agents_app.py:102-106`), usado también al construir la tupla efectiva (`:165`). El test de caracterización `test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals` cubre el orden del registro y de la tupla. Al romperlo, quedó rojo:

$ ai/scripts/heartbeat-run.py --interval 20 -- python3 -m unittest tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals -v

test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals) ... FAIL

======================================================================
FAIL: test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals (tests.test_routing.RoutingTests.test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "/home/federico/SET-AGENTES/tests/test_routing.py", line 4016, in test_adr0042_ac01_ac02_all_seven_provider_tables_are_byte_identical_to_the_pre_refactor_literals
    self.assertEqual(list(provider_registry.PROVIDERS),
AssertionError: Lists differ: ['anthropic', 'openai-codex', 'opencode-zen', 'opencode-go'] != ['openai-codex', 'anthropic', 'opencode-zen', 'opencode-go']

----------------------------------------------------------------------
Ran 1 test in 0.004s

FAILED (failures=1)

PUNTO 3 — sin hallazgo. ADR-0042 supersede explícitamente el no-goal anterior (`docs/adr/0042-provider-registry-single-source.md:113-119`), corrige la afirmación de ADR-0034 con la medición (`:120-129`), y los comentarios de código son coherentes con la derivación y la diferencia de tipos (`set_agents_app.py:92-106`, `:125-129`).

PUNTO 4 — falló la mordida requerida y queda cubierto por F1. El test nuevo prueba que `set_agents_app` deriva del registro, pero no que el registro conserve el contenido correcto.
