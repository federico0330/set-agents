# PKG-B residue matrix (T-104)

Feature `035-panel-honesto-consola-y-tips` · path **(b)** — nothing moved; each row records the concrete anchor and the experiment/read performed by the implementer (2026-08-20).

| comando | dependencia concreta | experimento o lectura hecha | resultado |
|---|---|---|---|
| `cmd_route_explain` | `PROJECT_KEY` via `_routing_store()` (`set_agents_app.py:68-73`); `ROUTING_WARNINGS` via `_routing_output()` (`:524-527`) | Read `:550-572`: body calls `routing_catalog` then `_routing_output`; `_routing_store()` reads `PROJECT_KEY` at `:71`; `_routing_output` merges `ROUTING_WARNINGS` at `:525-527`. Valve §11.2 (1) fails on both globals. | anclado |
| `cmd_routing_report` | `PROJECT_KEY` via `_routing_store().report()` (`:575-577`); `ROUTING_WARNINGS` via `_routing_output()` (`:582`) | Read `:575-583`: `_routing_store().report()` at `:577`; envelope via `_routing_output` at `:582`. Same globals as explain. | anclado |
| `cmd_route_doctor` | `PROJECT_KEY` via `_probe_cache_root()` → `_routing_store()` (`:76-89`, `:605-607`); `ROUTING_WARNINGS` patched on module in `tests/test_menu_ui.py:241` with in-process call `:246` | Read `:586-614` and `tests/test_menu_ui.py:241-246`: `route_doctor(..., cache_root=_probe_cache_root())`; test patches `app.ROUTING_WARNINGS` and calls `app.cmd_route_doctor` — valve §11.2 (3) fails. | anclado |
| `cmd_route_decide` | `PROJECT_KEY` in `_append_decision_log` (`:759`) and `_routing_store()` when not simulating (`:725`); `ROUTING_WARNINGS` via `_routing_output` throughout (`:697-779`) | Read `:671-779`: non-simulate path uses `_routing_store()`; log line writes `"project_key": PROJECT_KEY` at `:759`. Host policy also marks `--route-decide` declared-uncharacterizable in `PKG-B-characterization/MANIFEST.md`. | anclado |
| `cmd_route_dispatched` | `PROJECT_KEY` via `_lifecycle_command` → `_routing_store()` (`:782-787`, `:794-797`) | Read `:794-797` and `:782-787`: `action(_routing_store())` at `:786`; `_routing_store()` reads `PROJECT_KEY` at `:71`. | anclado |
| `cmd_route_quota_exhausted` | `PROJECT_KEY` via `_lifecycle_command` → `_routing_store()` (`:782-787`, `:800+`) | Read `:800-831`: delegates to `_lifecycle_command` which calls `_routing_store()` at `:786`. | anclado |
| `cmd_route_terminal` | `PROJECT_KEY` via `_lifecycle_command` → `_routing_store()` (`:782-787`, `:833+`) | Read `:833-864`: `_lifecycle_command("route-terminal", …)` at `:833-864` → `_routing_store()` at `:786`. | anclado |
| `cmd_routing_open_runs` | `PROJECT_KEY` via `_routing_store()` (`:866-868`) | Read `:866-871`: `_routing_store().open_runs()` at `:868`. | anclado |
| `cmd_routing_recent_writers` | `PROJECT_KEY` via `_routing_store()` (`:874-876`) | Read `:874-879`: `_routing_store().recent_verified_writers()` at `:876`. | anclado |
| `cmd_routing_decisions` | `PROJECT_KEY` filter on decision log entries (`set_agents_app.py:900`) | Read `:882-905`: iterates `_decision_log_path()` lines; keeps entries where `entry.get("project_key") == PROJECT_KEY` at `:900`. Store selection still uses `_routing_store()`/`:71`. | anclado |
| `cmd_routing_migrate` | `PROJECT_KEY` via `_routing_store().migrate(key)` after `project_key_for(ROOT, …)` (`:3619-3624`) | Read `:3619-3639`: `_routing_store().migrate(key)` at `:3624`; `_routing_store()` reads `PROJECT_KEY` at `:71`. No test patches `PROJECT_KEY` (`rg 'PROJECT_KEY' tests/*.py` → only `_TEST_PROJECT_KEY` in routing_store tests). | anclado |
| `cmd_vault_init` | `write_app_config` / `APP_CONFIG` / `STATE_DIR` (`:2895-2896`, definitions `:1075`/`:1086`) | Read `:2869-2897`: `write_app_config(vault=…)` at `:2895`; `grep patch.object(app.*APP_CONFIG tests/test_harness.py` → `:1912-1913`, `:1968-1969`, `:2072`, `:2095-2096`. Valve (1)+(3) fail. | anclado |
| `find_vault` | `app_config().get("vault")` fallback (`:2908-2910`) | Read `:2900-2911`: configured fallback reads `app_config()` at `:2908`; `app_config` tied to `APP_CONFIG`/`STATE_DIR` (`:1075+`). | anclado |
| `vault_link_private` | Imported from `vault_ops.py:207` at `set_agents_app.py:2854`; callee body has no `set_agents_app` globals (valve §11.2 passes for the function). Caller `cmd_vault_link` (`:3048`) still reads `write_app_config`/`find_vault` | AST compare (`vault_link_private` in both modules): identical. Removed duplicate shadow at former `:2989-3045` (F005 repair). `rg 'patch.*vault_link_private' tests/` → no patches on the symbol; tests exercise via CLI (`tests/test_harness.py:4192`). Valve (1) fails on **caller** `:3057` (`write_app_config`), not on callee. | anclado (caller) |
| `cmd_vault_doctor` | `STATE_DIR` via `_vault_doctor_marker_path` (`:3160-3162`, used `:3222+`) | Read `:3192-3222`: `_vault_doctor_marker_path(project_path)` at `:3222`; marker path uses `STATE_DIR` at `:3162`. `rg 'patch.object(app, "STATE_DIR"' tests/test_harness.py` → seven vault-doctor sites (`:4577`, `:4638`, `:4662`, `:4678`, `:4691`, `:4711`, `:5207`). Valve (3) fails. | anclado |
| `vault_menu` | Resolves `cmd_vault_init` / `cmd_vault_link` as globals in `set_agents_app.__dict__` (`:3356`, `:3375`) | Read `:3339-3375` and `tests/test_harness.py:3702-3703`: menu calls `cmd_vault_init`/`cmd_vault_link` by bare name; tests `patch.object(app, "cmd_vault_init")` — moving menu or callees makes patches hit wrong module (valve (3), design §11.1 F-B-ARCH-02). | anclado |

**Valve (T-103):** all sixteen rows fail at least one §11.2 condition; **zero** commands moved. No new files under `ai/scripts/`.
