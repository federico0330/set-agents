# PKG-C implementer evidence — techo-frontier-y-metrica

Package: PKG-C. Feature: 034-cuota-organica-y-writer-barato.
Spec hash `539a4ff6b58b9ca67df9342dddcc79783660dcac73ce987a5da58ce136bd9721`. HOW: ADR-0061 (amends ADR-0039 in part).
`strict_tdd`: false. Runtime: Cursor inherit. No `--route-decide`. No Engram.
`MODE_BUDGETS.scoped.max_spawns_per_package == 8` byte-equal (`model.py:123-128`).

Owned: `ai/scripts/feature_state_lib/model.py`, `ai/scripts/feature-state.py`,
`ai/scripts/feature_state_lib/cli_lifecycle.py`, `ai/scripts/feature_state_lib/render_status.py`,
`ai/scripts/cost-report.py`, `tests/test_harness.py`.

Side effects (orchestrator `--exception`):
- PROYECTO twins (tests execute this copy): `PROYECTO/ai/scripts/feature-state.py`,
  `PROYECTO/ai/scripts/feature_state_lib/{model,cli_lifecycle,render_status}.py`.
- Generated by `./build.sh` (not hand-edited): `Global/{opencode,claude-code,codex,pi,cursor}/hooks/feature_state_lib/{model,cli_lifecycle,render_status}.py`.
- This evidence file.

Not touched: PKG-A/B behavior except cheap-default classification against
`opencode/deepseek-v4-flash-free`. PKG-D Cursor pins / `generate.py` source. Engram.
`MODE_BUDGETS` values. Caps not written into feature JSON. `frontier_used` not under
`attempts`. cost-report.py:26-30 never-sum paragraph intact.

## AC → change → proof

| AC | Change (file:line) | Proof |
|---|---|---|
| AC-C.1 | Constants outside `MODE_BUDGETS`: `FRONTIER_CAP_PER_PACKAGE = 4`, `FRONTIER_CAP_PER_FEATURE = 16` (`model.py:133-135`). Additive `frontier_used` on feature (`:345`) and package (`:428`). Caps never stored in JSON. | `test_mode_budgets_scoped_max_spawns_stays_eight` OK. Scoped row byte-equal `:125`. |
| AC-C.2 | `is_frontier_spawn` (`model.py:689-699`): `--model` present, ≠ cheap BASE, role ≠ `local-gate-runner`, not P001. Judges and salvage count. Absent `--model` does not increment. | `test_cheap_spawn_does_not_increment_frontier` + `test_salvage_and_heavy_reviewer_increment_frontier_p001_does_not` OK. |
| AC-C.3 | Cap check in `cmd_record_spawn` **before** mint (`feature-state.py:440-461`). 5th → `FRONTIER_CAP_EXHAUSTED` + `HUMAN_DECISION_REQUIRED`. `max_spawns` untouched. Status column Frontier reads the constant (`render_status.py:236`, `:283`). | `test_fifth_frontier_of_a_package_is_rejected` OK. STATUS `4/4 pkg · 4/16 feat`. budgets.max_spawns_per_package still 8. |
| AC-C.4 | Same check runs before salvage mint and before a promoted non-cheap spawn. | `test_frontier_cap_beats_salvage_and_promotion` OK. Salvage not minted; promoted spawn not recorded. |
| AC-C.5 | `cost-report.py` S2 companion `render_organic_quota` (`:588`) after S2 tables, before never-sum disclaimer. `% green-on-first-attempt` per feature + filter total + `frontier_used/cap`. | Live `cost-report.py --project … --since 2026-08-10` printed the block; `:26-30` intact (`test_cost_report_never_sum_s1_s2_lines_stay_intact`). |
| AC-C.6 | Derived, not persisted (`green_on_first_attempt_outcome` `:532`). Numerador = gates pass AND `salvage is None`. Denominador = implementer-cheap that reached a gate. Absence → outside (n/a, not 0%/100%). Bite: salvage-green must not raise numerator. | `test_salvage_green_is_not_green_on_first_attempt`: 1/2 (50%), numerator ≠ 2. |
| ADR-0039 | Third counter shape `{"scope":"frontier","key":"used","grain":"package"|"feature"}` (`cli_lifecycle.py:474`, `_reset_blocker_counter` `:509-526`). Existing attempts/finding shapes unchanged. | `test_reopen_resets_only_the_frontier_counter_named_on_the_blocker`: package `frontier_used` → 0, feature stays 4, `attempts.spawns` stays 4. |

## Unittest cluster

```
python3 -m unittest \
  tests.test_harness.HarnessTests.test_mode_budgets_scoped_max_spawns_stays_eight \
  tests.test_harness.HarnessTests.test_cheap_spawn_does_not_increment_frontier \
  tests.test_harness.HarnessTests.test_salvage_and_heavy_reviewer_increment_frontier_p001_does_not \
  tests.test_harness.HarnessTests.test_fifth_frontier_of_a_package_is_rejected \
  tests.test_harness.HarnessTests.test_frontier_cap_beats_salvage_and_promotion \
  tests.test_harness.HarnessTests.test_reopen_resets_only_the_frontier_counter_named_on_the_blocker \
  tests.test_harness.HarnessTests.test_cost_report_never_sum_s1_s2_lines_stay_intact \
  tests.test_harness.HarnessTests.test_salvage_green_is_not_green_on_first_attempt \
  tests.test_harness.HarnessTests.test_cost_report_prints_two_never_summed_sections_named_by_source \
  tests.test_harness.HarnessTests.test_cheap_red_plus_salvage_red_counts_one_consecutive \
  tests.test_harness.HarnessTests.test_second_salvage_is_rejected \
  tests.test_harness.HarnessTests.test_record_spawn_warns_at_eighty_percent_of_the_mode_ceiling
............
Ran 12 tests in 13.243s
OK
```

## MODE_BUDGETS scoped=8

```
ai/scripts/feature_state_lib/model.py:123-128
    "scoped": {"max_spawns_per_package": 8, "max_deep_review_cycles": 2, "max_gate_failures_per_package": 3, "max_verifications_per_package": 6},
```

Still 8 after generate. Caps sit at `:133-134`, outside the dict.

## `cost-report.py --since 2026-08-10`

Section 2 metric block present. Live features without implementer-cheap+gate print `n/a (fuera del denominador)` + `frontier 0/16`, never 0%/100%. Never-sum disclaimer still printed. `:26-30` unchanged.

## `./build.sh --check` / `git diff --check`

```
SELF_SCAFFOLD_SYNC_OK files=23
GLOBAL_TREE_SYNC_OK harnesses=5
BUILD_CHECK_PASS
```

`python3 ai/scripts/heartbeat-run.py --interval 15 -- ./build.sh --check` → HEARTBEAT_EXIT=0.
`git diff --check` → DIFF_CHECK_EXIT=0.
