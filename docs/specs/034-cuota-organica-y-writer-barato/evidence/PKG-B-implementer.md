# PKG-B implementer evidence — escritor-barato-y-salvage

Package: PKG-B. Feature: 034-cuota-organica-y-writer-barato.
Spec hash `539a4ff6b58b9ca67df9342dddcc79783660dcac73ce987a5da58ce136bd9721`. HOW: ADR-0060 + ADR-0062.
`strict_tdd`: false (AC-B.2 still required a visible RED→GREEN bite via `cp`, never git checkout).
Runtime: Cursor. No `--route-decide`. No Engram. MODE_BUDGETS untouched (`model.py:123-128`).
`billing_rank` read-only (`catalog.py:196-207`).

Owned: `models.toml`, `ai/scripts/feature-state.py`, `ai/scripts/feature_state_lib/model.py`,
`ai/scripts/feature_state_lib/cli_repair.py`, `Global/_canonical/agents/orchestrator.md`,
`tests/test_harness.py`.

Side effects (orchestrator `--exception`):
- `ai/scripts/feature_state_lib/cli_lifecycle.py:321` — `create-package` copies `next_rung` → `writer_rung` (AC-B.6; compact_package cannot see feature state).
- PROYECTO twins: `PROYECTO/ai/scripts/feature-state.py`, `PROYECTO/ai/scripts/feature_state_lib/{model,cli_repair,cli_lifecycle}.py` (tests execute the PROYECTO copy).
- Generated by `./build.sh` (not hand-edited): `Global/{opencode,claude-code,codex,pi,cursor}/` orchestrator + OpenCode `code-rw` agent `model:` lines + `hooks/feature_state_lib/{model,cli_repair,cli_lifecycle}.py`.
- This evidence file.

Not touched: PKG-A risk-signal, PKG-C frontier cap / cost-report S2, PKG-D Cursor pins / `generate.py` / `AREA_FIELDS`, `billing_rank` body, `MODE_BUDGETS` values, 033, Engram, `@tier` agents on Cursor, `opencode/*` in `tiers.*`.

## V-B01 inventory (before pin)

Catalog rank 0 (`billing_rank` via subscription or `-free` suffix): 35 ids. Preferred `-free` among rank 0: 7 catalog zen ids.

```
-- rank0 with -free (preferred) --
  opencode-zen     opencode/deepseek-v4-flash-free
  opencode-zen     opencode/laguna-s-2.1-free
  opencode-zen     opencode/ling-3.0-tiny-free
  opencode-zen     opencode/longcat-2.0-free
  opencode-zen     opencode/mimo-v2.5-free
  opencode-zen     opencode/nemotron-3-ultra-free
  opencode-zen     opencode/north-mini-code-free
```

Live `opencode models opencode --pure` (62 lines, EXIT 0) included:

```
opencode/deepseek-v4-flash-free
opencode/hy3-free
opencode/laguna-s-2.1-free
opencode/mimo-v2.5-free
opencode/nemotron-3-ultra-free
opencode/nemotron-3.5-lightning-free
```

`hy3-free` / `nemotron-3.5-lightning-free` are live but **not** in `[catalog].opencode_zen` — not pinned (would invent a catalog id). `ling-3.0-tiny-free` / `longcat-2.0-free` / `north-mini-code-free` are catalog-only, not in the live list.

Probe cache `~/.local/state/set-agentes/routing-v2/probe-cache.json` pair `opencode|opencode-zen` also lists `deepseek-v4-flash-free`.

Tools floor: synthesized inferred routes get `["read", "shell", "write"]` (`inference.py:117`; hard exclusion `TOOLS_MISSING` at `service.py:368`; writer request floor `set_agents_app.py:731`). Vendor-reported tool schema of `deepseek-v4-flash-free` itself: **sin verificar** (the probe lists ids, not tools; ADR-0043: credential presence, not per-model capability). Routing-layer floor is verified for every inferred zen id.

Candidate chosen: `opencode/deepseek-v4-flash-free` — living, catalog, rank 0, `-free` preferred, already in tree on `local-gate-runner` (`models.toml:252`) and `app-runner` (`models.toml:194`).

## AC → change → proof

| AC | Change (file:line) | Proof |
|---|---|---|
| AC-B.1 | `[areas.implement].opencode = "opencode/deepseek-v4-flash-free"` (`models.toml:109-118`). Deleted spark overrides `[roles.frontend-engineer]` and `[roles.refactor-specialist].opencode` so they inherit area cheap (`models.toml` no longer has those opencode keys; refactor keeps `codex` at `:273-274`). Tiers luna/sol/terra kept (`:242-248`). Feature-new `writer_promotion.next_rung` / `writer_rung` default `"base"`, never `"fast"` (`model.py:140-144`, `:332-335`, `:413-414`; `cli_lifecycle.py:321`). | `test_new_feature_does_not_dispatch_implementer_at_fast` OK. Generated OpenCode BASE `implementer.md` `model: opencode/deepseek-v4-flash-free`; `@fast` stays `openai/gpt-5.6-luna`. Cursor still `inherit` (PKG-D). |
| AC-B.2 | Hot-path test rewritten, not deleted (`test_harness.py:742-794`). `product-analyst` out of the `-fast` loop. `implementer` + `debugger` assert `billing_rank==0` and not `endswith("-fast")`. Independence half conserved (`:788-794`). Comment cites 034 / ADR-0060, same pattern 0044 used for orchestrator. | Bite RED→GREEN below. Unittest OK. |
| AC-B.3 | No test forces `product-analyst` to `-fast` or `-free`. | `test_product_analyst_is_not_forced_to_fast_or_free` OK. |
| AC-B.4 | `record-spawn --salvage` once (`feature-state.py:425-430`, `:476-486`, argparse `:1008-1009`). Persists `package.salvage = {spawn_id, role, model, at}`. `repair-agent` pin stays cheap (inherits area; OpenCode generated `model: opencode/deepseek-v4-flash-free`). Cursor: doctrine override or `HUMAN_DECISION_REQUIRED`, no heavy pin (`orchestrator.md:531-536`). | `test_second_salvage_is_rejected` OK. |
| AC-B.5 | Second `--salvage` → `SALVAGE_ALREADY_USED` + `block_with_reason`. Second gate red after salvage → `HUMAN_DECISION_REQUIRED` (`cli_repair.py:49-53`). Distinct from ADR-0011 D2 (`feature-state.py:476-478`, `orchestrator.md:704-705`). | `test_cheap_red_plus_salvage_red_counts_one_consecutive` + `test_second_salvage_is_rejected` OK. |
| AC-B.6 | `record-gate`: +1 consecutive once per package if cheap was not green-on-first; salvage already set → do not increment; green-on-first (pass AND salvage is None AND no latch) resets to 0 (`cli_repair.py:42-60`). 2 consecutive → `next_rung` `base`→`balanced` (`model.py:287-294`). | cheap-red+salvage-red consecutive=1. Three synthetic packages: PKG-03 `writer_rung=balanced`, never `fast`. |
| AC-B.7 | Test rewritten, not deleted. `rg -- '-fast' tests/` remaining hits are comments, `assertFalse(...endswith("-fast"))`, judge fixtures, `debugger.tiers.fast` (@tier, not BASE), and `test_discovered_routes.py` hint comment. Independence `assertNotEqual` family loop still present (`:790-794`). | rg below. |

## Bite RED→GREEN (cp, not git)

Broke `[areas.implement].opencode` to `openai/gpt-5.6-fast`. Restored with `cp /tmp/pkg-b-tdd/models.toml.green models.toml` (also saved `tests/test_harness.py` via `cp`).

RED:

```
python3 -m unittest tests.test_harness.HarnessTests.test_repo_go_zen_routes_hot_path_to_fast_variants_and_keeps_reviewers_apart
FAIL: AssertionError: True is not false : implementer   # assertFalse(model_id.endswith("-fast"))
Ran 1 test in 0.699s
FAILED (failures=1)
RED_EXIT=1
```

GREEN (after `cp` restore):

```
Ran 1 test in 0.694s
OK
GREEN_EXIT=0
```

## Unittest cluster

```
python3 -m unittest \
  tests.test_harness.HarnessTests.test_repo_go_zen_routes_hot_path_to_fast_variants_and_keeps_reviewers_apart \
  tests.test_harness.HarnessTests.test_product_analyst_is_not_forced_to_fast_or_free \
  tests.test_harness.HarnessTests.test_new_feature_does_not_dispatch_implementer_at_fast \
  tests.test_harness.HarnessTests.test_cheap_red_plus_salvage_red_counts_one_consecutive \
  tests.test_harness.HarnessTests.test_second_salvage_is_rejected \
  tests.test_harness.HarnessTests.test_two_cheap_misses_promote_next_package_off_fast
......
Ran 6 tests in 7.985s
OK
```

## `./build.sh --check` / `git diff --check`

```
SELF_SCAFFOLD_SYNC_OK files=23
GLOBAL_TREE_SYNC_OK harnesses=5
BUILD_CHECK_PASS
```

`git diff --check` → exit 0.

Generated OpenCode BASE writers (`implementer`, `debugger`, `frontend-engineer`, `repair-agent`, `test-writer`, `integrator`, `refactor-specialist`) pin `opencode/deepseek-v4-flash-free`. `implementer@fast` remains `openai/gpt-5.6-luna`. Cursor `implementer.md` still `model: inherit` (PKG-D).
