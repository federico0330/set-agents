# PKG-D implementer evidence — pins-cursor-por-rol

Package: PKG-D. Feature: 034-cuota-organica-y-writer-barato.
Spec hash `539a4ff6b58b9ca67df9342dddcc79783660dcac73ce987a5da58ce136bd9721`. HOW: ADR-0063.
`strict_tdd`: false. Runtime: Cursor. No `--route-decide`. No Engram.
Cursor stays **out of** `RUNTIMES` (`models_config.py:44`).

Owned: `ai/scripts/generate.py`, `ai/scripts/models_config.py`, `models.toml`,
`tests/test_harness.py`.

Side effects:
- `Global/_shared/AGENTS.cursor.md` — doctrine source for `write_cursor_rule` / `AGENTS.md`.
- `tests/fixtures/models.toml` — fixture areas need `cursor=` once `AREA_FIELDS` includes it.
- Generated by `./build.sh` (not hand-edited): `Global/cursor/**`.
- This evidence file.

Not touched: `docs/specs/032-*`, 033, Engram, `hooks.json`, `RUNTIMES`, `--route-decide`
block (`generate.py:125-132` intact), OpenCode `@tier` emission (`generate.py:587`
loop still OpenCode-ONLY), heavy `repair-agent` pin.

## V-D01 measured slugs (orchestrator, 2026-08-19)

URLs:

- https://cursor.com/docs/subagents — `model` field legal; examples `composer-2`,
  `composer-2.5[]`, `composer-2.5[fast=false]`, `gpt-5.6-sol`, `claude-opus-5`,
  `claude-opus-5[effort=high]`, `inherit`.
- https://cursor.com/docs/models-and-pricing — Cursor Models pool (cheapest on this
  host) = Grok 4.6 / Grok 4.5 / Composer 2.5. Other Models: GPT-5.6 Luna ($0.2)
  through Sol ($5), Claude Opus 5.
- This session's picker includes grok (parent is Cursor Grok 4.6).

`[catalog].cursor` (`models.toml:26`):
`claude-opus-5`, `claude-opus-5[effort=high]`, `composer-2`, `composer-2.5`,
`composer-2.5[fast=false]`, `gpt-5.6-luna`, `gpt-5.6-sol`, `grok-4.5`, `grok-4.6`,
`inherit`. Pin outside that list → `die` (`models_config.py:637-638`).

## V-D02 family()

`family("cursor_model", …)` (`models_config.py:560-573`): `[families]` first, else the
raw slug. Live:

- `implementer` / `repair-agent` → `composer-2.5` → family `composer-2.5`
- `package-reviewer` / `adversarial-judge` / `spec-challenger` → `gpt-5.6-sol` → family `gpt-5.6-sol`

Two families. No noisy-degradation fallback required for the default pins. Doctrine
still names `record-subreview --evidence` (`generate.py:140-141`) for the collapse case.

## V-D03 invocation override

Cursor Task tool has a `model` parameter on subagent spawn. Salvage / promotion stay
invocation override (or `HUMAN_DECISION_REQUIRED`). `repair-agent` frontmatter stays
cheap. `@tier` variants remain OpenCode-ONLY.

## AC → change → proof

| AC | Change (file:line) | Proof |
|---|---|---|
| AC-D.1 | `generate.py:580` emits `model: {row['cursor_model']}` (not hardcoded inherit). `[areas.implement].cursor = "composer-2.5"` (`models.toml:134`). | `/tmp/034-pkg-d-out/cursor/agents/implementer.md:4` `model: composer-2.5`. `repair-agent.md:4` same. Zero `model: inherit` in that tree. |
| AC-D.1 salvage | `repair-agent` inherits cheap. Heavy pin dies (`generate.py:788-790`). | Bite: `[roles.repair-agent].cursor = "claude-opus-5"` → `CHECK_FAILED: cursor: repair-agent must pin the cheap code-rw model, not a heavy salvage pin` rc=2. |
| AC-D.2 | `[areas.docs].cursor = "claude-opus-5"` (`models.toml:119`). | `product-analyst.md:4` and `architect.md:4` = `claude-opus-5`, not forced to `composer-2.5`. |
| AC-D.3 | `[areas.audit].cursor` / `[areas.judge].cursor` = `gpt-5.6-sol` (`models.toml:162`, `:180`). `family()` cursor branch + `load_roles` field loop includes `cursor_model` (`models_config.py:667`). | Frontmatter `package-reviewer` ≠ implementer family. Test asserts `family()` inequality. |
| AC-D.4 | `validate_cursor_target` rewritten (`generate.py:741-800`), not deleted. `test_no_cursor_agent_pins_a_model` rewritten (`test_harness.py:14840-14870`), comment cites 034 / 032 AC-06 superseded. | Bite missing pin: drop `[areas.implement].cursor` → `unresolved cursor_model`. Bite inherit-universal: all `cursor="inherit"` → `separation violation` at load (same family). Test successor GREEN on real pins; `assertFalse(all inherit)`. |
| AC-D.5 | `RUNTIMES` unchanged (`models_config.py:44`). `--route-decide` prohibition intact (`generate.py:125-132`). No `hooks.json`. Roster + `readonly` tests still in `CursorRuntimeTargetTests`. | `self.assertNotIn("cursor", mc.RUNTIMES)`. `ls /tmp/034-pkg-d-out/cursor/hooks.json` → absent. No `@tier` under `cursor/agents`. |
| AC-D.6 | `CURSOR_DELEGATION_OVERRIDE` point 2 (`generate.py:133-144`); `AGENTS.cursor.md:12-19`. | `rg` of `No model is pinned\|every role inherits` in `Global/cursor` = 0. Detector remains in `validate_cursor_target` (`generate.py:755`) so a reverted doctrine dies. |

## Frontmatter (required)

| role | `model:` | note |
|---|---|---|
| implementer | `composer-2.5` | cheap Cursor counterpart of B's `opencode/deepseek-v4-flash-free` |
| repair-agent | `composer-2.5` | same cheap; not heavy |
| package-reviewer | `gpt-5.6-sol` | family ≠ implementer (`gpt-5.6-sol` vs `composer-2.5`) |
| adversarial-judge | `gpt-5.6-sol` | same as reviewer |
| architect / product-analyst | `claude-opus-5` | frontier allowed (AC-D.2) |

`Global/cursor/agents/implementer.md:4`, `repair-agent.md:4`, `package-reviewer.md:4`.

## Bites (temp models.toml via emit, not git)

```
BITE inherit rc 2
CHECK_FAILED: separation violation: spec-challenger shares cursor_model=inherit with implementation

BITE missing rc 2
CHECK_FAILED: test-writer: unresolved cursor_model (area implement)

BITE repair-heavy-frontier rc 2
CHECK_FAILED: cursor: repair-agent must pin the cheap code-rw model, not a heavy salvage pin
```

GREEN restored: repo `models.toml` untouched by bites; `./build.sh --check` PASS.

## Local validation

```
python3 ai/scripts/heartbeat-run.py --interval 15 -- ./build.sh --check
SELF_SCAFFOLD_SYNC_OK files=23
GLOBAL_TREE_SYNC_OK harnesses=5
BUILD_CHECK_PASS

./build.sh --output /tmp/034-pkg-d-out
CHECK_PASS: generated and validated

python3 -m unittest tests.test_harness.CursorRuntimeTargetTests
Ran 10 tests in 25.431s
OK

python3 -m unittest tests.test_harness.HarnessTests.test_models_config_resolves_area_and_role_override \
  tests.test_harness.HarnessTests.test_models_config_rejects_incomplete_area \
  tests.test_harness.HarnessTests.test_models_config_emit_roundtrip \
  tests.test_harness.HarnessTests.test_models_config_separation_violation \
  tests.test_harness.HarnessTests.test_repo_go_zen_routes_hot_path_to_fast_variants_and_keeps_reviewers_apart
Ran 5 tests in 5.093s
OK

git diff --check  → exit 0
rg -n 'No model is pinned|every role inherits' Global/cursor  → 0 matches
```

`AREA_FIELDS` (`models_config.py:39`) includes `"cursor"`. `resolve_role` exposes
`cursor_model` (`models_config.py:607`).
