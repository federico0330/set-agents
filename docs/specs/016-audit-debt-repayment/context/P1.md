# Context pack — P1 (priority debt on feature-state.py)

## Objective
Close PR-07 (`repair_entry` authoritative field, 6 mutation sites), PR-08 (extract
`_apply_verification_waiver`/`_apply_verdicts` from `cmd_record_verification`), PR-09
(docstring fix + ADR-0009 D7 pointer) on `ai/scripts/feature-state.py`, replicated
byte-identical on its twin `PROYECTO/ai/scripts/feature-state.py`. Covers AC-01, AC-02,
AC-03, AC-04, AC-05(a)+(b), AC-06, AC-07, AC-11 (regression floor).

## This file IS the harness running this very workflow
`ai/scripts/feature-state.py` is executed by the orchestrator to record every event of
this package's own lifecycle (`record-spawn`, `record-review`, `record-testing`, etc).
Edit it in place, run tests against the edited copy before any state-mutating command
that depends on the new behavior is trusted, and never leave it in a syntactically
broken state between edits — a broken harness cannot record its own repair. Prefer
small, individually-testable edits over one large rewrite.

## Relevant files
- `ai/scripts/feature-state.py` — target of all three PRs. Key regions:
  - `:2701-2719` `_repair_entered_from_review` — read `repair_entry` first (AC-02), fallback to log inference when absent/unrecognized, fix docstring (AC-06, remove "Only the first...").
  - `:2315` `cmd_record_review`, `:2472` `cmd_finalize_review_panel` (branch `verdict == "repair_required"`), `:3078` `cmd_record_delta_review`, `:3118` `cmd_record_testing` (`args.status == "fail"`), `:3149` `cmd_record_runtime_qa` (`args.status == "fail"`) — the 5 one-line assignments that also fix `data["phase"] = "PACKAGE_REPAIR"`; each gets `package["repair_entry"] = "review"|"review"|"delta_review"|"testing"|"runtime_qa"` respectively, same line.
  - `:2005-2024` `cmd_transition` — generic `data["phase"] = args.to_phase` (no literal string match); in the branch where `args.to_phase == "PACKAGE_REPAIR"`, add `package.pop("repair_entry", None)`.
  - `:3389` dry-run self-demo block — OUT OF UNIVERSE for AC-01/AC-02, do not touch.
  - `:2767-2879` `cmd_record_verification` — extract `_apply_verification_waiver` (waiver branch, ends `return True`) and `_apply_verdicts` (verdicts branch, includes the `_repair_entered_from_review` call at `:2867` which MUST stay in `_apply_verdicts`, never move to the waiver function). `cmd_record_verification` becomes the dispatcher: shared actor/phase/replay guards, then delegates.
- `PROYECTO/ai/scripts/feature-state.py` — byte-identical twin, verified by `./build.sh --check`. Apply every edit from the bullet above here too, identically. Do not treat as a second independent review target — it is the same content applied twice.
- `tests/test_harness.py` — extend, do not restructure. The 9 tests in AC-04 must keep passing with unmodified assertions (verify via `grep -c "assert" <range>` before/after per test, per AC-03). New tests: 4 for AC-02 (review/delta_review/testing/runtime_qa recognized values + unrecognized value falls to inference), 1 for the `cmd_transition` pop (AC-01, F-03), AC-05(a) behavior tests on the 4 named fixtures against the two extracted functions directly (pin `StateError` messages, field mutations, `record_event` payload; normalize/freeze `now()`).
- `docs/adr/0009-finding-verification.md` — section "D7 (new)" at `:134-141` (the "2 for feature/scoped, 1 for quick-fix/incident" text) gets one added pointer line at its end referencing "D7 corrected again" (`:178`). Do not rewrite the 2/1 value itself.

## Constraints / invariants
- No opportunistic refactor beyond the two named extractions (PR-08) and the `repair_entry` field (PR-07).
- `cmd_record_verification`'s public CLI surface (flags, arg names, exit codes, `output_state` format) is unchanged.
- `_repair_entered_from_review` fallback-to-inference path must stay byte-identical for any state file lacking `repair_entry` (all `ai/state/features/*.json` today lack it — verified live, `grep -rl repair_entry ai/state/features/` returns nothing).
- AC-05(b) is a reviewer-only obligation (line-by-line diff of the extraction), not a test — do not attempt to encode it as a test.

## Local validation
- `python3 -m unittest discover -s tests -v` — 0 fail, 0 skip, count does not drop (baseline measured live at 572 defs across tests/*.py before this package; must rise by the new tests named above).
- `python3 -m unittest tests.test_harness -v -k verif` and the 9 named AC-04 tests individually.
- `./build.sh --check` must report `SELF_SCAFFOLD_SYNC_OK files=2` (no `SELF_SCAFFOLD_DRIFT`).
- `./ai/scripts/verify.sh` if it applies to the touched scope.
- `grep -n "repair_entry" ai/scripts/feature-state.py` — 6 mutation sites (5 value writes + 1 pop) plus the read in `_repair_entered_from_review`.
- `grep -n "Only the first is a findings problem" ai/scripts/feature-state.py` — no results after the fix.
- `grep -n "D7 corrected again" docs/adr/0009-finding-verification.md` — referenced from within "D7 (new)" too.
- Manual full-content review of `git diff tests/test_harness.py` before close (AC-03) — no removed line falls inside an existing `def test_` body.

## Out of scope
- PR-06 (budget double-counter), PR-10 (test suite shape), PR-11 (mutate compare-and-swap) — do not touch `metrics.verifications` semantics, do not restructure/rename/merge existing tests beyond what PR-07/PR-08 strictly require, do not touch `mutate()`/`--expect-revision`.
- `RoutingService`, `service.py`, `package-gate-runner.md` — belong to P2, not this package.
- ADR-0009 sections other than the single pointer line in "D7 (new)".
