# Context pack — P2 (minor cleanup, disjoint from P1)

## Objective
Two independent, low-risk hygiene fixes with no shared files with P1: (a) strip a client
machine's absolute paths/business module names from a portable agent template, (b)
add a non-blocking, additive reason_code when `_effective_runtime` silently redirects a
runtime. Covers AC-08, AC-09, AC-10.

## Relevant files
- `Global/_canonical/opencode-agents/package-gate-runner.md` — portable subagent template, copied to every machine that installs the harness. Currently hardcodes one client's absolute paths and business module names inside `permission.read`/`permission.edit`/`permission.external_directory`/`permission.bash` (lines 10-14, 29-33, 64-70) and in the frontmatter `description:` (line 2, `"RPL-P0A"`). Remove/genericize all of: `/home/`, `/Users/`, `/tmp/opencode/`, hash `4ef70b0ab6da`, `contabilium-ingestion`, `replenishment-v2`, `RPL-P0A` (any case incl. lowercase `rpl-p0a-...`), `iey-ai` — case-insensitive universe, verified live against the real file. Preserve YAML frontmatter validity, the same top-level `permission` keys (`read`, `edit`, `glob`, `grep`, `list`, `task`, `question`, `webfetch`, `websearch`, `lsp`, `skill`, `todowrite`, `doom_loop`, `external_directory`, `bash`), and the default-deny discipline on `"*"` where it already exists. Placeholder vs. outright removal of the now-dead entries is your call — the template's structure and named gates must survive.
- `ai/scripts/routing_core/service.py` — `_effective_runtime` at `:133-159`; the redirect happens at the return in that method. `route()` at `:154+` builds the `RouteDecision` (positional field 6 is `reasons`, a tuple — today `()` in the writer branch, `("REVIEW_IDENTITY_UNVERIFIED",)` in the review/simulate branch when the reviewer is unverified). Add a new non-blocking code (name TBD, e.g. something like `RUNTIME_REDIRECTED` or similar, distinguishable from `REVIEW_IDENTITY_UNVERIFIED`) into `reasons` (or the audit trail, if that turns out to be the better seam) identifying that a redirect happened, the requested runtime, and the effective runtime — additive only. Do not touch `_PROVIDER_RUNTIME_REDIRECTS`, `_NEVER_REDIRECT_FROM_RUNTIMES`, or when the redirect fires.
- `tests/test_routing.py` — the 5 `test_ac01_shape_*` tests (`:759,787,817,834,861`) plus `test_ac01_new_pairs_are_registered_in_the_closed_pair_table_only` (`:2988`) are the full universe selected by `-k test_ac01`; all 6 must keep passing with unmodified `runtime`/`success` assertions. Add a new test asserting: before/after `RouteDecision` on a forced redirect (same fixture shape (b) already uses) has identical `success`/`runtime`/`identity`/`fallback`, plus the new element present where it wasn't before. Also add/confirm assertions on shape (c) and shape (e) that neither emits the new code, for their two distinct reasons (pair present & already authenticated vs. `pi` categorically exempt).

## Constraints / invariants
- No change to `RouteDecision`'s `success`/`runtime`/`identity`/`fallback` for any existing shape — additive field/tuple element only.
- No change to when a redirect occurs, only to whether it is now observable.
- No change to `package-gate-runner.md`'s gate structure or default-deny discipline, only removal/genericization of the dead client-specific entries.
- Assume no other automated consumer of `package-gate-runner.md`'s current absolute paths exists (spec assumption; if you find one, stop and flag — do not silently break it).

## Local validation
- `grep -inE "/home/|/Users/|/tmp/opencode/|4ef70b0ab6da|contabilium-ingestion|replenishment-v2|RPL-P0A|iey-ai" Global/_canonical/opencode-agents/package-gate-runner.md` — no results.
- YAML frontmatter parse check on the template (whatever parser the repo already uses for agent templates).
- `python3 -m unittest tests.test_routing -v -k test_ac01` — all 6 selected tests pass unmodified plus the new redirect-observability test.
- `python3 -m unittest discover -s tests -v` — 0 fail, 0 skip, count does not drop.
- `./ai/scripts/verify.sh` if it applies to the touched scope.

## Out of scope
- `ai/scripts/feature-state.py`, `PROYECTO/ai/scripts/feature-state.py`, `tests/test_harness.py`, `docs/adr/0009-*.md` — belong to P1, not this package.
- ADR-0019 and `015-anthropic-dispatch-parity`'s ACs — do not reopen that contract; only name what the reason_code audit trail requires.
- Any other cleanup of `package-gate-runner.md` beyond the named client-specific entries.
