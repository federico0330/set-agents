# Agent Harness Verification Matrix

| ID | Requirement | Initial State | Initial Evidence | Change Applied | Test Run | Final State | Final Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| V-01 | Requirements and human approval remain before implementation | Preserved declaratively | `Global/_canonical/agents/orchestrator.md` front half | Commands require `feature-state.py init` only after approval | `python3 -m unittest tests.test_harness.HarnessTests.test_package_workflow_happy_path_executes_real_transitions` | PASS | State starts at `PACKAGE_PLANNING` after approved spec |
| V-02 | Package implementation replaces task-by-task deep audit | Partially declarative | `PROYECTO/prompt.md` and README still referenced task audit loop | Rewrote active prompt/README and command docs | `test_active_docs_do_not_teach_task_by_task_deep_audit` | PASS | Active docs recommend `/feature-batch` and package review |
| V-03 | Packages contain multiple tasks when reasonable | Prompt-only | No executable check | `create-package` rejects non-small single-task normal packages | `test_package_workflow_happy_path_executes_real_transitions` | PASS | Test package has three completed tasks |
| V-04 | Local validation per task | Prompt-only | Implementer prompt only | `complete-task` requires at least one `--validation` | `test_package_workflow_happy_path_executes_real_transitions` | PASS | Each task records focused local validation |
| V-05 | Deep review once per integrated package | Prompt-only | Prior dry-run fabricated terminal state | State machine only enters `PACKAGE_REVIEW` after gates/tasks/diff/integration | `test_package_review_requires_completed_tasks`, `test_failed_gate_blocks_package_review_path` | PASS | Premature review and failing gate are rejected |
| V-06 | Independent reviewer | Configured | `package-reviewer` is read-only | Orchestrator direct delegation excludes historical audit chain | `./build.sh --check` plus generation validation | PASS | Generated orchestrator denies direct historical auditors |
| V-07 | Consolidated findings | Declarative | Reviewer prompt schema | `record-review` stores multiple findings in one event | `test_consolidated_findings_and_delta_review_do_not_increment_full_review` | PASS | Findings `F-001` and `F-002` recorded together |
| V-08 | Consolidated repair | Declarative | Repair prompt | `record-repair` stores finding batch and increments repair batch | `test_consolidated_findings_and_delta_review_do_not_increment_full_review` | PASS | One repair batch covers both findings |
| V-09 | Focused delta review | Declarative | Delta reviewer prompt | `record-delta-review` does not increment package review count unless full review requested | `test_consolidated_findings_and_delta_review_do_not_increment_full_review` | PASS | `package_reviews=1`, `delta_reviews=1` |
| V-10 | Retry budget enforced | Validator only | Could detect but not govern | `record-review` and delta full-review path enforce budget and block | `test_retry_budget_blocks_third_review_cycle` | PASS | State becomes `BLOCKED` when budget exhausted |
| V-11 | Terminal states enforced | Missing | No transition preconditions | `DONE` requires accepted packages and passing global gates | `test_package_workflow_happy_path_executes_real_transitions` | PASS | `DONE` reached only after global gate |
| V-12 | Resume from persisted state | Partial | `resume-feature` was prompt-only | `resume` returns deterministic `next` from state | `test_resume_and_invalid_transition_are_deterministic` | PASS | Resume continues implementation without replaying spec |
| V-13 | Ownership is deterministic | Missing | Ownership only in prompts | Added `check-owned-paths.py` gate | `test_owned_paths_gate` | PASS | Out-of-scope and read-only paths fail |
| V-14 | Complexity/model routing is persisted | Missing | Models were static and cheap for packages | `create-package/update-package` persist `complexity`, `selected_role`, `selected_model`, `routing_reason`; package roles moved to Terra | `test_package_workflow_happy_path_executes_real_transitions`, `opencode models` | PASS | State records medium package routed to Terra-class model |
| V-15 | Delegation graph is broad but state-governed | Previously either too broad or too restrictive | Orchestrator could either serial-audit freely or be unable to call useful specialists | Generator allowlist includes specialist reviewers, while review-panel state commands make them one bounded review cycle | `test_orchestrator_delegation_graph_is_broad_but_state_governed`, `test_review_panel_allows_many_subagents_as_one_cycle` | PASS | Specialists are callable; multiple subreviews consume one package review cycle |
| V-16 | User questions restricted | Configured | Workers/reviewers had question denied | Kept generated permissions; workflow records blockers instead of routine questions | `./build.sh --check` | PASS | OpenCode frontmatter retains `question: deny` for subagents |
| V-17 | Documentation consistency | Contradictory | README/prompt taught old loop | Rewrote active docs and example tasks | `test_active_docs_do_not_teach_task_by_task_deep_audit` | PASS | Active docs no longer reference `/next-task T-001` or direct task auditors |
| V-18 | Runtime QA before package acceptance | Missing | Testing meant mostly code/gates | Added `record-testing` and `record-runtime-qa` before `accept-package` | `test_package_workflow_happy_path_executes_real_transitions`, `feature-state.py dry-run` | PASS | Package acceptance requires tests and browser/runtime evidence |
| V-19 | A review finding is refuted before it is repaired | Missing | Findings went straight from the panel to `repair-agent`; the only terminal statuses were `closed` (set by repair) and `accepted`, so a reviewer who was wrong forced a code change and burned one of two review cycles | Added the read-only `finding-verifier` role, the `refuted` terminal status and `record-verification`; only the verifier may refute, refutation requires shaped evidence, `upheld` is terminal for verification, the pass is budgeted, and `record-repair` refuses a `medium+` finding that never went through it | `test_refuted_finding_never_reaches_repair_and_never_blocks_acceptance`, `test_only_the_verifier_may_refute_and_never_its_own_finding`, `test_upheld_is_sticky_and_verification_has_a_physical_budget`, `test_verification_is_required_in_code_not_only_in_prose`, `feature-state.py dry-run` | PASS | A false finding is killed with evidence on the record instead of being patched; verification never consumes a review cycle |
